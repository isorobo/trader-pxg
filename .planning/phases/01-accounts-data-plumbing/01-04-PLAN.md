---
phase: 01-accounts-data-plumbing
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - trader/data/stock_source.py
  - tests/test_stock_source.py
autonomous: true
requirements: [ACCT-04]

must_haves:
  truths:
    - "A stock symbol's daily bars come back with a plain UTC calendar-date string, never a tz-aware America/New_York timestamp"
    - "Requesting no start date fetches the fully available history, not a truncated default window"
  artifacts:
    - path: "trader/data/stock_source.py"
      provides: "fetch_stock_bars(symbol, start=None, end=None), normalize_stock_bars(df)"
      contains: "def fetch_stock_bars"
  key_links:
    - from: "trader/data/stock_source.py normalize_stock_bars"
      to: "trader/data/db.py write_bars_cache row shape"
      via: "list[dict] with keys ts/open/high/low/close/volume"
      pattern: "\"ts\":"
---

<objective>
Build the stock half of the daily-bars pipeline: fetch via `yfinance`, normalize its timezone-aware `America/New_York` index to a plain UTC calendar-date string (01-RESEARCH.md Pitfall 1), and return rows shaped exactly as `trader/data/db.py`'s `write_bars_cache` expects.

Purpose: D-01 confirms yfinance as the stock source; D-11's "UTC timestamps" claim is corrected by research — the raw index is NOT UTC, so this plan's normalization step is the load-bearing fix.
Output: `trader/data/stock_source.py` with `fetch_stock_bars` and `normalize_stock_bars`, fully tested against a mocked `yfinance.Ticker`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\phases\01-accounts-data-plumbing\01-CONTEXT.md
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\phases\01-accounts-data-plumbing\01-RESEARCH.md
</context>

<interfaces>
<!-- Contract this plan must produce, consumed by Plan 01-06's get_daily_bars -->
Row shape returned by normalize_stock_bars (list[dict]), one dict per bar:
```python
{"ts": "2026-07-24", "open": 210.5, "high": 212.0, "low": 209.8, "close": 211.9, "volume": 51234000.0}
```
This exact key set (ts, open, high, low, close, volume) matches trader/data/db.py's write_bars_cache row contract from Plan 01-01 — no renaming needed at the call site in Plan 01-06.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for stock bar fetch and UTC normalization (RED)</name>
  <read_first>01-RESEARCH.md "Architecture Patterns" Pattern 2, "Common Pitfalls" Pitfall 1, "Code Examples" (yfinance section)</read_first>
  <files>tests/test_stock_source.py</files>
  <behavior>
    - test_stock_bars_normalize_to_utc_date: given a tz-aware (America/New_York) pandas DataFrame fixture built locally in this test file with columns Open/High/Low/Close/Volume/Dividends/"Stock Splits" (matching yfinance's live-tested shape), normalize_stock_bars returns a list[dict] with "ts" as a plain YYYY-MM-DD UTC-converted date string and exactly the keys ts/open/high/low/close/volume (Dividends and Stock Splits dropped).
    - test_fetch_stock_bars_uses_period_max_when_no_start: mocking yfinance.Ticker so .history is a Mock, fetch_stock_bars("AAPL") (no start) asserts .history was called with period="max", auto_adjust=True.
    - test_fetch_stock_bars_uses_start_end_when_given: fetch_stock_bars("AAPL", start="2024-01-01", end="2024-06-01") asserts .history was called with start="2024-01-01", end="2024-06-01", auto_adjust=True.
    - test_normalize_stock_bars_raises_on_missing_columns: given a DataFrame missing an expected column (e.g. no Volume), normalize_stock_bars raises a clear exception rather than a bare KeyError propagating from deep inside the function.
  </behavior>
  <action>
  Build the tz-aware fixture DataFrame locally within this test file using pandas.date_range(..., tz="America/New_York") as the index — do not add it to tests/conftest.py. Patch yfinance.Ticker via unittest.mock.patch("trader.data.stock_source.yfinance.Ticker") for the fetch-call assertions. Write the four tests in <behavior> against trader.data.stock_source's not-yet-existing fetch_stock_bars and normalize_stock_bars. Run pytest and confirm all 4 fail with ModuleNotFoundError (RED).
  </action>
  <verify>
    <automated>python -m pytest tests/test_stock_source.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_stock_source.py exists with exactly 4 test functions named as listed in <behavior>
    - `python -m pytest tests/test_stock_source.py --collect-only -q` lists all 4 with zero collection errors
    - Running the suite fails referencing trader.data.stock_source, confirming RED
  </acceptance_criteria>
  <done>4 failing tests committed, pinning the tz-normalization contract before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement stock bar fetch and UTC-date normalization (GREEN)</name>
  <read_first>tests/test_stock_source.py (just written), 01-RESEARCH.md "Code Examples" yfinance section</read_first>
  <files>trader/data/stock_source.py</files>
  <action>
  Implement normalize_stock_bars(df: pd.DataFrame) -> list[dict]: convert df.index with .tz_convert("UTC").strftime("%Y-%m-%d") into a "ts" column, select and rename Open/High/Low/Close/Volume to open/high/low/close/volume, drop Dividends and Stock Splits, and return one dict per row in the exact shape from <interfaces>. Raise a clear ValueError (not a bare KeyError) if an expected input column is missing. Implement fetch_stock_bars(symbol: str, start: str | None = None, end: str | None = None) -> list[dict]: construct yfinance.Ticker(symbol); call .history(period="max", auto_adjust=True) when start is None, else .history(start=start, end=end, auto_adjust=True); pass the result through normalize_stock_bars. Run pytest until all 4 tests from Task 1 pass.
  </action>
  <verify>
    <automated>python -m pytest tests/test_stock_source.py tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - normalize_stock_bars never returns a "ts" value with a time-of-day component or a non-UTC offset
    - fetch_stock_bars with no start argument requests the fully available history (period="max"), not a truncated default window
    - Full test suite remains green (no regression to Phase 0 or Plan 01-01's tests)
  </acceptance_criteria>
  <done>stock_source.py implements fetch_stock_bars and normalize_stock_bars exactly per contract; all 4 tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Yahoo Finance response (via yfinance) → normalize_stock_bars | untrusted third-party data assumed to have a stable column shape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | normalize_stock_bars column assumptions | mitigate | Raises a clear ValueError when an expected column (Open/High/Low/Close/Volume) is missing, instead of allowing a bare KeyError or silent NaN propagation downstream |
| T-01-09 | Information Disclosure | none — yfinance requires no API key | accept | No secret is read or transmitted by this module |
</threat_model>

<verification>
`python -m pytest tests/test_stock_source.py -q` green (mocked yfinance, no live network call in this plan's tests — the live check happens once in Plan 01-06's exit-criterion task).
</verification>

<success_criteria>
fetch_stock_bars and normalize_stock_bars implement D-01's stock path and correct the tz-normalization gap identified in 01-RESEARCH.md Pitfall 1; all tests green.
</success_criteria>

<output>
Create `.planning/phases/01-accounts-data-plumbing/01-04-SUMMARY.md` when done
</output>
