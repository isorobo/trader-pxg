---
phase: 01-accounts-data-plumbing
plan: 06
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-04", "01-05"]
files_modified:
  - trader/data/api.py
  - trader/data/exit_criterion_smoke.py
  - tests/test_data_api.py
autonomous: true
requirements: [ACCT-04, ACCT-07]

must_haves:
  truths:
    - "One function call returns multi-year daily bars for any US stock"
    - "The same one function call returns multi-year daily bars for any major crypto pair"
    - "A second call for an already-cached range makes zero network calls"
    - "bars.venue records true fetch provenance ('yahoo' or 'binance'), never a fee-model venue"
  artifacts:
    - path: "trader/data/api.py"
      provides: "get_daily_bars(symbol, start=None, end=None, asset_class=None, conn=None) -> pandas.DataFrame"
      contains: "def get_daily_bars"
  key_links:
    - from: "trader/data/api.py get_daily_bars"
      to: "trader/data/db.py read_bars_cache / write_bars_cache"
      via: "cache-first read, fetch-on-miss write-through"
      pattern: "read_bars_cache"
    - from: "trader/data/api.py get_daily_bars"
      to: "trader/data/stock_source.py / trader/data/crypto_source.py"
      via: "asset_class-routed fetcher dispatch"
      pattern: "fetch_stock_bars|fetch_crypto_bars"
---

<objective>
Wire the Phase 1 exit criterion: `get_daily_bars(symbol, start=None, end=None) -> DataFrame`, a cache-first router over the stock and crypto fetchers built in Plans 01-04/01-05, backed by the schema and cache helpers from Plan 01-01.

Purpose: This is the phase's single acceptance test (D-10/D-11). Task 3 in this plan runs the actual exit-criterion proof live, against real Yahoo Finance and Binance endpoints — no mocks — confirming ACCT-07 end-to-end.
Output: `trader/data/api.py` with `get_daily_bars`; a live acceptance script; a fully mocked test suite covering routing, cache-hit, and cache-miss-then-hit behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\phases\01-accounts-data-plumbing\01-CONTEXT.md
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\phases\01-accounts-data-plumbing\01-RESEARCH.md
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\trader\ground_truth\smoke.py
</context>

<interfaces>
<!-- Contracts this plan consumes, produced by earlier Phase 1 plans -->
From trader/data/db.py (Plan 01-01):
```python
get_connection(db_path="data/trader.db") -> sqlite3.Connection
get_instrument(conn, symbol, venue) -> dict | None   # keys: symbol, venue, asset_class, coingecko_id, override
upsert_instrument(conn, symbol, venue, asset_class, coingecko_id=None, override=None) -> None
read_bars_cache(conn, venue, symbol, timeframe, start=None, end=None) -> list[dict]  # keys: ts, open, high, low, close, volume
write_bars_cache(conn, venue, symbol, timeframe, rows) -> int
```
From trader/data/stock_source.py (Plan 01-04):
```python
fetch_stock_bars(symbol, start=None, end=None) -> list[dict]  # keys: ts, open, high, low, close, volume
```
From trader/data/crypto_source.py (Plan 01-05):
```python
fetch_crypto_bars(symbol, since_ms=None) -> list[dict]  # keys: ts, open, high, low, close, volume
CRYPTO_VENUE = "binance"
```
Venue mapping this plan owns: asset_class "stock" -> venue "yahoo"; asset_class "crypto_major" or "memecoin" -> venue CRYPTO_VENUE ("binance"). This is true data provenance, not the Kraken fee-model venue (01-RESEARCH.md Pitfall 5, orchestrator's locked decision).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for get_daily_bars routing, cache-first behavior, and the DataFrame contract (RED)</name>
  <read_first>01-RESEARCH.md "Architecture Patterns" Pattern 1 and the System Architecture Diagram, "Validation Architecture" Phase Requirements to Test Map, 01-CONTEXT.md D-10/D-11</read_first>
  <files>tests/test_data_api.py</files>
  <behavior>
    - test_get_daily_bars_stock_contract: with fetch_stock_bars mocked to return a small fixed list of bar dicts, get_daily_bars("AAPL", asset_class="stock", conn=<temp db>) returns a pandas DataFrame indexed by date with columns open, high, low, close, volume.
    - test_get_daily_bars_crypto_contract: with fetch_crypto_bars mocked similarly, get_daily_bars("BTC/USDT", asset_class="crypto_major", conn=<temp db>) returns a DataFrame via the crypto path, and the written bars row has venue="binance".
    - test_cache_hit_skips_fetch: pre-seed the bars cache (via db.write_bars_cache) to fully cover the requested range, then call get_daily_bars; assert the mocked fetch_stock_bars/fetch_crypto_bars was never called.
    - test_cache_miss_then_cache_hit: starting from an empty cache, call get_daily_bars once (asserting the mocked fetcher WAS called and the cache is now populated), then call it again for the same range (asserting the fetcher was NOT called the second time, and the returned DataFrame content is identical).
    - test_get_daily_bars_resolves_asset_class_from_instruments_table: upsert_instrument for a symbol with asset_class="stock" via db.upsert_instrument (no explicit asset_class= passed to get_daily_bars this time), confirming get_daily_bars still routes to the stock fetcher using the stored instruments row (D-10's primary resolution path, not just the explicit-argument fallback).
  </behavior>
  <action>
  Define a local `data_conn` fixture inside this test file (built from tmp_db_path, calling trader.data.db.get_connection) — do not modify tests/conftest.py. Patch trader.data.api.stock_source.fetch_stock_bars and trader.data.api.crypto_source.fetch_crypto_bars via unittest.mock.patch for the mocked-fetch tests. Write the five tests in <behavior> against trader.data.api's not-yet-existing get_daily_bars. Run pytest and confirm all 5 fail with ModuleNotFoundError (RED).
  </action>
  <verify>
    <automated>python -m pytest tests/test_data_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_data_api.py exists with exactly 5 test functions named as listed in <behavior>
    - `python -m pytest tests/test_data_api.py --collect-only -q` lists all 5 with zero collection errors
    - Running the suite fails referencing trader.data.api, confirming RED
  </acceptance_criteria>
  <done>5 failing tests committed, pinning get_daily_bars's full contract before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement get_daily_bars — cache-first routing over the stock and crypto fetchers (GREEN)</name>
  <read_first>tests/test_data_api.py (just written), trader/data/db.py, trader/data/stock_source.py, trader/data/crypto_source.py, 01-RESEARCH.md "Architecture Patterns" Pattern 1</read_first>
  <files>trader/data/api.py</files>
  <action>
  Implement resolve_instrument(conn, symbol: str, asset_class: str | None) -> tuple[str, str]: if asset_class is given, use it directly; otherwise query instruments WHERE symbol = ? LIMIT 1 (Phase 1 does not yet support one symbol under two different asset classes simultaneously) and use its override column if set, else its asset_class column; if neither an explicit argument nor an instruments-table row exists, fall back to a symbol-shape heuristic (contains "/" -> "crypto_major", else -> "stock"). Map the resolved asset_class to a venue: "stock" -> "yahoo"; "crypto_major" or "memecoin" -> crypto_source.CRYPTO_VENUE. Implement get_daily_bars(symbol: str, start: str | None = None, end: str | None = None, asset_class: str | None = None, conn: sqlite3.Connection | None = None) -> pandas.DataFrame: default conn to db.get_connection() when None; resolve (resolved_asset_class, venue) via resolve_instrument; read cached = db.read_bars_cache(conn, venue, symbol, "daily", start, end); treat the cache as a hit when start is None and cached is non-empty, OR when start is given and the earliest cached row's ts <= start; on a cache miss, dispatch to stock_source.fetch_stock_bars(symbol, start, end) when resolved_asset_class == "stock", else crypto_source.fetch_crypto_bars(symbol), write the result via db.write_bars_cache(conn, venue, symbol, "daily", fresh_rows), and re-read the cache; build and return a pandas DataFrame indexed by pd.to_datetime(ts) with columns open, high, low, close, volume, sorted ascending by date. Run pytest until all 5 tests from Task 1 pass, then run the full suite to confirm no regressions.
  </action>
  <verify>
    <automated>python -m pytest tests/test_data_api.py tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - get_daily_bars returns a pandas.DataFrame with exactly the columns open, high, low, close, volume, indexed by date
    - A cache-hit call makes zero calls to fetch_stock_bars/fetch_crypto_bars (verified via mock.assert_not_called in the test suite)
    - A cache-miss call writes through to the bars table with venue="yahoo" for stock rows and venue="binance" for crypto rows — never a Kraken fee-model label
    - Full test suite (`pytest tests/ -q`) is green with no regression to Phase 0 or earlier Phase 1 plans
  </acceptance_criteria>
  <done>get_daily_bars implements the full D-10/D-11 contract, routing correctly and caching correctly; all 5 tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Live exit-criterion acceptance run — real stock and real crypto pair, one function call each</name>
  <read_first>trader/ground_truth/smoke.py (live smoke-test entry-point pattern), trader/data/api.py (just implemented)</read_first>
  <files>trader/data/exit_criterion_smoke.py</files>
  <action>
  Create trader/data/exit_criterion_smoke.py mirroring trader/ground_truth/smoke.py's pattern: a main() entry point (no mocks) that calls get_daily_bars("AAPL", asset_class="stock") and get_daily_bars("BTC/USDT", asset_class="crypto_major") against the real Yahoo Finance and Binance endpoints, using the project's actual data/trader.db (default conn), and prints each result's row count and date range. Run `python -m trader.data.exit_criterion_smoke` once and confirm both calls return more than 500 rows (roughly 2+ years of daily bars), proving the Phase 1 exit criterion end-to-end with one function call per asset class.
  </action>
  <verify>
    <automated>python -m trader.data.exit_criterion_smoke</automated>
  </verify>
  <acceptance_criteria>
    - The real call to get_daily_bars("AAPL", asset_class="stock") returns a DataFrame with more than 500 rows
    - The real call to get_daily_bars("BTC/USDT", asset_class="crypto_major") returns a DataFrame with more than 500 rows
    - Both results came from the same get_daily_bars function — the phase's literal "one function call" exit criterion
    - The script exits 0 with no unhandled exception
  </acceptance_criteria>
  <done>Live run proves ACCT-07's exit criterion: one function call returns multi-year daily bars for a real US stock and a real major crypto pair.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| get_daily_bars caller (later phases) -> trader/data/db.py | api.py never builds SQL directly; delegates entirely to db.py's parameterized helpers |
| Live smoke run -> real Yahoo/Binance endpoints | outbound HTTPS calls with no secrets involved (both are keyless public data) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | SQL construction | accept (delegated) | api.py never constructs SQL itself; all persistence goes through trader/data/db.py's already-parameterized helpers from Plan 01-01 |
| T-01-11 | Tampering | bars.venue provenance mislabeling (Pitfall 5) | mitigate | api.py hardcodes venue="yahoo" for the stock path and venue=crypto_source.CRYPTO_VENUE ("binance") for the crypto path as constants — never derived from or conflated with the Kraken fee-model assumption, directly implementing the orchestrator's locked decision |
| T-01-12 | Denial of Service | live smoke-test network calls | accept | Task 3 runs once per plan execution, not on a schedule; negligible load on Yahoo/Binance |
</threat_model>

<verification>
`python -m pytest tests/test_data_api.py tests/ -q` green (mocked, full suite including Phase 0).
`python -m trader.data.exit_criterion_smoke` succeeds live once — this is the phase's exit-criterion proof (success criterion #1).
</verification>

<success_criteria>
get_daily_bars implements the exit-criterion function exactly per D-10/D-11; cache-first behavior is provably correct via mocked tests; the live acceptance run confirms multi-year daily bars for both a real US stock and a real major crypto pair through one function call, completing Phase 1's ACCT-04 and ACCT-07 requirements.
</success_criteria>

<output>
Create `.planning/phases/01-accounts-data-plumbing/01-06-SUMMARY.md` when done
</output>
