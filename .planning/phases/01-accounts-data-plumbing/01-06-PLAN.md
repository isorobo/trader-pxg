---
phase: 01-accounts-data-plumbing
plan: 06
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-04", "01-05"]
files_modified:
  - trader/data/api.py
  - trader/data/exit_criterion_smoke.py
  - tests/test_data_api.py
autonomous: true
requirements: [ACCT-04, ACCT-07]

must_haves:
  truths:
    - "One function call returns multi-year daily bars for any US stock"
    - "The same one function call returns multi-year daily bars for any major crypto pair"
    - "A second call for an already-cached range makes zero network calls"
    - "bars.venue records true fetch provenance ('yahoo' or 'binance'), never a fee-model venue"
    - "An unresolved crypto symbol in D-15's named universe (BTC, ETH, DOGE, SHIB, PEPE, BONK, WIF) is classified via CoinGecko and persisted before routing — never silently defaulted to crypto_major without a classification call"
    - "get_daily_bars's returned DataFrame index is explicitly UTC tz-aware, not a naive date, so Phase 2's point-in-time iterator inherits an unambiguous contract"
  artifacts:
    - path: "trader/data/api.py"
      provides: "get_daily_bars(symbol, start=None, end=None, asset_class=None, conn=None) -> pandas.DataFrame"
      contains: "def get_daily_bars"
  key_links:
    - from: "trader/data/api.py get_daily_bars"
      to: "trader/data/db.py read_bars_cache / write_bars_cache"
      via: "cache-first read, fetch-on-miss write-through"
      pattern: "read_bars_cache"
    - from: "trader/data/api.py get_daily_bars"
      to: "trader/data/stock_source.py / trader/data/crypto_source.py"
      via: "asset_class-routed fetcher dispatch"
      pattern: "fetch_stock_bars|fetch_crypto_bars"
    - from: "trader/data/api.py resolve_instrument"
      to: "trader/data/classify.py register_crypto_instrument"
      via: "base-asset -> coingecko_id lookup for D-15's named universe"
      pattern: "register_crypto_instrument"
---

<objective>
Wire the Phase 1 exit criterion: `get_daily_bars(symbol, start=None, end=None) -> DataFrame`, a cache-first router over the stock and crypto fetchers built in Plans 01-04/01-05, backed by the schema and cache helpers from Plan 01-01, and now also wired to Plan 01-02's classification onboarding so unresolved crypto symbols are classified — not silently defaulted.

Purpose: This is the phase's single acceptance test (D-10/D-11). Task 3 in this plan runs the actual exit-criterion proof live, against real Yahoo Finance and Binance endpoints — no mocks — confirming ACCT-07 end-to-end. Per D-16, `resolve_instrument`'s fallback path must not silently write `crypto_major` for a new symbol it has never seen — it must classify first, via `classify.register_crypto_instrument`, for every symbol in D-15's named universe.
Output: `trader/data/api.py` with `get_daily_bars` (UTC tz-aware DataFrame index) and `resolve_instrument` (classification-wired); a live acceptance script; a fully mocked test suite covering routing, cache-hit, cache-miss-then-hit, and onboarding-classification behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\phases\01-accounts-data-plumbing\01-CONTEXT.md
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\phases\01-accounts-data-plumbing\01-RESEARCH.md
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\trader\ground_truth\smoke.py
</context>

<interfaces>
<!-- Contracts this plan consumes, produced by earlier Phase 1 plans -->
From trader/data/db.py (Plan 01-01):
```python
get_connection(db_path="data/trader.db") -> sqlite3.Connection
get_instrument(conn, symbol, venue) -> dict | None   # keys: symbol, venue, asset_class, coingecko_id, override
upsert_instrument(conn, symbol, venue, asset_class, coingecko_id=None, override=None) -> None
read_bars_cache(conn, venue, symbol, timeframe, start=None, end=None) -> list[dict]  # keys: ts, open, high, low, close, volume
write_bars_cache(conn, venue, symbol, timeframe, rows) -> int
```
From trader/data/stock_source.py (Plan 01-04):
```python
fetch_stock_bars(symbol, start=None, end=None) -> list[dict]  # keys: ts, open, high, low, close, volume
```
From trader/data/crypto_source.py (Plan 01-05):
```python
fetch_crypto_bars(symbol, since_ms=None) -> list[dict]  # keys: ts, open, high, low, close, volume
CRYPTO_VENUE = "binance"
```
From trader/data/classify.py (Plan 01-02 — the classification onboarding contract this plan wires into resolve_instrument):
```python
register_crypto_instrument(conn, symbol, venue, coingecko_id, override=None) -> str  # "memecoin" | "crypto_major"; classifies via CoinGecko (unless override given) then persists via db.upsert_instrument
```

Venue mapping this plan owns: asset_class "stock" -> venue "yahoo"; asset_class "crypto_major" or "memecoin" -> venue CRYPTO_VENUE ("binance"). This is true data provenance, not the Kraken fee-model venue (01-RESEARCH.md Pitfall 5 / Open Questions #1 RESOLVED, orchestrator's locked decision).

**CRYPTO_COINGECKO_IDS (new, owned by this plan, defined in trader/data/api.py):** a small module-level dict mapping the base-asset ticker of every symbol in D-15's named universe to its CoinGecko id — {"BTC": "bitcoin", "ETH": "ethereum", "DOGE": "dogecoin", "SHIB": "shiba-inu", "PEPE": "pepe", "BONK": "bonk", "WIF": "dogwifcoin"}. `resolve_instrument` derives the base asset from a trading-pair symbol (e.g. "DOGE/USDT" -> "DOGE") and looks it up here to obtain the coingecko_id `register_crypto_instrument` needs — this is a symbol-to-id lookup, not a classification whitelist (classification itself still comes from CoinGecko's live categories field via Pattern 4, never from this map).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for get_daily_bars routing, cache-first behavior, classification wiring, and the tz-aware DataFrame contract (RED)</name>
  <read_first>01-RESEARCH.md "Architecture Patterns" Pattern 1 and the System Architecture Diagram, "Validation Architecture" Phase Requirements to Test Map, 01-CONTEXT.md D-10/D-11/D-15/D-16</read_first>
  <files>tests/test_data_api.py</files>
  <behavior>
    - test_get_daily_bars_stock_contract: with fetch_stock_bars mocked to return a small fixed list of bar dicts, get_daily_bars("AAPL", asset_class="stock", conn=<temp db>) returns a pandas DataFrame indexed by date with columns open, high, low, close, volume.
    - test_get_daily_bars_index_is_utc_tz_aware: the DataFrame returned by get_daily_bars has a DatetimeIndex whose .tz is UTC (not None, not naive) — asserts df.index.tz is not None and str(df.index.tz) == "UTC".
    - test_get_daily_bars_crypto_contract: with fetch_crypto_bars mocked similarly, get_daily_bars("BTC/USDT", asset_class="crypto_major", conn=<temp db>) returns a DataFrame via the crypto path, and the written bars row has venue="binance".
    - test_cache_hit_skips_fetch: pre-seed the bars cache (via db.write_bars_cache) to fully cover the requested range, then call get_daily_bars; assert the mocked fetch_stock_bars/fetch_crypto_bars was never called.
    - test_cache_miss_then_cache_hit: starting from an empty cache, call get_daily_bars once (asserting the mocked fetcher WAS called and the cache is now populated), then call it again for the same range (asserting the fetcher was NOT called the second time, and the returned DataFrame content is identical).
    - test_get_daily_bars_resolves_asset_class_from_instruments_table: upsert_instrument for a symbol with asset_class="stock" via db.upsert_instrument (no explicit asset_class= passed to get_daily_bars this time), confirming get_daily_bars still routes to the stock fetcher using the stored instruments row (D-10's primary resolution path, not just the explicit-argument fallback).
    - test_get_daily_bars_registers_new_crypto_instrument_via_coingecko: for a crypto symbol whose base asset IS in CRYPTO_COINGECKO_IDS (e.g. "DOGE/USDT") with no pre-existing instruments row and no explicit asset_class= argument, mock trader.data.api.classify.register_crypto_instrument and assert it IS called with (conn, "DOGE/USDT", "binance", "dogecoin") before the crypto fetcher runs — confirming resolve_instrument classifies via CoinGecko rather than silently defaulting.
    - test_get_daily_bars_falls_back_without_classification_for_unmapped_crypto_symbol: for a crypto-shaped symbol whose base asset is NOT in CRYPTO_COINGECKO_IDS (e.g. "ZZZ/USDT") with no pre-existing instruments row and no explicit asset_class=, mock trader.data.api.classify.register_crypto_instrument and assert it is NOT called, while db.upsert_instrument IS called directly with asset_class="crypto_major" and coingecko_id=None — confirming the documented degraded fallback for symbols outside the researched universe.
  </behavior>
  <action>
  Define a local `data_conn` fixture inside this test file (built from tmp_db_path, calling trader.data.db.get_connection) — do not modify tests/conftest.py. Patch trader.data.api.stock_source.fetch_stock_bars, trader.data.api.crypto_source.fetch_crypto_bars, and trader.data.api.classify.register_crypto_instrument via unittest.mock.patch for the mocked-fetch and mocked-classification tests. Write the seven tests in <behavior> against trader.data.api's not-yet-existing get_daily_bars and CRYPTO_COINGECKO_IDS. Run pytest and confirm all 7 fail with ModuleNotFoundError (RED).
  </action>
  <verify>
    <automated>python -m pytest tests/test_data_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_data_api.py exists with exactly 7 test functions named as listed in <behavior>
    - `python -m pytest tests/test_data_api.py --collect-only -q` lists all 7 with zero collection errors
    - Running the suite fails referencing trader.data.api, confirming RED
  </acceptance_criteria>
  <done>7 failing tests committed, pinning get_daily_bars's full contract — routing, caching, UTC tz-awareness, and classification wiring — before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement get_daily_bars — cache-first routing, classification-wired resolve_instrument, tz-aware UTC index (GREEN)</name>
  <read_first>tests/test_data_api.py (just written), trader/data/db.py, trader/data/stock_source.py, trader/data/crypto_source.py, trader/data/classify.py, 01-RESEARCH.md "Architecture Patterns" Pattern 1</read_first>
  <files>trader/data/api.py</files>
  <action>
  Import trader.data.classify as classify (for register_crypto_instrument) alongside trader.data.db as db, trader.data.stock_source as stock_source, and trader.data.crypto_source as crypto_source. Define the module-level CRYPTO_COINGECKO_IDS dict exactly as specified in <interfaces> (D-15's named universe: BTC, ETH, DOGE, SHIB, PEPE, BONK, WIF). Implement resolve_instrument(conn, symbol: str, asset_class: str | None) -> tuple[str, str]: if asset_class is given, use it directly (venue derived the same way as below, no classification call — an explicit argument is treated as already-known truth); otherwise query instruments WHERE symbol = ? LIMIT 1 (Phase 1 does not yet support one symbol under two different asset classes simultaneously) and if a row exists, use its override column if set, else its asset_class column. If NO instruments row exists and NO explicit asset_class was given: when the symbol shape indicates crypto (contains "/"), derive base_asset = symbol.split("/")[0].upper() and look it up in CRYPTO_COINGECKO_IDS — if found, call classify.register_crypto_instrument(conn, symbol, crypto_source.CRYPTO_VENUE, coingecko_id) and use its returned asset_class (this is the real classification path satisfying D-16); if base_asset is NOT in CRYPTO_COINGECKO_IDS (a symbol outside the researched universe), call db.upsert_instrument(conn, symbol, crypto_source.CRYPTO_VENUE, "crypto_major", coingecko_id=None) directly as a documented degraded fallback (no live classification call possible without a coingecko_id) and log a warning noting the symbol was not in the known universe. When the symbol shape does NOT indicate crypto, resolve as "stock" directly (no classification applies to stocks — they default to "stock" with no CoinGecko call, per the architecture diagram). Map the resolved asset_class to a venue: "stock" -> "yahoo"; "crypto_major" or "memecoin" -> crypto_source.CRYPTO_VENUE. Implement get_daily_bars(symbol: str, start: str | None = None, end: str | None = None, asset_class: str | None = None, conn: sqlite3.Connection | None = None) -> pandas.DataFrame: default conn to db.get_connection() when None; resolve (resolved_asset_class, venue) via resolve_instrument; read cached = db.read_bars_cache(conn, venue, symbol, "daily", start, end); treat the cache as a hit when start is None and cached is non-empty, OR when start is given and the earliest cached row's ts <= start; on a cache miss, dispatch to stock_source.fetch_stock_bars(symbol, start, end) when resolved_asset_class == "stock", else crypto_source.fetch_crypto_bars(symbol), write the result via db.write_bars_cache(conn, venue, symbol, "daily", fresh_rows), and re-read the cache; build and return a pandas DataFrame indexed by pd.to_datetime(ts).tz_localize("UTC") (explicitly UTC tz-aware — not a naive date index, per D-11) with columns open, high, low, close, volume, sorted ascending by date. Run pytest until all 7 tests from Task 1 pass, then run the full suite to confirm no regressions.
  </action>
  <verify>
    <automated>python -m pytest tests/test_data_api.py tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - get_daily_bars returns a pandas.DataFrame with exactly the columns open, high, low, close, volume, indexed by a UTC tz-aware DatetimeIndex (df.index.tz is UTC, confirmed via pd.to_datetime(...).tz_localize("UTC"), never a naive index)
    - A cache-hit call makes zero calls to fetch_stock_bars/fetch_crypto_bars (verified via mock.assert_not_called in the test suite)
    - A cache-miss call writes through to the bars table with venue="yahoo" for stock rows and venue="binance" for crypto rows — never a Kraken fee-model label
    - An unresolved crypto symbol whose base asset is in CRYPTO_COINGECKO_IDS triggers exactly one call to classify.register_crypto_instrument before the fetcher runs — classification is never skipped for the named universe
    - An unresolved crypto symbol outside CRYPTO_COINGECKO_IDS falls back to a direct db.upsert_instrument("crypto_major") call with no classify.register_crypto_instrument call — documented degraded path, not a silent contract violation
    - Full test suite (`pytest tests/ -q`) is green with no regression to Phase 0 or earlier Phase 1 plans
  </acceptance_criteria>
  <done>get_daily_bars implements the full D-10/D-11/D-16 contract — routing, caching, tz-aware UTC index, and classification-wired instrument onboarding — correctly; all 7 tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Live exit-criterion acceptance run — real stock and real crypto pair, one function call each</name>
  <read_first>trader/ground_truth/smoke.py (live smoke-test entry-point pattern), trader/data/api.py (just implemented)</read_first>
  <files>trader/data/exit_criterion_smoke.py</files>
  <action>
  Create trader/data/exit_criterion_smoke.py mirroring trader/ground_truth/smoke.py's pattern: a main() entry point (no mocks) that calls get_daily_bars("AAPL", asset_class="stock") and get_daily_bars("BTC/USDT", asset_class="crypto_major") against the real Yahoo Finance and Binance endpoints, using the project's actual data/trader.db (default conn), and prints each result's row count, date range, and index tz. Run `python -m trader.data.exit_criterion_smoke` once and confirm both calls return more than 500 rows (roughly 2+ years of daily bars) with a UTC tz-aware index, proving the Phase 1 exit criterion end-to-end with one function call per asset class.
  </action>
  <verify>
    <automated>python -m trader.data.exit_criterion_smoke</automated>
  </verify>
  <acceptance_criteria>
    - The real call to get_daily_bars("AAPL", asset_class="stock") returns a DataFrame with more than 500 rows and a UTC tz-aware index
    - The real call to get_daily_bars("BTC/USDT", asset_class="crypto_major") returns a DataFrame with more than 500 rows and a UTC tz-aware index
    - Both results came from the same get_daily_bars function — the phase's literal "one function call" exit criterion
    - The script exits 0 with no unhandled exception
  </acceptance_criteria>
  <done>Live run proves ACCT-07's exit criterion: one function call returns multi-year, UTC tz-aware daily bars for a real US stock and a real major crypto pair.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| get_daily_bars caller (later phases) -> trader/data/db.py | api.py never builds SQL directly; delegates entirely to db.py's parameterized helpers |
| resolve_instrument -> trader/data/classify.py register_crypto_instrument | delegates the CoinGecko classification call for D-15's named universe |
| Live smoke run -> real Yahoo/Binance endpoints | outbound HTTPS calls with no secrets involved (both are keyless public data) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | SQL construction | accept (delegated) | api.py never constructs SQL itself; all persistence goes through trader/data/db.py's already-parameterized helpers from Plan 01-01 |
| T-01-11 | Tampering | bars.venue provenance mislabeling (Pitfall 5) | mitigate | api.py hardcodes venue="yahoo" for the stock path and venue=crypto_source.CRYPTO_VENUE ("binance") for the crypto path as constants — never derived from or conflated with the Kraken fee-model assumption, directly implementing the orchestrator's locked decision |
| T-01-12 | Denial of Service | live smoke-test network calls | accept | Task 3 runs once per plan execution, not on a schedule; negligible load on Yahoo/Binance |
| T-01-15 | Tampering | degraded fallback for a crypto symbol outside CRYPTO_COINGECKO_IDS | accept | An unmapped symbol is persisted as "crypto_major" without a live classification call and a warning is logged; this is a documented, narrow gap (only affects symbols never seen by this research) and the `override` column (D-16) remains available for manual correction later |
</threat_model>

<verification>
`python -m pytest tests/test_data_api.py tests/ -q` green (mocked, full suite including Phase 0).
`python -m trader.data.exit_criterion_smoke` succeeds live once — this is the phase's exit-criterion proof (success criterion #1).
</verification>

<success_criteria>
get_daily_bars implements the exit-criterion function exactly per D-10/D-11, with a tz-aware UTC DataFrame index; resolve_instrument wires classification for D-15's named crypto universe via register_crypto_instrument, satisfying D-16; cache-first behavior is provably correct via mocked tests; the live acceptance run confirms multi-year daily bars for both a real US stock and a real major crypto pair through one function call, completing Phase 1's ACCT-04 and ACCT-07 requirements.
</success_criteria>

<output>
Create `.planning/phases/01-accounts-data-plumbing/01-06-SUMMARY.md` when done
</output>
