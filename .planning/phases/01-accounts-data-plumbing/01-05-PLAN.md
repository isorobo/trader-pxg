---
phase: 01-accounts-data-plumbing
plan: 05
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - trader/data/crypto_source.py
  - tests/test_crypto_source.py
autonomous: true
requirements: [ACCT-04]

must_haves:
  truths:
    - "A crypto pair's daily bars fetch through Binance (via ccxt), never Kraken's 720-candle-capped endpoint, for depth beyond ~2 years"
    - "A backfill spanning more than 1000 daily candles pages correctly and returns every row, not just the most recent 1000"
    - "Every bar timestamp normalizes to a plain UTC calendar-date string"
  artifacts:
    - path: "trader/data/crypto_source.py"
      provides: "fetch_crypto_bars(symbol, since_ms=None), fetch_all_daily_ohlcv(exchange, symbol, since_ms), normalize_crypto_bars(raw_ohlcv), CRYPTO_VENUE"
      contains: "def fetch_crypto_bars"
  key_links:
    - from: "trader/data/crypto_source.py fetch_crypto_bars"
      to: "ccxt.binance().fetch_ohlcv"
      via: "timeframe='1d', limit=1000, paginated since=cursor"
      pattern: "fetch_ohlcv"
---

<objective>
Build the crypto half of the daily-bars pipeline: fetch via `ccxt`'s Binance client (public market data, no API key), paginate past Binance's 1000-candle-per-call limit, and normalize Unix-millisecond timestamps to a plain UTC calendar-date string.

Purpose: D-02 requires 3+ years of daily crypto history; Kraken's own OHLC endpoint hard-caps at 720 candles regardless of `since` (01-RESEARCH.md Pitfall 2). This plan implements ONLY the Binance path for Phase 1 — no Kraken fetch code exists, which eliminates the silent-truncation risk by construction rather than by a runtime check.
Output: `trader/data/crypto_source.py` with `fetch_crypto_bars`, `fetch_all_daily_ohlcv`, `normalize_crypto_bars`, and a `CRYPTO_VENUE` constant for Plan 01-06 to record as true data provenance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\phases\01-accounts-data-plumbing\01-CONTEXT.md
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\phases\01-accounts-data-plumbing\01-RESEARCH.md
</context>

<interfaces>
<!-- Contract this plan must produce, consumed by Plan 01-06's get_daily_bars -->
Row shape returned by normalize_crypto_bars (list[dict]), one dict per bar — identical key set to Plan 01-04's stock rows:
```python
{"ts": "2018-01-01", "open": 13800.0, "high": 13950.0, "low": 13500.0, "close": 13650.0, "volume": 4521.3}
```
CRYPTO_VENUE = "binance" — this constant is the true data-provenance value Plan 01-06 writes into `bars.venue` for every crypto row (per the orchestrator's locked decision: `bars.venue` records true fetch provenance, never the Kraken fee-model venue).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for Binance pagination and UTC normalization (RED)</name>
  <read_first>01-RESEARCH.md "Architecture Patterns" Pattern 3, "Common Pitfalls" Pitfall 2, "Code Examples" (ccxt section)</read_first>
  <files>tests/test_crypto_source.py</files>
  <behavior>
    - test_crypto_bars_paginate_past_1000: a mocked exchange whose fetch_ohlcv side_effect returns a first batch of exactly 1000 rows and a second, shorter batch, then an empty batch on a third call if invoked — fetch_all_daily_ohlcv concatenates all rows across calls and stops as soon as a batch shorter than limit=1000 is returned, advancing since=cursor by one day past the last row's timestamp between calls.
    - test_normalize_crypto_bars_converts_ms_to_utc_date: given raw_ohlcv rows shaped [[timestamp_ms, open, high, low, close, volume], ...], normalize_crypto_bars returns list[dict] with "ts" as a plain UTC YYYY-MM-DD string and exactly the keys ts/open/high/low/close/volume.
    - test_fetch_crypto_bars_constructs_binance_with_no_api_key: mocking ccxt.binance, fetch_crypto_bars("BTC/USDT") asserts ccxt.binance() was constructed with no apiKey/secret arguments (public market data only) and fetch_ohlcv was called with timeframe="1d".
    - test_normalize_crypto_bars_raises_on_malformed_row: given a raw_ohlcv row with fewer than 6 elements, normalize_crypto_bars raises a clear exception rather than an IndexError from mis-unpacking.
  </behavior>
  <action>
  Build the mocked exchange object locally inside this test file (unittest.mock.Mock with a fetch_ohlcv attribute set via side_effect) — do not add fixtures to tests/conftest.py. Patch ccxt.binance via unittest.mock.patch("trader.data.crypto_source.ccxt.binance") for the construction-assertion test. Write the four tests in <behavior> against trader.data.crypto_source's not-yet-existing fetch_crypto_bars, fetch_all_daily_ohlcv, and normalize_crypto_bars. Run pytest and confirm all 4 fail with ModuleNotFoundError (RED).
  </action>
  <verify>
    <automated>python -m pytest tests/test_crypto_source.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_crypto_source.py exists with exactly 4 test functions named as listed in <behavior>
    - `python -m pytest tests/test_crypto_source.py --collect-only -q` lists all 4 with zero collection errors
    - Running the suite fails referencing trader.data.crypto_source, confirming RED
  </acceptance_criteria>
  <done>4 failing tests committed, pinning the pagination and normalization contract before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Binance fetch, pagination, and UTC-date normalization (GREEN)</name>
  <read_first>tests/test_crypto_source.py (just written), 01-RESEARCH.md "Code Examples" ccxt section</read_first>
  <files>trader/data/crypto_source.py</files>
  <action>
  Define CRYPTO_VENUE = "binance" as a module-level constant. Implement fetch_all_daily_ohlcv(exchange, symbol: str, since_ms: int) -> list[list]: loop calling exchange.fetch_ohlcv(symbol, timeframe="1d", since=cursor, limit=1000), extend an accumulator with each batch, advance cursor to the last row's timestamp plus one day in milliseconds, and stop when a batch is empty or shorter than 1000 rows. Implement normalize_crypto_bars(raw_ohlcv: list[list]) -> list[dict]: for each row, convert timestamp_ms via datetime.fromtimestamp(row[0] / 1000, tz=timezone.utc).strftime("%Y-%m-%d") into "ts", and map the remaining four/five elements to open/high/low/close/volume; raise a clear ValueError if a row has fewer than 6 elements. Implement fetch_crypto_bars(symbol: str, since_ms: int | None = None) -> list[dict]: construct ccxt.binance() with no API key (public data only), default since_ms to a fixed early epoch (e.g. 2017-01-01 in ms) when None, call fetch_all_daily_ohlcv, then normalize_crypto_bars. Run pytest until all 4 tests from Task 1 pass.
  </action>
  <verify>
    <automated>python -m pytest tests/test_crypto_source.py tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - fetch_all_daily_ohlcv correctly concatenates rows across at least two paginated calls in the mocked test
    - No code path in this module constructs a ccxt.kraken() client for data depth — Kraken's 720-candle cap is avoided by construction, not by a runtime row-count check
    - Full test suite remains green (no regression to Phase 0 or earlier Phase 1 plans)
  </acceptance_criteria>
  <done>crypto_source.py implements fetch_crypto_bars, fetch_all_daily_ohlcv, and normalize_crypto_bars exactly per contract; all 4 tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Binance response (via ccxt) → normalize_crypto_bars | untrusted third-party data assumed to have a stable 6-element row shape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Tampering | Kraken 720-candle silent truncation (Pitfall 2) | mitigate | This module implements ONLY the Binance fetch path for Phase 1; no Kraken data-fetch code exists, eliminating the silent-truncation risk by construction rather than a runtime assertion |
| T-01-10 | Tampering | normalize_crypto_bars row-shape assumptions | mitigate | Raises a clear ValueError when a raw_ohlcv row has fewer than 6 elements, instead of an opaque IndexError propagating from a mis-unpacked row |
</threat_model>

<verification>
`python -m pytest tests/test_crypto_source.py -q` green (mocked ccxt exchange, no live network call in this plan's tests — the live check happens once in Plan 01-06's exit-criterion task).
</verification>

<success_criteria>
fetch_crypto_bars, fetch_all_daily_ohlcv, and normalize_crypto_bars implement D-02's crypto path via Binance only, correctly paginating past the 1000-candle-per-call limit; all tests green.
</success_criteria>

<output>
Create `.planning/phases/01-accounts-data-plumbing/01-05-SUMMARY.md` when done
</output>
