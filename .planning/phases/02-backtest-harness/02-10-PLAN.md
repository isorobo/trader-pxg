---
phase: 02-backtest-harness
plan: 10
type: execute
wave: 4
depends_on: ["02-08", "02-03", "02-07"]
files_modified:
  - trader/backtest/run_momentum_placeholder.py
  - tests/test_backtest_momentum_e2e.py
  - reports/backtests/.gitkeep
autonomous: true
requirements: [BACK-06]

must_haves:
  truths:
    - "Running the momentum placeholder strategy over real cached AAPL history, offline, produces at least one real simulated trade"
    - "A markdown metrics report actually appears on disk under reports/backtests/ with plausible, non-NaN numbers"
    - "The full pipe — iterator, fills, exits, ledger, metrics, report — runs end to end with no manual steps"
  artifacts:
    - path: "trader/backtest/run_momentum_placeholder.py"
      provides: "main() wiring run_backtest + compute_metrics + write_report for the D-15 placeholder"
      min_lines: 30
  key_links:
    - from: "trader/backtest/run_momentum_placeholder.py"
      to: "trader/backtest/runner.run_backtest, trader/backtest/metrics.compute_metrics, trader/backtest/metrics.write_report"
      via: "sequential calls: run -> fetch trades -> compute metrics -> write report"
      pattern: "run_backtest\\(|compute_metrics\\(|write_report\\("
---

<objective>
Prove the full pipe end to end (D-15, phase success criterion 2): run the simplest-possible momentum placeholder strategy over real cached history, produce a real ledger, compute real metrics, and write a real markdown report — with no strategy-development ambition (that is Phase 3's job) and no manual steps.

Purpose: BACK-01 through BACK-06 each have isolated and integration tests, but the phase is not done until one real (if simple) strategy runs the entire pipe and a human-readable report exists to show for it.
Output: `trader/backtest/run_momentum_placeholder.py`, a green automated test proving the report renders with plausible numbers, and an actual report file on disk under `reports/backtests/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-backtest-harness/02-CONTEXT.md
@.planning/phases/02-backtest-harness/02-VALIDATION.md

<interfaces>
<!-- trader/backtest/runner.run_backtest(strategy_fn, universe, profile, bars_by_symbol, seed,
params, strategy_id, conn) -> run_id (plan 02-08)
trader/backtest/momentum_placeholder.pick_entries (plan 02-07)
trader/backtest/config.PROFILE_MOMENTUM_PLACEHOLDER (plan 02-01)
trader/backtest/ledger.get_trades_for_run(conn, run_id) -> list[dict] (plan 02-06)
trader/backtest/metrics.compute_metrics(trades) -> dict, metrics.write_report(run_id, metrics,
  strategy_id, base_dir="reports/backtests") -> Path (plan 02-03)
trader/data/api.get_daily_bars(symbol, asset_class="stock", conn=None) -> real cached AAPL
  history, offline (already cached: 11,495 rows, 1980-12-12..2026-07-24) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: End-to-end momentum placeholder run producing a real metrics report (D-15, BACK-06)</name>
  <files>trader/backtest/run_momentum_placeholder.py, tests/test_backtest_momentum_e2e.py, reports/backtests/.gitkeep</files>
  <read_first>
    trader/backtest/runner.py, trader/backtest/metrics.py, trader/backtest/momentum_placeholder.py, trader/backtest/config.py (all as actually implemented in prior waves)
  </read_first>
  <behavior>
    - main(conn=None) fetches AAPL's full cached history via get_daily_bars("AAPL", asset_class="stock", conn=conn or None), runs run_backtest with momentum_placeholder.pick_entries, PROFILE_MOMENTUM_PLACEHOLDER, a fixed seed, strategy_id="momentum_placeholder", over that single-symbol universe; fetches trades via ledger.get_trades_for_run; computes metrics via compute_metrics(trades); writes the report via write_report(run_id, metrics, "momentum_placeholder"); returns (run_id, metrics, report_path)
    - A test using AAPL's real cached history (offline, no mock — this exercises the genuine end-to-end pipe, matching 02-VALIDATION.md's one Manual-Only Verification item, now automated) asserts trade_count in the returned metrics is >= 1 (AAPL's 45-year history with a 20-day momentum lookback must produce at least a handful of signals — if it produces zero, the test fails loudly rather than silently passing on an empty run, since a strategy that never trades proves nothing)
    - The test asserts every BACK-06 metrics key (profit_factor, sharpe_ratio, max_drawdown, win_rate, avg_win, avg_loss, trade_count, total_fees_paid) is present and, where not None, is a finite float (no NaN, no inf leaking through where a finite value is expected — profit_factor may legitimately be math.inf only if there are zero losing trades, which the test checks for explicitly rather than blanket-rejecting inf)
    - The test asserts report_path exists on disk (Path.exists()) and its contents contain the string "momentum_placeholder" and the string "trade_count"
  </behavior>
  <action>
    Create reports/backtests/.gitkeep (empty file) so the directory exists in version control even before any report is written (data/ and generated reports may be gitignored — check .gitignore and, if reports/backtests/ is covered by a broad ignore rule, confirm the report file itself is fine to be ignored while .gitkeep still marks the directory's presence). Write tests/test_backtest_momentum_e2e.py first (RED), calling main() against the real project data/trader.db (conn=None, offline, cache-hit only — no live network in this test, matching AAPL's already-cached status). Confirm RED (ImportError on trader.backtest.run_momentum_placeholder). Then implement trader/backtest/run_momentum_placeholder.py: main(conn=None) -> tuple[int, dict, Path] wiring the calls exactly as described in the behavior block, with a `if __name__ == "__main__":` guard printing a short human-readable summary (run_id, trade_count, profit_factor, sharpe_ratio) to stdout. Run pytest to confirm GREEN, then also run the script directly once via its module entry point and read the resulting report file to visually confirm it renders sensibly (this replaces 02-VALIDATION.md's manual-only verification step with an automated one, per this phase's "no human checkpoints" requirement).
  </action>
  <verify>
    <automated>pytest tests/test_backtest_momentum_e2e.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_backtest_momentum_e2e.py -x -q` passes
    - `pytest tests/ -q` (full suite, all Phase 2 tests plus the pre-existing 53+) passes with zero failures
    - A real file exists under reports/backtests/ after running the script (verified by the test's Path.exists() assertion, and independently confirmed once by actually running `python -m trader.backtest.run_momentum_placeholder` and inspecting the printed summary and the file contents)
    - `grep -c "trade_count" <the written report file>` returns >= 1
  </acceptance_criteria>
  <done>The momentum placeholder strategy runs end to end over real cached AAPL history, producing a real ledger, real metrics, and a real, readable markdown report on disk — proving the full BACK-01 through BACK-06 pipe works together, automatically, with no manual steps (phase success criterion 2).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Real cached AAPL data -> full pipe | Already-validated Phase 1 data; no new trust boundary introduced |
| Generated report file -> filesystem | Written only under reports/backtests/, filename derived from internally-fixed strategy_id/run_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-21 | Repudiation | An end-to-end run that produces zero trades silently "passing" and proving nothing | mitigate | explicit trade_count >= 1 assertion, test fails loudly on a no-trade run |
| T-02-22 | Information Disclosure | Malformed metrics dict (NaN/inf) silently rendered into the report without explanation | mitigate | finite-value assertions per metric key, mirroring plan 02-03's edge-case policy |
</threat_model>

<verification>
`pytest tests/test_backtest_momentum_e2e.py -q` green. `pytest tests/ -q` full suite green. A real markdown report exists under reports/backtests/.
</verification>

<success_criteria>
One real (placeholder) strategy runs end-to-end over real cached data and produces a metrics report on disk with plausible, finite numbers — phase success criterion 2, fully automated.
</success_criteria>

<output>
Create `.planning/phases/02-backtest-harness/02-10-SUMMARY.md` when done
</output>
