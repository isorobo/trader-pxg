---
phase: 02-backtest-harness
plan: 07
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - trader/backtest/random_strategy.py
  - trader/backtest/momentum_placeholder.py
  - tests/test_backtest_strategies.py
autonomous: true
requirements: [BACK-07]

must_haves:
  truths:
    - "The random strategy, given a fixed seed, picks the exact same sequence of symbols on every replay"
    - "The random strategy never picks a symbol that has no bar on the current simulation date"
    - "The momentum placeholder never signals before it has enough history to evaluate its own lookback window"
  artifacts:
    - path: "trader/backtest/random_strategy.py"
      provides: "pick_entries(iterator, date, open_positions, rng) -> list[str], matching the shared strategy_fn contract"
      min_lines: 20
    - path: "trader/backtest/momentum_placeholder.py"
      provides: "pick_entries(iterator, date, open_positions, rng) -> list[str], lookback-based momentum signal"
      min_lines: 25
  key_links:
    - from: "trader/backtest/random_strategy.py, trader/backtest/momentum_placeholder.py"
      to: "trader/backtest/iterator.PointInTimeIterator"
      via: "iterator.history(symbol) and iterator.bar_on(symbol, date) calls"
      pattern: "iterator\\.(history|bar_on)"
    - from: "trader/backtest/random_strategy.py, trader/backtest/momentum_placeholder.py"
      to: "trader/backtest/runner.py (plan 02-08), tests/test_backtest_sanity.py (plan 02-09)"
      via: "runner calls pick_entries(iterator, date, open_positions, rng) uniformly for any strategy"
      pattern: "pick_entries\\("
---

<objective>
Implement the two strategies Phase 2 needs to prove the harness honest: the D-14 seeded random strategy (the sanity gate's engine) and the D-15 simplest-possible momentum placeholder (the end-to-end proof). Both share one call signature so the runner (plan 02-08) treats them polymorphically.

Purpose: D-14's sanity test can only be trusted if the random strategy is genuinely random-but-reproducible and genuinely blind to any edge (it must not accidentally encode a real signal). D-15's placeholder must be simple enough to audit by eye while still being a real, non-trivial signal.
Output: `trader/backtest/random_strategy.py`, `trader/backtest/momentum_placeholder.py`, green tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-backtest-harness/02-CONTEXT.md
@.planning/phases/02-backtest-harness/02-RESEARCH.md

<interfaces>
<!-- trader/backtest/iterator.PointInTimeIterator (plan 02-02):
.calendar -> sorted list of pd.Timestamp (union of every symbol's dates)
.history(symbol) -> bars visible as of the last advance_to(date) call (D-02-safe, historical only)
.bar_on(symbol, date) -> dict{open,high,low,close,volume} or None if that symbol has no bar
  on that exact date
Shared strategy_fn contract this plan defines and plan 02-08 (runner) consumes:
def pick_entries(iterator: PointInTimeIterator, date: pd.Timestamp,
                  open_positions: set[str], rng: random.Random) -> list[str]
Returns the list of symbols to enter TODAY, drawn only from symbols with iterator.bar_on(symbol,
date) is not None and symbol not in open_positions. Both strategies in this plan implement this
exact signature so runner.py can call either polymorphically. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Seeded random strategy (D-14, BACK-07's engine)</name>
  <files>trader/backtest/random_strategy.py, tests/test_backtest_strategies.py</files>
  <read_first>
    .planning/phases/02-backtest-harness/02-CONTEXT.md D-14, trader/backtest/iterator.py (bar_on/history contract)
  </read_first>
  <behavior>
    - pick_entries(iterator, date, open_positions=set(), rng=random.Random(42)) called against a fixture iterator where 3 of 5 universe symbols have a bar on `date` -> returns a list containing exactly one symbol (the D-14 strategy trades one symbol per day, "buy a random universe symbol, hold one day, repeat") drawn only from the 3 available symbols
    - Given rng=random.Random(42) fresh each time, two separate calls with identical inputs (same date, same available symbols, same open_positions) return the identical chosen symbol — reproducibility
    - Given a symbol already in open_positions, pick_entries never returns it again (the random strategy holds at most one position at a time per the "hold one day" cadence — document this single-position assumption explicitly)
    - If zero symbols have a bar on `date`, pick_entries returns []
  </behavior>
  <action>
    Write the random-strategy tests into tests/test_backtest_strategies.py first (RED), using a small hand-built fixture iterator (reuse the same fixture-DataFrame construction pattern as tests/test_backtest_iterator.py, building a real PointInTimeIterator instance rather than a mock, since iterator.py already exists from plan 02-02). Confirm RED. Then implement trader/backtest/random_strategy.py: pick_entries(iterator, date, open_positions, rng) -> list[str] filters iterator's universe (a list of symbol names passed at construction or discoverable from the iterator — if PointInTimeIterator does not already expose a `.symbols` list, read plan 02-02's actual implementation and use whatever it exposes, or accept a `universe: list[str]` argument explicitly if the iterator has no such property) down to symbols where iterator.bar_on(symbol, date) is not None and symbol not in open_positions; if the filtered list is empty, return []; else return [rng.choice(filtered)]. Run pytest to confirm GREEN.
  </action>
  <verify>
    <automated>pytest tests/test_backtest_strategies.py -k random -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_backtest_strategies.py -k random -x -q` passes
    - A determinism test exists and passes: `grep -c "def test_random.*determin\|def test_random.*reproduc" tests/test_backtest_strategies.py` returns >= 1
  </acceptance_criteria>
  <done>random_strategy.py picks reproducibly, never picks an unavailable symbol, and never double-enters an already-open position.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Simplest-possible momentum placeholder (D-15)</name>
  <files>trader/backtest/momentum_placeholder.py, tests/test_backtest_strategies.py</files>
  <read_first>
    .planning/phases/02-backtest-harness/02-CONTEXT.md D-15, trader/backtest/iterator.py
  </read_first>
  <behavior>
    - pick_entries(iterator, date, open_positions, rng) with a LOOKBACK constant (module-level, e.g. LOOKBACK_DAYS = 20 — Claude's discretion, document the choice) signals to enter a symbol when iterator.history(symbol) has at least LOOKBACK_DAYS + 1 rows AND the latest close in history(symbol) is strictly greater than the close LOOKBACK_DAYS bars earlier (simple momentum: price is higher now than LOOKBACK_DAYS ago)
    - A symbol with fewer than LOOKBACK_DAYS + 1 bars in history(symbol) never signals, regardless of price shape — a test using a short fixture (e.g. 5 bars with LOOKBACK_DAYS=20) asserts pick_entries returns [] for that symbol
    - A crafted rising fixture (monotonically increasing closes over LOOKBACK_DAYS+1 bars) signals entry for that symbol; a crafted flat or falling fixture over the same length does not
    - A symbol already in open_positions is never re-entered even if its momentum condition is true
  </behavior>
  <action>
    Write the momentum-placeholder tests into tests/test_backtest_strategies.py first (RED) — same file as Task 1, additional test functions. Confirm RED. Then implement trader/backtest/momentum_placeholder.py: LOOKBACK_DAYS = 20 module constant; pick_entries(iterator, date, open_positions, rng) -> list[str] iterates the iterator's universe, skips symbols already in open_positions, skips symbols with len(iterator.history(symbol)) < LOOKBACK_DAYS + 1, and for the rest compares the latest visible close to the close LOOKBACK_DAYS rows earlier in that same history — appends the symbol if latest > earlier. Returns all qualifying symbols (unlike the random strategy's single pick, momentum can signal on multiple symbols the same day — document this difference explicitly). Run pytest to confirm GREEN.
  </action>
  <verify>
    <automated>pytest tests/test_backtest_strategies.py -k momentum -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_backtest_strategies.py -k momentum -x -q` passes
    - `pytest tests/test_backtest_strategies.py -q` (both strategies together) passes with no failures
    - A short-history-no-signal test exists: `grep -c "def test_momentum.*short\|def test_momentum.*insufficient" tests/test_backtest_strategies.py` returns >= 1
  </acceptance_criteria>
  <done>momentum_placeholder.py signals only with sufficient history, correctly on rising vs. flat/falling fixtures, and never re-enters an open position.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RNG seed (internal, test/config-supplied) -> random_strategy | No external input; risk is non-reproducibility, not injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Repudiation | Random strategy not actually reproducible given a fixed seed, undermining D-12/D-14 | mitigate | explicit determinism test comparing two separate calls |
| T-02-15 | Tampering | Random strategy accidentally encoding a real signal (defeating the point of the sanity test) | accept | pick_entries only ever calls rng.choice over available symbols — no price data is read by random_strategy.py at all, by construction |
</threat_model>

<verification>
`pytest tests/test_backtest_strategies.py -q` green. `pytest tests/ -q` full suite green.
</verification>

<success_criteria>
Both strategies implement the shared pick_entries(iterator, date, open_positions, rng) contract; the random strategy is provably reproducible and price-blind; the momentum placeholder signals only with sufficient history.
</success_criteria>

<output>
Create `.planning/phases/02-backtest-harness/02-07-SUMMARY.md` when done
</output>
