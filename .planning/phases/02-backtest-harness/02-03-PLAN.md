---
phase: 02-backtest-harness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - trader/backtest/metrics.py
  - tests/test_backtest_metrics.py
autonomous: true
requirements: [BACK-06]

must_haves:
  truths:
    - "Feeding a hand-worked trade list into compute_metrics reproduces the numbers a human gets doing the arithmetic by hand"
    - "Zero losing trades, fewer than two trades, and zero trades each produce a documented, non-crashing result rather than a ZeroDivisionError or a silent NaN"
    - "A markdown report file actually appears on disk under reports/backtests/ after write_report runs"
  artifacts:
    - path: "trader/backtest/metrics.py"
      provides: "compute_metrics(trades) -> dict, write_report(run_id, metrics, strategy_id) -> Path"
      min_lines: 60
  key_links:
    - from: "trader/backtest/metrics.py"
      to: "trader/backtest/runner.py (plan 02-08), trader/backtest/run_momentum_placeholder.py (plan 02-10)"
      via: "compute_metrics(trades) called on ledger.get_trades_for_run output"
      pattern: "compute_metrics\\("
---

<objective>
Build the metrics module (BACK-06, D-13): profit factor, Sharpe, max drawdown, win rate, avg win/loss, trade count, total fees paid — each backed by an explicit, tested edge-case policy, plus the markdown report writer under `reports/backtests/`.

Purpose: an "honest machine" cannot have its own honesty-scoring math be unverified. This module is cross-checked against a hand-worked golden fixture (per 02-RESEARCH.md Q3), not just implemented and trusted.
Output: `trader/backtest/metrics.py`, green tests including the golden fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-backtest-harness/02-CONTEXT.md
@.planning/phases/02-backtest-harness/02-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: compute_metrics with golden fixture and edge cases (BACK-06, D-13)</name>
  <files>trader/backtest/metrics.py, tests/test_backtest_metrics.py</files>
  <read_first>
    .planning/phases/02-backtest-harness/02-RESEARCH.md Q3 (formulas) and Pitfall 4 (edge-case crashes)
  </read_first>
  <behavior>
    - Golden fixture A (profit factor / win rate / avg win/loss / max drawdown / fees): trades = [{"pnl": 100, "fees": 5, "exit_ts": "2026-01-01"}, {"pnl": -50, "fees": 5, "exit_ts": "2026-01-02"}, {"pnl": 200, "fees": 5, "exit_ts": "2026-01-03"}, {"pnl": -30, "fees": 5, "exit_ts": "2026-01-04"}, {"pnl": -20, "fees": 5, "exit_ts": "2026-01-05"}], starting_equity=1000.0. Expected: profit_factor == pytest.approx(3.0) (gains 300 / losses 100), win_rate == pytest.approx(0.4) (2/5), avg_win == pytest.approx(150.0), avg_loss == pytest.approx(-33.3333, rel=1e-3), trade_count == 5, total_fees_paid == pytest.approx(25.0). Daily equity curve built by summing pnl per exit_ts date and cumulating from starting_equity is [1000, 1100, 1050, 1250, 1220, 1200]; max_drawdown == pytest.approx(-50/1100, rel=1e-3) (the -1100->1050 drawdown of -0.045455 is larger in magnitude than the later -1250->1200 drawdown of -0.04)
    - Golden fixture B (Sharpe, kept independent of fixture A for clean hand arithmetic): daily_returns = [0.02, -0.01, 0.03, 0.00, -0.02] fed directly to the Sharpe helper. mean = 0.004, sample stdev (ddof=1) ~= 0.020736, sharpe_ratio == pytest.approx(3.06, abs=0.05) (mean/stdev * sqrt(252))
    - profit_factor with zero losing trades and >=1 winning trade returns math.inf; with zero trades total returns None
    - sharpe_ratio with fewer than 2 daily return observations returns None (not a ZeroDivisionError)
    - compute_metrics([]) (zero trades) returns a dict with every metric key present and set to None, trade_count=0 — never raises
    - compute_metrics returns a dict with exactly these keys: profit_factor, sharpe_ratio, max_drawdown, win_rate, avg_win, avg_loss, trade_count, total_fees_paid
    - write_report(run_id, metrics, strategy_id, base_dir=<tmp_path in test>) creates a file at {base_dir}/{YYYY-MM-DD}-{strategy_id}-run{run_id}.md (date = today, from datetime.now or an injectable clock) containing a "# Backtest Report" heading and every metric key rendered somewhere in the body; the returned value is the Path to the file
  </behavior>
  <action>
    Write tests/test_backtest_metrics.py first (RED) with both golden fixtures spelled out as literal Python lists/dicts (not loaded from any external file) plus the edge-case assertions above. Confirm RED. Then implement trader/backtest/metrics.py: profit_factor(pnls) and a private _sharpe_from_returns(daily_returns, rf=0.0, ddof=1) per Q3's formulas exactly (statistics.mean/statistics.stdev, ddof=1 pinned explicitly per Assumption A3, annualise with math.sqrt(252)); max_drawdown(equity_curve) per Q3; a helper that builds the daily equity curve by grouping trades by exit_ts date (pandas groupby or a plain dict sum), reindexing across the full date range with 0-fill for no-trade days, and cumulating from starting_equity (default 100_000.0, overridable); compute_metrics(trades: list[dict], starting_equity: float = 100_000.0) -> dict wiring all of the above together with the documented edge-case policy (None for undefined, math.inf only for profit_factor with zero losses and >=1 win); write_report(run_id, metrics, strategy_id, base_dir: str = "reports/backtests") -> Path creating base_dir if missing (Path(base_dir).mkdir(parents=True, exist_ok=True)) and writing the markdown file. Run pytest to confirm GREEN.
  </action>
  <verify>
    <automated>pytest tests/test_backtest_metrics.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_backtest_metrics.py -x -q` passes
    - Test file contains both golden-fixture assertions and at least 3 distinct edge-case tests (zero losses, <2 return observations, zero trades): `grep -c "def test_" tests/test_backtest_metrics.py` returns >= 6
    - `grep -c "ddof=1\|ddof = 1" trader/backtest/metrics.py` returns >= 1 (A3 pinned explicitly, not left to an implicit default)
  </acceptance_criteria>
  <done>metrics.py's formulas match the hand-worked golden fixtures exactly; every documented edge case is covered by a named passing test; write_report produces a real file on disk.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trade list (internal, from ledger) -> metrics | Trusted internal data, but must not crash on degenerate shapes (0 trades, 1 trade, no losses) |
| write_report -> filesystem | Writes only under reports/backtests/, no user-controlled path components beyond strategy_id/run_id (both internally generated) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure / Repudiation | Metrics silently returning NaN/crashing on edge cases, hiding a broken run | mitigate | explicit None/inf policy per case, each proven by a named test (Pitfall 4) |
| T-02-06 | Tampering | strategy_id used unsanitized in a report filename | mitigate | strategy_id values originate from this codebase's own strategy modules (random_strategy, momentum_placeholder), never from external/user input in Phase 2's scope |
</threat_model>

<verification>
`pytest tests/test_backtest_metrics.py -q` green. `pytest tests/ -q` full suite green.
</verification>

<success_criteria>
compute_metrics matches the hand-computed golden fixtures exactly and never crashes on documented edge cases; write_report produces a real markdown file.
</success_criteria>

<output>
Create `.planning/phases/02-backtest-harness/02-03-SUMMARY.md` when done
</output>
