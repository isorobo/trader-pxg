---
phase: 02-backtest-harness
plan: 08
type: execute
wave: 3
depends_on: ["02-02", "02-04", "02-05", "02-06", "02-07"]
files_modified:
  - trader/backtest/runner.py
  - tests/test_backtest_runner.py
autonomous: true
requirements: [BACK-01, BACK-04, BACK-05]

must_haves:
  truths:
    - "A full backtest run over a small deterministic fixture produces ledger rows whose entry timestamps are always one bar after the signal bar, never the signal bar itself"
    - "A position that gaps through its stop on the very bar it entered is recorded with exit_reason='stop' on that same bar, not a later one"
    - "Running the runner twice with the same seed, params, and fixture data produces byte-identical trade content (ignoring autoincrement/timestamp columns)"
  artifacts:
    - path: "trader/backtest/runner.py"
      provides: "run_backtest(strategy_fn, universe, profile, bars_by_symbol, seed, params, strategy_id, conn) -> run_id"
      min_lines: 70
  key_links:
    - from: "trader/backtest/runner.py"
      to: "trader/backtest/iterator.py, trader/backtest/fills.py, trader/backtest/exits.py, trader/backtest/ledger.py"
      via: "orchestrates all four in the per-day simulation loop"
      pattern: "PointInTimeIterator\\(|evaluate_exit\\(|record_trade\\("
    - from: "trader/backtest/runner.py"
      to: "tests/test_backtest_sanity.py (plan 02-09), trader/backtest/run_momentum_placeholder.py (plan 02-10)"
      via: "both call run_backtest with their respective strategy_fn"
      pattern: "run_backtest\\("
---

<objective>
Wire the point-in-time iterator (plan 02-02), fills (plan 02-04), exits (plan 02-05), ledger (plan 02-06), and either strategy (plan 02-07) into one orchestration loop: `run_backtest`. This is the assembly step — every honesty rule proven in isolation by the prior plans must now hold true in combination.

Purpose: the sanity test (plan 02-09) and the end-to-end momentum proof (plan 02-10) both call this single function. If the wiring gets the signal-bar vs. fill-bar lag wrong, or skips the entry-bar exit check, every downstream proof is compromised even though each module's own unit tests still pass.
Output: `trader/backtest/runner.py`, a green integration test exercising the full loop against a small deterministic fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-backtest-harness/02-CONTEXT.md
@.planning/phases/02-backtest-harness/02-RESEARCH.md

<interfaces>
<!-- From plans 02-02, 02-04, 02-05, 02-06, 02-07 (all landed by this wave) -- read each
module's actual signatures directly before wiring, since this plan is pure integration:

iterator.PointInTimeIterator(bars_by_symbol: dict[str, pd.DataFrame])
  .calendar, .advance_to(date), .history(symbol), .bar_on(symbol, date)

fills.entry_fill_price(bar_open, asset_class, side) -> float
fills.fee_for(asset_class, qty, price) -> float
fills.worse_of_fill(trigger_price, bar_open, side) -> float

exits.evaluate_exit(profile, position: exits.PositionState, bar: dict, days_held: int,
  watermark: float | None) -> exits.ExitResult | None

ledger.record_run(conn, strategy_id, profile_name, params, seed, code_version=None) -> int
ledger.record_trade(conn, run_id, position_id, strategy_id, symbol, asset_class, entry_ts,
  entry_price, exit_ts, exit_price, qty, fees, slippage, pnl, exit_reason) -> int

Shared strategy_fn contract (plan 02-07):
  pick_entries(iterator, date, open_positions: set[str], rng: random.Random) -> list[str]
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: run_backtest — full simulation loop with correct entry-lag, entry-bar checking, and ledger writes (BACK-01, BACK-04, BACK-05)</name>
  <files>trader/backtest/runner.py, tests/test_backtest_runner.py</files>
  <read_first>
    trader/backtest/iterator.py, trader/backtest/fills.py, trader/backtest/exits.py, trader/backtest/ledger.py, trader/backtest/config.py (all as actually implemented — this task's action depends on their exact final signatures from plans 02-02/02-04/02-05/02-06/02-07)
  </read_first>
  <behavior>
    - Signal-then-fill-lag test: a fixture where the strategy_fn signals entry for symbol S on calendar day T (using history through day T) -> the resulting ledger row's entry_ts equals day T+1's date (the next bar after the signal bar), never day T's own date, and entry_price equals fills.entry_fill_price(day T+1's open, asset_class, "buy") exactly (D-04: entries never fill at the signal bar's close)
    - Entry-bar exit test: craft day T+1 (the fill bar) so its own low breaches the stop computed from the entry price fixed at that bar's open -> the ledger row for that position has exit_ts equal to day T+1 (the SAME bar as entry), exit_reason='stop' — proves the wiring calls evaluate_exit against the entry/fill bar itself, not starting one bar later
    - Full pipe test: a small (10-20 day), 2-symbol fixture run through run_backtest with the D-14-style PROFILE_TIME_STOP_1D profile and a deterministic (non-random, e.g. a stub strategy_fn returning a fixed schedule) strategy_fn produces >= 1 ledger row with all of entry_ts, entry_price, exit_ts, exit_price, qty, fees, slippage, pnl, exit_reason populated and internally consistent (pnl equals (exit_price-entry_price)*qty - fees, within floating point tolerance, accounting for slippage already baked into entry_price/exit_price)
    - Reproducibility test (D-12): run_backtest called twice with the same fixture, same seed, same params, against two SEPARATE fresh temp DBs -> the resulting trade rows (via ledger.get_trades_for_run) are identical across both runs on every column except run_id/trade_id/started_at
    - run_backtest returns the run_id (an int) from ledger.record_run, and that run_id's row in backtest_runs has params_json matching the params dict passed in
  </behavior>
  <action>
    Write tests/test_backtest_runner.py first (RED), building small fixture DataFrames (mirroring tests/test_backtest_iterator.py's fixture style) and a data_conn fixture (mirroring tests/test_backtest_ledger.py's pattern). Confirm RED. Then implement trader/backtest/runner.py: run_backtest(strategy_fn, universe: list[str], profile, bars_by_symbol: dict[str, pd.DataFrame], seed: int, params: dict, strategy_id: str, conn) -> int. Algorithm: build iterator = PointInTimeIterator(bars_by_symbol); rng = random.Random(seed); run_id = ledger.record_run(conn, strategy_id, profile.__class__.__name__ or a passed profile_name, params, seed); maintain open_positions: dict[str, exits.PositionState] and pending_entries: dict[str, str] (symbol -> asset_class resolved once at position open, e.g. via trader.data.api.resolve_instrument or a params-supplied mapping — document which source this plan uses); for each date in iterator.calendar (in order): (a) advance iterator.advance_to(date); (b) for each symbol currently in open_positions, fetch bar = iterator.bar_on(symbol, date) (skip if None — no bar today for this symbol), call exits.evaluate_exit(profile, position, bar, days_held=(date - position.entry_ts).days or an incrementing counter, watermark=position.watermark); on a non-None ExitResult, compute the actual fill price via fills.worse_of_fill or bar['close'] per the exit reason (time_stop/eod_flat use bar['close'] directly per plan 02-05's own raw_price, which ALREADY encodes the worse-of/close convention — do not re-apply worse_of_fill on top of exits.py's raw_price, only layer fills.apply_slippage and fills.fee_for on top of that raw_price), record_trade via ledger.record_trade with position_id, reduce or close the position (partial for scale_out, full otherwise), removing fully-closed positions from open_positions; (c) call strategy_fn(iterator, date, set(open_positions.keys()), rng) to get symbols to enter TODAY (this call uses history() only, so it is inherently signal-at-day-T); for each returned symbol, do NOT open the position immediately — instead schedule it to open at the NEXT date in the calendar where iterator.bar_on(symbol, next_date) is not None (implement via a pending-entries structure carried into the next loop iteration, or by looking ahead one index in iterator.calendar and using bar_on for that specific next date — NOT a blind "next calendar index" if the symbol has no bar that day; skip forward to the first date the iterator.calendar contains where that symbol actually has a bar) — open the position there using fills.entry_fill_price(that bar's open, asset_class, "buy") plus fills.fee_for for the entry fee, and IMMEDIATELY (within the same loop pass that opens it, before moving to the next date) call exits.evaluate_exit against that same fill bar with days_held=0 and watermark=entry_price, so the entry-bar check is never skipped. Return run_id. Run pytest to confirm GREEN, iterating on the exact lag/ordering logic until all four behavior tests pass.
  </action>
  <verify>
    <automated>pytest tests/test_backtest_runner.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_backtest_runner.py -x -q` passes
    - The signal-lag test and the entry-bar-exit test are both present and separately named: `grep -c "def test_.*lag\|def test_.*signal_then_fill\|def test_.*entry_bar" tests/test_backtest_runner.py` returns >= 2
    - The reproducibility test passes: `grep -c "def test_.*reproduc" tests/test_backtest_runner.py` returns >= 1
    - Full suite green: `pytest tests/ -q` shows no regressions
  </acceptance_criteria>
  <done>run_backtest correctly lags entries by exactly one bar from the signal, checks the entry/fill bar itself for exits, writes a fully attributable and reproducible ledger, and is proven end-to-end by a passing integration test — not just by each module's isolated unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| strategy_fn output (internal, from plan 02-07 or a future Phase 3 strategy) -> runner | Trusted symbol list; runner still validates each symbol has a real bar via bar_on before acting on it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-16 | Tampering | Wiring accidentally fills at the signal bar's close instead of the next bar's open (silently reintroducing lookahead at the integration layer even though iterator.py itself is correct) | mitigate | dedicated signal-lag test asserting the exact one-bar offset |
| T-02-17 | Tampering | Wiring skips the entry-bar exit check even though exits.py itself supports it | mitigate | dedicated entry-bar-exit integration test |
| T-02-18 | Repudiation | Non-reproducible ledger despite ledger.py's own reproducibility guarantee, due to wiring introducing incidental nondeterminism (e.g. dict iteration order, unseeded randomness elsewhere) | mitigate | reproducibility test at the runner level, not just the ledger-unit level |
</threat_model>

<verification>
`pytest tests/test_backtest_runner.py -q` green. `pytest tests/ -q` full suite green.
</verification>

<success_criteria>
run_backtest correctly orchestrates iterator, fills, exits, and ledger with the exact signal-to-fill lag and entry-bar exit checking D-04's rules require, proven by a passing integration test against a small deterministic fixture.
</success_criteria>

<output>
Create `.planning/phases/02-backtest-harness/02-08-SUMMARY.md` when done
</output>
