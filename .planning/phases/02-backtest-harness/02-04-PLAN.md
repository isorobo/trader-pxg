---
phase: 02-backtest-harness
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - trader/backtest/fills.py
  - tests/test_backtest_fills.py
autonomous: true
requirements: [BACK-02, BACK-03]

must_haves:
  truths:
    - "A stock fee never comes out below the $1.00 IBKR minimum, and a crypto fee is always exactly 0.26% of notional"
    - "Slippage always pushes the fill price against the trader, on both entries and exits, never in the trader's favour"
    - "A stop/TP fill price that gapped through is priced at the worse of the trigger price or the bar's open, never at the better one"
  artifacts:
    - path: "trader/backtest/fills.py"
      provides: "fee_for(asset_class, qty, price), slippage_pct_for(asset_class), apply_slippage(price, side, asset_class), entry_fill_price(bar_open, asset_class, side), worse_of_fill(trigger_price, bar_open, side)"
      min_lines: 40
  key_links:
    - from: "trader/backtest/fills.py"
      to: "trader/backtest/config.py"
      via: "import FEE_TABLE, SLIPPAGE_TABLE"
      pattern: "from trader\\.backtest\\.config import|from trader\\.backtest import config"
    - from: "trader/backtest/fills.py"
      to: "trader/backtest/runner.py (plan 02-08)"
      via: "runner calls entry_fill_price/fee_for/apply_slippage on every open and close"
      pattern: "entry_fill_price\\("
---

<objective>
Implement fee and slippage mechanics (BACK-02, BACK-03, D-06, D-07, D-08) as pure functions of (asset_class, price, qty, side) — the economic honesty layer the sanity test (plan 02-09) ultimately checks.

Purpose: fees/slippage must be config-driven, not hard-coded in the engine (D-06), and must always bias against the trader (project core value). This plan proves both properties with tests before the runner (plan 02-08) ever calls them.
Output: `trader/backtest/fills.py`, green tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-backtest-harness/02-CONTEXT.md
@.planning/phases/02-backtest-harness/02-RESEARCH.md

<interfaces>
<!-- trader/backtest/config.py (plan 02-01, already built by the time this plan runs):
FEE_TABLE = {"stock": {"kind": "per_share", "rate": 0.005, "minimum": 1.00},
             "crypto_major": {"kind": "taker_pct", "rate": 0.0026},
             "memecoin": {"kind": "taker_pct", "rate": 0.0026}}
SLIPPAGE_TABLE = {"stock": 0.0005, "crypto_major": 0.02, "memecoin": 0.04}
DEFAULT_NOTIONAL = 10_000.0
Read these directly from trader.backtest.config -- do not re-declare the values here. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fee, slippage, and conservative fill-price functions (BACK-02, BACK-03, D-06, D-07, D-08)</name>
  <files>trader/backtest/fills.py, tests/test_backtest_fills.py</files>
  <read_first>
    trader/backtest/config.py (FEE_TABLE, SLIPPAGE_TABLE shapes — read this file directly once plan 02-01 has landed), .planning/phases/02-backtest-harness/02-RESEARCH.md Pattern 2 (conservative fill simulation, worse-of-gap-or-trigger)
  </read_first>
  <behavior>
    - fee_for("stock", qty=100, price=50.0) == max(0.005*100, 1.00) == 1.00 (per-share cost 0.50 loses to the $1 minimum)
    - fee_for("stock", qty=1000, price=50.0) == max(0.005*1000, 1.00) == 5.00 (per-share cost now wins)
    - fee_for("crypto_major", qty=1.0, price=50000.0) == pytest.approx(0.0026 * 50000.0) == pytest.approx(130.0)
    - fee_for("memecoin", qty=1000.0, price=0.10) == pytest.approx(0.0026 * 100.0) == pytest.approx(0.26)
    - slippage_pct_for("stock") == 0.0005, slippage_pct_for("crypto_major") == 0.02, slippage_pct_for("memecoin") == 0.04
    - apply_slippage(price=100.0, side="buy", asset_class="stock") == pytest.approx(100.0 * 1.0005) (buy fills worse = higher price)
    - apply_slippage(price=100.0, side="sell", asset_class="stock") == pytest.approx(100.0 * 0.9995) (sell fills worse = lower price)
    - entry_fill_price(bar_open=100.0, asset_class="crypto_major", side="buy") == apply_slippage(100.0, "buy", "crypto_major") (entries always fill at the bar's open per D-04, then slippage is layered on)
    - worse_of_fill(trigger_price=90.0, bar_open=95.0, side="sell") == 90.0 (bar opened above the stop, no gap — the trigger price stands; "sell" here means closing a long position, i.e. exiting at or below trigger is worse for the trader, so the lower of the two applies)
    - worse_of_fill(trigger_price=90.0, bar_open=80.0, side="sell") == 80.0 (bar gapped down through the stop — the worse, lower open price applies, per D-04's gap-through rule)
    - worse_of_fill(trigger_price=110.0, bar_open=115.0, side="sell_at_tp") == 110.0 (a favourable gap past a take-profit still fills at the TP price, never better — TP fills never improve past the target; document this asymmetry vs. the stop case explicitly in the function's docstring)
    - Every function raises KeyError (not a silent default) if given an asset_class outside {"stock","crypto_major","memecoin"}
  </behavior>
  <action>
    Write tests/test_backtest_fills.py first (RED), covering every behavior item above plus the KeyError-on-unknown-asset_class case. Confirm RED. Then implement trader/backtest/fills.py importing FEE_TABLE/SLIPPAGE_TABLE from trader.backtest.config: fee_for(asset_class, qty, price) branches on FEE_TABLE[asset_class]["kind"] ("per_share" -> max(rate*qty, minimum); "taker_pct" -> rate*qty*price); slippage_pct_for(asset_class) -> SLIPPAGE_TABLE[asset_class]; apply_slippage(price, side, asset_class) multiplies by (1 + slippage_pct) for side="buy" and (1 - slippage_pct) for side="sell"; entry_fill_price(bar_open, asset_class, side) = apply_slippage(bar_open, side, asset_class); worse_of_fill(trigger_price, bar_open, side) implements the D-04 asymmetric rule: for a stop exit (side="sell" closing a long, or symmetrically "buy" closing a short — Phase 2 is long-only per D-15/random-strategy scope, so document that short-side branches are unimplemented/NotImplementedError for now) take the WORSE of trigger_price and bar_open (min for a stop-sell, since a lower exit price is worse for the trader); for a take-profit fill, always cap at trigger_price even if bar_open gapped favourably past it (min(trigger_price, bar_open) would be wrong here — use trigger_price directly, or accept a `capped=True` argument; choose one approach and make the chosen behavior explicit in the docstring and covered by the take-profit test above). Run pytest to confirm GREEN.
  </action>
  <verify>
    <automated>pytest tests/test_backtest_fills.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_backtest_fills.py -x -q` passes
    - `grep -c "0.005\|0.0026\|0.0005\|0.02\|0.04" trader/backtest/fills.py` returns 0 (fee/slippage numbers live only in config.py, never re-hard-coded in fills.py — D-06's "fees are parameters, not hard-coded in engine logic")
    - A test asserting KeyError on an unknown asset_class exists: `grep -c "pytest.raises(KeyError)" tests/test_backtest_fills.py` returns >= 1
  </acceptance_criteria>
  <done>fills.py computes fees/slippage purely from trader.backtest.config values; every fill always biases against the trader, proven by passing tests, not just documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config values -> fills.py -> runner.py | Internal only; risk is a silent favourable-to-trader bug, not external attack |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | Fee/slippage values re-hard-coded in fills.py instead of read from config | mitigate | grep gate in acceptance_criteria bans numeric literals matching the config values inside fills.py |
| T-02-08 | Repudiation | A fill silently favouring the trader (wrong side of apply_slippage/worse_of_fill) going undetected | mitigate | explicit buy/sell direction tests plus the take-profit-cap asymmetry test |
</threat_model>

<verification>
`pytest tests/test_backtest_fills.py -q` green. `pytest tests/ -q` full suite green.
</verification>

<success_criteria>
Fee and slippage are pure, config-driven functions; every fill (entry or exit, gapped or not) is proven, by test, to bias against the trader.
</success_criteria>

<output>
Create `.planning/phases/02-backtest-harness/02-04-SUMMARY.md` when done
</output>
