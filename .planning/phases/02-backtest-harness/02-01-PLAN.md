---
phase: 02-backtest-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - trader/backtest/__init__.py
  - trader/backtest/config.py
  - migrations/0003_backtest.sql
  - tests/test_backtest_config.py
  - tests/test_backtest_migration.py
autonomous: true
requirements: [BACK-02, BACK-03, BACK-04]

must_haves:
  truths:
    - "A caller can import FEE_TABLE, SLIPPAGE_TABLE, DEFAULT_NOTIONAL, and EXIT_PROFILE from trader.backtest.config and get the exact D-06/D-07/D-08 values"
    - "Attempting to mutate a constructed EXIT_PROFILE (or its scale_out tuple) raises, proving standing rule 2 is enforced by the type, not by convention"
    - "A fresh DB connection through trader.data.db.get_connection ends up with schema_version containing 3, and backtest_runs/backtest_trades tables with the locked columns"
    - "Writing an exit_reason or asset_class outside the allowed set into backtest_trades raises sqlite3.IntegrityError"
  artifacts:
    - path: "trader/backtest/config.py"
      provides: "FEE_TABLE, SLIPPAGE_TABLE, DEFAULT_NOTIONAL, EXIT_PROFILE dataclass, named profile constants PROFILE_TIME_STOP_1D and PROFILE_MOMENTUM_PLACEHOLDER"
      min_lines: 40
    - path: "migrations/0003_backtest.sql"
      provides: "backtest_runs and backtest_trades DDL with CHECK constraints and indices"
      contains: "CREATE TABLE IF NOT EXISTS backtest_trades"
  key_links:
    - from: "trader/backtest/config.py"
      to: "trader/backtest/fills.py (plan 02-04), trader/backtest/exits.py (plan 02-05)"
      via: "import trader.backtest.config"
      pattern: "from trader\\.backtest import config"
    - from: "migrations/0003_backtest.sql"
      to: "trader/data/db.py apply_migrations"
      via: "filename version prefix 0003_ picked up by the existing migration runner"
      pattern: "0003_"
---

<objective>
Create the two foundational contracts every other Phase 2 module builds on: the fee/slippage/exit-profile config module (D-06, D-07, D-08, D-09) and the `backtest_runs`/`backtest_trades` schema (D-11) via a new ordered migration.

Purpose: fills.py, exits.py, ledger.py, and runner.py (Waves 2-3) all import from `trader.backtest.config` and write against the schema created here. Defining both up front (Interface-First Task Ordering) means downstream plans implement against a fixed contract instead of inventing one mid-module.
Output: `trader/backtest/config.py`, `migrations/0003_backtest.sql`, green tests for both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-backtest-harness/02-CONTEXT.md
@.planning/phases/02-backtest-harness/02-RESEARCH.md
@trader/data/db.py
@migrations/0002_instruments_bars.sql

<interfaces>
<!-- trader/data/db.py's apply_migrations (already exists, do not modify): sorts migrations/*.sql
by filename, reads each leading 4-digit prefix as its version, skips versions already in
schema_version, executescript()s the file, records the version. 0003_backtest.sql only needs
to exist with correct DDL — the runner mechanism is already built and tested. -->

<!-- instruments.asset_class / bars.venue conventions this phase's config keys off, from
migrations/0002_instruments_bars.sql: asset_class CHECK (IN ('stock','crypto_major','memecoin')).
Phase 2's FEE_TABLE/SLIPPAGE_TABLE must be keyed by exactly these three string values — no
other asset_class values are ever produced by trader.data.classify or trader.data.api. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fee/slippage/exit-profile config module (D-06, D-07, D-08, D-09)</name>
  <files>trader/backtest/__init__.py, trader/backtest/config.py, tests/test_backtest_config.py</files>
  <read_first>
    .planning/phases/02-backtest-harness/02-CONTEXT.md (D-06 through D-09), migrations/0002_instruments_bars.sql (the exact three asset_class values)
  </read_first>
  <behavior>
    - FEE_TABLE["stock"] == {"kind": "per_share", "rate": 0.005, "minimum": 1.00} (IBKR, D-06)
    - FEE_TABLE["crypto_major"] == {"kind": "taker_pct", "rate": 0.0026} (Kraken taker, D-06/D-07)
    - FEE_TABLE["memecoin"] == {"kind": "taker_pct", "rate": 0.0026} (same venue; memecoin's extra cost lives in slippage per D-06, not a separate fee)
    - SLIPPAGE_TABLE == {"stock": 0.0005, "crypto_major": 0.02, "memecoin": 0.04} (D-08's three phase-document tiers mapped 1:1 onto the schema's three asset_class values in the order given: large-cap stock -> "stock", small-cap-runner -> "crypto_major" (the only third schema bucket available; document this mapping in a code comment quoting D-08 verbatim so the choice is traceable, not silently arbitrary), memecoin -> "memecoin")
    - DEFAULT_NOTIONAL == 10_000.0 (Claude's discretion: fixed per-trade notional Phase 2 uses for all strategies since Phase 4 owns real sizing)
    - EXIT_PROFILE is a frozen dataclass with fields stop_pct: float | None, tp_pct: float | None, scale_out: tuple[tuple[float, float], ...], trailing_pct: float | None, max_hold_days: int | None, eod_flat: bool — constructing one then doing `profile.stop_pct = -0.05` raises dataclasses.FrozenInstanceError
    - Passing a list (not a tuple) as scale_out raises TypeError or is rejected by a __post_init__ check — scale_out must never be a mutable list (standing rule 2 enforced by the type)
    - PROFILE_TIME_STOP_1D == EXIT_PROFILE(stop_pct=None, tp_pct=None, scale_out=(), trailing_pct=None, max_hold_days=1, eod_flat=False) — the D-14 sanity strategy's "hold one day" profile
    - PROFILE_MOMENTUM_PLACEHOLDER is a concrete EXIT_PROFILE with a stop_pct, a tp_pct, and max_hold_days set (Claude's discretion values, e.g. stop_pct=-0.10, tp_pct=0.20, max_hold_days=30, eod_flat=False, scale_out=()) — used by the D-15 end-to-end proof in plan 02-10
  </behavior>
  <action>
    Write tests/test_backtest_config.py first (RED): import trader.backtest.config wrapped in try/except ImportError set to None, matching tests/test_data_api.py's RED-phase-safe pattern, so collection succeeds before the module exists. Assert every behavior item above. Run pytest to confirm RED (ImportError-driven failures, not collection errors). Then implement trader/backtest/__init__.py (empty, just makes trader/backtest a package) and trader/backtest/config.py: use @dataclass(frozen=True) for EXIT_PROFILE; add a __post_init__ that raises TypeError if scale_out is not a tuple, or coerce via object.__setattr__ only within __post_init__ if you accept a list input and convert to tuple (choose one approach and document it in the module docstring — do not leave scale_out mutably assignable after construction either way). Define FEE_TABLE, SLIPPAGE_TABLE, DEFAULT_NOTIONAL, PROFILE_TIME_STOP_1D, PROFILE_MOMENTUM_PLACEHOLDER as module-level constants. Run pytest again to confirm GREEN.
  </action>
  <verify>
    <automated>pytest tests/test_backtest_config.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_backtest_config.py exists and is collected without import errors even before config.py exists (RED-phase-safe pattern verified by running pytest before implementing)
    - trader/backtest/config.py exports FEE_TABLE, SLIPPAGE_TABLE, DEFAULT_NOTIONAL, EXIT_PROFILE, PROFILE_TIME_STOP_1D, PROFILE_MOMENTUM_PLACEHOLDER
    - `pytest tests/test_backtest_config.py -x -q` passes with 0 failures
    - Grep confirms no mutable default (`scale_out: list` or `= []`) survives in the dataclass field definition: `grep -c "scale_out: list" trader/backtest/config.py` returns 0
  </acceptance_criteria>
  <done>config.py implements D-06/D-07/D-08/D-09 exactly; EXIT_PROFILE immutability is proven by a passing test, not asserted by comment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: backtest_runs/backtest_trades migration (D-11)</name>
  <files>migrations/0003_backtest.sql, tests/test_backtest_migration.py</files>
  <read_first>
    migrations/0002_instruments_bars.sql (exact DDL style: CREATE TABLE IF NOT EXISTS, CHECK constraints, no ORM), trader/data/db.py apply_migrations (version-prefix mechanism, do not modify)
  </read_first>
  <behavior>
    - After trader.data.db.get_connection(tmp_db_path), `SELECT MAX(version) FROM schema_version` returns 3
    - sqlite_master lists both backtest_runs and backtest_trades as tables
    - backtest_runs has columns: run_id (INTEGER PRIMARY KEY AUTOINCREMENT), started_at, strategy_id, profile, params_json, seed, code_version
    - backtest_trades has columns: trade_id (INTEGER PRIMARY KEY AUTOINCREMENT), run_id (REFERENCES backtest_runs), position_id, strategy_id, symbol, asset_class, entry_ts, entry_price, exit_ts, exit_price, qty, fees, slippage, pnl, exit_reason
    - Inserting a backtest_trades row with asset_class='not_a_real_class' raises sqlite3.IntegrityError
    - Inserting a backtest_trades row with exit_reason='not_a_real_reason' raises sqlite3.IntegrityError (allowed set: 'stop', 'take_profit', 'trailing_stop', 'scale_out', 'time_stop', 'eod_flat')
  </behavior>
  <action>
    Write tests/test_backtest_migration.py first (RED) using the tmp_db_path fixture from tests/conftest.py and trader.data.db.get_connection directly (mirrors tests/test_data_db.py's data_conn fixture pattern — define a local data_conn fixture in this file too, do not modify tests/conftest.py). Confirm RED (0003_backtest.sql does not exist yet, so schema_version max is 2). Then write migrations/0003_backtest.sql: CREATE TABLE IF NOT EXISTS backtest_runs with run_id INTEGER PRIMARY KEY AUTOINCREMENT, started_at TEXT NOT NULL DEFAULT (datetime('now')), strategy_id TEXT NOT NULL, profile TEXT NOT NULL, params_json TEXT NOT NULL, seed INTEGER NOT NULL, code_version TEXT. CREATE TABLE IF NOT EXISTS backtest_trades with trade_id INTEGER PRIMARY KEY AUTOINCREMENT, run_id INTEGER NOT NULL REFERENCES backtest_runs(run_id), position_id TEXT NOT NULL, strategy_id TEXT NOT NULL, symbol TEXT NOT NULL, asset_class TEXT NOT NULL CHECK (asset_class IN ('stock','crypto_major','memecoin')), entry_ts TEXT NOT NULL, entry_price REAL NOT NULL, exit_ts TEXT NOT NULL, exit_price REAL NOT NULL, qty REAL NOT NULL, fees REAL NOT NULL, slippage REAL NOT NULL, pnl REAL NOT NULL, exit_reason TEXT NOT NULL CHECK (exit_reason IN ('stop','take_profit','trailing_stop','scale_out','time_stop','eod_flat')). Add CREATE INDEX IF NOT EXISTS idx_backtest_trades_run ON backtest_trades(run_id) and idx_backtest_trades_strategy ON backtest_trades(strategy_id). Run pytest to confirm GREEN.
  </action>
  <verify>
    <automated>pytest tests/test_backtest_migration.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_backtest_migration.py -x -q` passes
    - `grep -c "CREATE TABLE IF NOT EXISTS backtest_trades" migrations/0003_backtest.sql` returns 1
    - `grep -c "CHECK (exit_reason IN" migrations/0003_backtest.sql` returns 1
    - Full suite still green after this task: `pytest tests/ -q` shows no new failures among the pre-existing 53+ tests
  </acceptance_criteria>
  <done>migrations/0003_backtest.sql applies cleanly via the existing migration runner; schema_version reaches 3; both new tables enforce their CHECK constraints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.py constants -> downstream modules | In-process only, no external input; risk is accidental mutation, not injection |
| migration SQL -> sqlite3 | DDL only, no user-supplied values at migration time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | EXIT_PROFILE mutation after entry (standing rule 2) | mitigate | frozen dataclass + tuple-only scale_out, enforced by a failing-mutation test, not convention |
| T-02-02 | Tampering | backtest_trades.exit_reason / asset_class malformed values | mitigate | CHECK constraints in 0003_backtest.sql, proven by an IntegrityError test |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | no new package installs in this plan (stdlib + existing pinned deps only) |
</threat_model>

<verification>
Run `pytest tests/test_backtest_config.py tests/test_backtest_migration.py -q` — both green. Run `pytest tests/ -q` — full suite (53+ pre-existing tests plus these two new files) green, no regressions.
</verification>

<success_criteria>
trader/backtest/config.py and migrations/0003_backtest.sql exist, are imported/applied without error, and their contracts (fee/slippage values, EXIT_PROFILE immutability, schema_version=3, CHECK constraints) are proven by passing automated tests.
</success_criteria>

<output>
Create `.planning/phases/02-backtest-harness/02-01-SUMMARY.md` when done
</output>
