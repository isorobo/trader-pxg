---
phase: 05
plan: 07
type: execute
wave: 5
depends_on: ["05-02", "05-04", "05-05", "05-06"]
files_modified:
  - trader/paper/daily_report.py
  - trader/ground_truth/report.py
  - tests/test_paper_daily_report.py
  - tests/test_recovery.py
  - requirements.txt
autonomous: true
requirements: [PAPER-07]

must_haves:
  truths:
    - "The daily report (reports/{date}.md) gains a paper-trading section showing open positions, closed-trade P&L, current breaker/halt state, scheduled-run coverage, and a Manual Interventions tally for the window (REVISED: W2)"
    - "A process that crashes after a broker fill but before the local ledger write heals on its NEXT calendar day's --once invocation instead of double-submitting or losing the position, even after an intervening halt + human clear (REVISED: BLOCKER 1 -- the exact multi-day scenario, not just a same-day resubmit)"
    - "A weekly scheduled_auth ops-log entry never appears in the same coverage tally as manual_restart_required entries"
    - "A real Phase 4 breaker trip (via record_breaker_transitions) causes run_entry_pipeline_once to submit ZERO new orders on its next call (REVISED: W4 -- a composed cross-module integration proof, not just is_entry_halted's unit test)"
    - "The full pytest suite (347+ pre-existing tests plus every Phase 5 test file) is green"
  artifacts:
    - path: "trader/paper/daily_report.py"
      provides: "compute_paper_section(conn) -> markdown lines: positions/P&L/breaker+halt state/coverage/manual-interventions tally"
      contains: "def compute_paper_section"
  key_links:
    - from: "trader/ground_truth/report.py"
      to: "trader/paper/daily_report.py"
      via: "main() appends compute_paper_section's lines to the same reports/{date}.md file (D-12)"
      pattern: "compute_paper_section"
    - from: "tests/test_recovery.py"
      to: "trader/paper/entry_pipeline.py + trader/paper/guardian.py + trader/paper/reconcile.py + trader/paper/clear_halt.py"
      via: "a multi-day scenario: day-1 crash -> reconciliation halt -> human clear (clear_halt) -> day-2 heal, asserting exactly one broker position and one local position (BLOCKER 1)"
      pattern: "run_entry_pipeline_once"
    - from: "tests/test_recovery.py"
      to: "trader.risk.breakers.record_breaker_transitions"
      via: "a real breaker trip persisted, then run_entry_pipeline_once asserted to submit zero orders (W4)"
      pattern: "record_breaker_transitions("
---

<objective>
Close out the mocked-broker-testable portion of Phase 5: extend the
existing daily report (Phase 0's `trader/ground_truth/report.py`) with a
paper-trading section per D-12 -- now including a Manual Interventions
tally sourced directly from `reconciliation_log`/`breaker_events` rows with
`actor='human'` (W2) -- and prove the crash-recovery contract end to end
across a simulated MULTI-DAY process restart, including the exact
plan-checker BLOCKER 1 scenario (crash -> halt -> human clear -> next-day
heal) and a composed real-breaker-trip proof (W4). This is the last
autonomous plan before the two human checkpoints.

Purpose: PAPER-07's "unattended overnight" requirement needs the daily
report to make the two-week window auditable from disk, and needs the
idempotency/heal path proven at the process-restart level across calendar
days, not just within a single run.
Output: trader/paper/daily_report.py, an extended
trader/ground_truth/report.py, a green full test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-paper-trading-loop/05-CONTEXT.md
@.planning/phases/05-paper-trading-loop/05-RESEARCH.md
@trader/ground_truth/report.py
@trader/ground_truth/db.py

<interfaces>
From trader/ground_truth/report.py (existing, extended not replaced):
    write_report_markdown(rows, coverage, out_path) -> None
    main(argv=None) -> None, writes reports/{date}.md

From trader/paper/ledger.py (Plan 05-01, includes the BLOCKER 1 revision):
    get_open_positions(conn) -> list[dict]
    get_recent_trades(conn, strategy_id, limit=30) -> list[dict]
    get_kill_state(conn) -> dict[str, dict]
    get_unresolved_orders(conn, strategy_id, symbol, side) -> list[dict]
    heal_order(conn, order_ref, perm_id, fill_price, filled_ts) -> None

From trader/paper/idempotency.py (Plan 05-01):
    find_unresolved_match(unresolved_local_orders, broker_fills) -> dict | None

From trader/risk/breakers.py: read_breaker_state(conn) -> dict;
    evaluate_breakers(equity_curve, trade_pnls, config=risk_config) -> dict;
    record_breaker_transitions(conn, previous_state, evaluation) -> None

From trader/paper/reconcile.py (Plan 05-04):
    is_entry_halted(conn) -> bool
    record_reconciliation(conn, symbol, venue, local_qty, broker_qty, classification, reason=None) -> None

From trader/paper/clear_halt.py (Plan 05-04):
    clear_entry_halt(conn, reason: str) -> None  # also appends manual_restart_required (BLOCKER 3)

From trader/paper/ops_log.py (Plan 05-02):
    compute_run_coverage(log_path, window_start, window_end, expected_cadence_minutes) -> dict
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Paper-trading daily report section, including Manual Interventions tally</name>
  <files>trader/paper/daily_report.py, trader/ground_truth/report.py, tests/test_paper_daily_report.py</files>
  <read_first>trader/ground_truth/report.py's write_report_markdown (the exact markdown-writing style this section must match)</read_first>
  <behavior>
    - compute_paper_section(conn) with two open positions and one closed
      trade returns markdown lines listing both open positions (symbol,
      strategy_id, qty, entry_price, unrealized state not computed here --
      entry snapshot only) and the closed trade's pnl
    - compute_paper_section reports "halted: True" when is_entry_halted
      returns True, and lists which breaker(s) or reconciliation state
      caused it
    - compute_paper_section includes a coverage line per scheduled process
      (entry pipeline, guardian, reconcile) using ops_log.compute_run_coverage
    - compute_paper_section's Manual Interventions section counts every
      reconciliation_log row with actor='human' (clear_halt.py's clears)
      PLUS every breaker_events row with actor='human'
      (trader.risk.clear_breaker's clears) within the reporting window,
      listing each with its ts/reason -- and explicitly excludes any
      'scheduled_auth' ops-log entries, since those are D-13's pre-
      registered exception, never a manual intervention (REVISED, W2)
    - compute_paper_section on a completely empty DB (no positions, no
      trades, no breaker events) returns valid markdown, never raises,
      and its Manual Interventions section reads "None this window"
  </behavior>
  <action>
    Write trader/paper/daily_report.py: compute_paper_section(conn,
    as_of=None) -> list[str]: as_of = as_of or datetime.now(timezone.utc);
    positions = ledger.get_open_positions(conn); recent_trades = a
    flattened list of ledger.get_recent_trades(conn, cfg.profile_name,
    limit=5) for every cfg in config.LIVE_STRATEGY_CONFIGS (5 most recent
    per config, not a global limit, so no single hot config crowds out the
    others); breaker_state = breakers.read_breaker_state(conn); halted =
    reconcile.is_entry_halted(conn); kill_state =
    ledger.get_kill_state(conn); coverage_entry =
    ops_log.compute_run_coverage("ops/paper_trading.log", as_of -
    timedelta(days=1), as_of, config.GUARDIAN_CADENCE_MINUTES) (guardian's
    cadence as the representative one, since it's the tightest -- reconcile's
    60s cadence would report a huge expected count on a daily window, not
    useful for a human skim).

    manual_interventions (NEW, W2): SELECT ts, reason FROM
    reconciliation_log WHERE actor='human' AND ts >= ? (window start,
    as_of - timedelta(days=1)) -- add a small parameterized read directly
    in this module (or a new ledger.get_manual_reconciliation_events(conn,
    since) -> list[dict] if that fits the existing ledger.py ownership
    convention better, Claude's discretion) -- UNION'd with a
    parameterized SELECT ts, reason FROM breaker_events WHERE
    actor='human' AND ts >= ? (Phase 4's table, migrations/0004, already
    has the identical actor column). Sort combined results by ts. This is
    the audit list the two-week exit gate's "zero manual interventions"
    claim must be checked against -- 'scheduled_auth' ops-log lines are
    NEVER included here (they live in the ops log, not these two tables,
    so there is no code path that could conflate them, W2/D-13).

    Build a "## Paper Trading" markdown section with subsections "###
    Open Positions" (a table: symbol, strategy_id, qty, entry_price,
    entry_ts), "### Recent Closed Trades" (a table: symbol, strategy_id,
    exit_reason, pnl, exit_ts), "### Breaker / Halt State" (one line per
    breaker_type from breaker_state plus one line "Entry halted:
    {halted}"), "### Retired Strategies" (one line per kill_state entry,
    or "None retired" if empty), "### Manual Interventions (window)" (one
    line per manual_interventions entry as "{ts}: {reason}", or "None this
    window" if empty -- NEW, W2), "### Scheduled-Run Coverage (24h)"
    (coverage_entry's completed/expected/pct). Return the list of markdown
    lines (never write the file itself -- that stays report.py's job,
    matching the existing separation of concerns).

    Extend trader/ground_truth/report.py's main(): after
    write_report_markdown(rows, coverage, out_path) call,
    append compute_paper_section's lines to the same out_path file (open
    in append mode) -- import trader.paper.daily_report lazily inside
    main() (not at module level) so trader/ground_truth/report.py, a
    Phase 0 module, does not gain a hard Phase 5 import dependency for
    callers who only ever use Phase 0's ground-truth reporting; wrap the
    call in a try/except that logs and continues on any failure (the
    ground-truth report must never fail to write because Phase 5's tables
    do not exist yet in an older DB, or because Phase 5 hasn't been
    executed yet in a fresh checkout).
  </action>
  <acceptance_criteria>
    - compute_paper_section never raises on a completely empty database
    - trader/ground_truth/report.py's main() still succeeds and writes a
      report even when trader.paper.daily_report's import or call raises
      (proven by a test that breaks the import path deliberately)
    - the Manual Interventions tally never includes a 'scheduled_auth'
      entry (proven by a test that seeds one and asserts it is absent from
      the tally)
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_paper_daily_report.py -x -q</automated>
  </verify>
  <done>tests/test_paper_daily_report.py passes; reports/{date}.md gains a Paper Trading section, including the Manual Interventions tally, without breaking Phase 0's existing report</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Multi-day crash-recovery integration test (BLOCKER 1 exact scenario) + composed breaker-trip test (W4) + final full-suite verification</name>
  <files>tests/test_recovery.py, requirements.txt</files>
  <read_first>trader/paper/entry_pipeline.py's revised heal-before-halt sequence (Plan 05-06 Task 2), trader/paper/guardian.py's equivalent exit-side sequence (Plan 05-05 Task 1), trader/paper/clear_halt.py (Plan 05-04 Task 2)</read_first>
  <behavior>
    - BLOCKER 1's exact scenario, end to end across a simulated day
      boundary: (1) DAY 1 -- run_entry_pipeline_once persists a
      'pending_submit' paper_orders row for a candidate, then the test
      simulates a crash by stopping BEFORE update_order_status/
      open_position ever run (the row stays 'pending_submit'); (2) the
      fake_ib fixture is configured so its .positions()/.fills() now
      reflect that the broker DID fill the order; (3) run_reconcile_once
      is called -- local_positions shows 0 for the symbol (open_position
      was never called), pending_qty from ledger.get_pending_order_qty
      excludes the 'pending_submit' row entirely (05-01's BLOCKER 1
      revision), so classify_divergence reports "unexplained" and
      is_entry_halted becomes True; (4) clear_halt.clear_entry_halt is
      called with a test reason, simulating the human clearing it (this
      also appends a manual_restart_required ops-log entry per BLOCKER 3
      -- assert that line exists); (5) DAY 2 -- run_entry_pipeline_once is
      called again for the NEXT calendar date; assert
      fake_ib.placeOrder was called exactly once in TOTAL across both
      days (never a second time on day 2), assert exactly ONE
      paper_positions row is open for that symbol (not zero, not two),
      and assert the healed paper_orders row's perm_id matches the fake
      fill's permId
    - the same multi-day crash/halt/clear/heal shape holds for a guardian
      exit order (an exit decided and persisted 'pending_submit' on one
      guardian tick, broker-filled, process "crashes", healed on a LATER
      tick without a second place_order/SELL call)
    - W4 (composed integration): a real equity_curve/trade_pnls pair that
      breaches trader.risk.config's drawdown threshold is fed through
      trader.risk.breakers.evaluate_breakers +
      record_breaker_transitions(conn, previous_state, evaluation)
      directly (not via the guardian, to isolate the exact mechanism
      under test); the VERY NEXT run_entry_pipeline_once call (fresh
      candidates available, gate/sizer would otherwise accept them)
      submits ZERO orders, and its returned summary dict's "halted" key is
      True
    - the full pytest suite (347+ pre-existing tests plus every
      tests/test_paper_*.py, tests/test_idempotency.py,
      tests/test_broker_*.py, tests/test_calendar.py, tests/test_alerts.py,
      tests/test_reconciliation.py, tests/test_guardian.py,
      tests/test_entry_pipeline.py, tests/test_recovery.py) passes green
  </behavior>
  <action>
    Write tests/test_recovery.py as a pure integration test (no new
    production code expected -- if a gap surfaces while writing this test,
    fix it in trader/paper/entry_pipeline.py, trader/paper/guardian.py, or
    trader/paper/reconcile.py directly, since 05-01/05-04/05-05/05-06
    already implemented the heal/halt/clear branches this test exercises):

    Test 1 (BLOCKER 1, entry side): construct a paper_conn fixture DB, a
    fake_ib fixture, seed fixture bars sufficient for one symbol to fire
    the loose momentum signal on day 1's as_of_date. Call
    run_entry_pipeline_once for day 1. Directly UPDATE (not delete -- this
    is the corrected simulation matching the REVISED lifecycle) the
    resulting paper_orders row's status back to (or leave at)
    'pending_submit' and clear its perm_id/fill_price/filled_ts in the
    test DB, simulating "the broker call happened and filled, but this
    process crashed before update_order_status/open_position ran" --
    delete any paper_positions row that open_position may have already
    written in the same call, since a genuine crash at this point means
    open_position never ran either. Configure fake_ib.positions()/.fills()
    to reflect the filled quantity and a fill dict carrying that
    order_ref. Call run_reconcile_once(conn, fake_ib_adapter) and assert
    is_entry_halted(conn) is now True. Call
    clear_halt.clear_entry_halt(conn, "test: verified broker fill in
    Gateway, healing next run") and assert the ops log now contains a
    'manual_restart_required' line. Call run_entry_pipeline_once again for
    DAY 2 (as_of_date advanced by one calendar day) and assert:
    fake_ib.placeOrder's total call count across both invocations is
    exactly 1; ledger.get_open_positions(conn) shows exactly one open
    position for the symbol; the paper_orders row for the original
    order_ref is now 'filled' with the fake fill's permId.

    Test 2 (BLOCKER 1, exit side): the equivalent shape around
    run_guardian_once and a 'sell' order_ref, proving the guardian's
    identical persist-before-submit + date-independent heal sequence
    (Plan 05-05 Task 1).

    Test 3 (W4, composed breaker-trip -> zero orders): seed
    paper_trades/an equity trajectory (directly via ledger writes, not
    through the guardian, to isolate the mechanism) that makes
    trader.risk.breakers.evaluate_breakers report drawdown_tripped=True;
    call trader.risk.breakers.record_breaker_transitions(conn,
    breakers.read_breaker_state(conn), that evaluation) directly. Seed
    fixture bars so the loose momentum signal would otherwise fire for at
    least one symbol. Call run_entry_pipeline_once and assert
    fake_ib.placeOrder was called zero times and the returned dict's
    "halted" is True.

    Confirm requirements.txt now pins both `ib_async==2.1.0` and
    `pandas-market-calendars==5.4.0` (added in Plans 05-03/05-02
    respectively) -- this task only verifies, does not re-add.
  </action>
  <acceptance_criteria>
    - fake_ib.placeOrder's call count is asserted across the ENTIRE
      multi-day scenario (both days combined), not merely the resulting
      row count, so a masked double-submit cannot pass silently
    - Test 1 and Test 2 each advance a real calendar date between their
      "day 1" and "day 2" calls -- a test that reuses the same as_of_date
      for both calls would not actually prove BLOCKER 1's date-independent
      matching and must not be accepted as satisfying this task
    - `python -m pytest -q` (full suite, no deselect) exits 0
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest -q</automated>
  </verify>
  <done>tests/test_recovery.py passes, including the exact multi-day BLOCKER 1 scenario and the W4 composed breaker-trip proof; the full suite (pre-existing + all of Phase 5) is green with zero deselected tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trader/ground_truth/report.py -> trader/paper/daily_report.py | A Phase 0 module optionally extended by Phase 5; failure must never break Phase 0's own report |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Denial of Service | trader/ground_truth/report.py | mitigate | The Phase 5 section append is wrapped in try/except; a Phase 5 failure degrades to "no paper section this run", never a missing Phase 0 report |
| T-05-13 | Tampering (crash-recovery double-order) | tests/test_recovery.py (process-restart proof) | mitigate | Explicit assertion on broker call COUNT across a real multi-day scenario, not just resulting row count or a same-day resubmit, so a masked double-submit cannot pass silently (BLOCKER 1) |
| T-05-14 | Repudiation | tests/test_recovery.py | mitigate | The clear_halt step in Test 1 is asserted to produce a `manual_restart_required` ops-log line, proving BLOCKER 3's wiring end to end, not just at the unit level |
</threat_model>

<verification>
python -m pytest -q
</verification>

<success_criteria>
Daily report gains an auditable paper-trading section including the Manual Interventions tally; crash-recovery is proven at the process-restart level across a real calendar-day boundary for both entry and exit paths, including the halt/clear step; a real breaker trip is proven to zero out new order submission; full suite green with nothing deselected.
</success_criteria>

<output>
Create `.planning/phases/05-paper-trading-loop/05-07-SUMMARY.md` when done
</output>
