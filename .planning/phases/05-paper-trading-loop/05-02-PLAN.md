---
phase: 05
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - trader/paper/calendar_.py
  - trader/paper/alerts.py
  - trader/paper/ops_log.py
  - tests/test_calendar.py
  - tests/test_alerts.py
autonomous: true
requirements: [PAPER-06, PAPER-07]

must_haves:
  truths:
    - "A known NYSE holiday is correctly reported as a non-trading day"
    - "A Telegram send failure never raises out of send_telegram_alert -- it returns False and logs, trading logic is never blocked (D-11)"
    - "The Telegram bot token never appears in any log line, even on a request failure"
    - "The ops log records a distinct 'scheduled_auth' entry type, separate from 'manual_restart_required', so D-13's weekly IBKR 2FA tap never counts as an unplanned intervention"
    - "A human can produce a scheduled_auth (or any other) ops-log entry from the command line without writing Python -- `python -m trader.paper.ops_log --entry-type scheduled_auth --message \"...\"` (REVISED: BLOCKER 2 -- D-13's weekly 2FA tap needs a producer the owner can actually run)"
  artifacts:
    - path: "trader/paper/calendar_.py"
      provides: "is_trading_day/session_window via pandas-market-calendars"
      contains: "def is_trading_day"
    - path: "trader/paper/alerts.py"
      provides: "Fire-and-forget Telegram sender with local-log fallback"
      contains: "def send_telegram_alert"
    - path: "trader/paper/ops_log.py"
      provides: "Rotating ops log appender + scheduled-run coverage computation + a CLI producer for scheduled_auth/manual_restart_required entries"
      contains: "def compute_run_coverage"
  key_links:
    - from: "trader/paper/alerts.py"
      to: "https://api.telegram.org/bot<token>/sendMessage"
      via: "requests.post, 5s timeout, never raises"
      pattern: "requests.post"
    - from: "trader/paper/ops_log.py"
      to: "reports/{date}.md (Plan 05-07 extends this)"
      via: "compute_run_coverage feeding the daily report's paper-trading section"
      pattern: "compute_run_coverage"
    - from: "trader/paper/ops_log.py main()"
      to: "05-09's monitoring-window runbook"
      via: "the owner runs this CLI after every weekly IBKR 2FA tap and after every manual clear (BLOCKER 2/3)"
      pattern: "argparse.*entry-type"
---

<objective>
Build the two ops-facing, broker-independent modules Phase 5 needs before
anything talks to IBKR: the NYSE trading-calendar gate and session-window
helper (`pandas-market-calendars`), and the alerting/logging pair (Telegram
fire-and-forget + rotating ops log with D-13's `scheduled_auth` exception
type and a command-line producer the owner can actually run). Both are
fully unit-testable with no broker, no daemon, no network in CI (Telegram
calls are mocked).

Purpose: give the entry pipeline (05-06) its trading-day gate and every
Phase 5 process its alerting/logging primitives, before broker wiring
begins -- and give the owner a concrete, scriptable way to log D-13's
weekly 2FA tap (plan-checker BLOCKER 2) and any manual intervention, so the
two-week exit gate's audit reads entirely from the ops log rather than
memory.
Output: trader/paper/calendar_.py, trader/paper/alerts.py,
trader/paper/ops_log.py (with CLI).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-paper-trading-loop/05-CONTEXT.md
@.planning/phases/05-paper-trading-loop/05-RESEARCH.md
@trader/ground_truth/poll.py

<interfaces>
Telegram send (verified pattern, 05-RESEARCH.md "Code Examples"):
    def send_telegram_alert(token: str, chat_id: str, text: str) -> bool
    POST https://api.telegram.org/bot{token}/sendMessage json={"chat_id","text"}
    timeout=5; catches every Exception, logs symbolically (never the token),
    returns False; never raises.

NYSE trading-day check (05-RESEARCH.md "Code Examples"):
    import pandas_market_calendars as mcal
    nyse = mcal.get_calendar("NYSE")
    schedule = nyse.schedule(start_date=d, end_date=d)
    is_trading_day = not schedule.empty
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NYSE trading calendar wrapper</name>
  <files>requirements.txt, trader/paper/calendar_.py, tests/test_calendar.py</files>
  <read_first>trader/ground_truth/poll.py's is_market_hours (the fixed-window predecessor this module supersedes for Phase 5's precision needs)</read_first>
  <behavior>
    - is_trading_day(date(2026, 7, 27)) -- a Monday -- returns True
    - is_trading_day(date(2026, 7, 25)) -- a Saturday -- returns False
    - is_trading_day(date(2026, 1, 1)) -- New Year's Day -- returns False
    - session_window(date(2026, 7, 27)) returns a (open_utc, close_utc) tuple
      of UTC-aware datetimes with open < close
    - session_window on a non-trading day raises ValueError, not a silent
      empty tuple
  </behavior>
  <action>
    Add `pandas-market-calendars==5.4.0` to requirements.txt (verified
    current on PyPI, slopcheck [OK]/Approved per 05-RESEARCH.md's Package
    Legitimacy Audit -- no install checkpoint required). Write
    trader/paper/calendar_.py: is_trading_day(check_date: date) -> bool
    using mcal.get_calendar("NYSE").schedule(start_date=check_date,
    end_date=check_date) and returning `not schedule.empty` (the verified
    05-RESEARCH.md pattern, do not re-derive differently). session_window(check_date:
    date) -> tuple[datetime, datetime]: call the same schedule() call;
    raise ValueError(f"{check_date} is not an NYSE trading day") if empty;
    otherwise return (schedule.iloc[0]["market_open"].to_pydatetime(),
    schedule.iloc[0]["market_close"].to_pydatetime()) -- both already UTC
    tz-aware per pandas-market-calendars' own convention, never
    tz_localize'd again here.
  </action>
  <acceptance_criteria>
    - is_trading_day never raises for any date input
    - session_window's returned datetimes have tzinfo set (not naive)
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_calendar.py -x -q</automated>
  </verify>
  <done>tests/test_calendar.py passes against real pandas-market-calendars NYSE data (no mocking needed -- this is a static calendar lookup, not a network call)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Telegram alerts + rotating ops log + CLI producer</name>
  <files>trader/paper/alerts.py, trader/paper/ops_log.py, tests/test_alerts.py</files>
  <read_first>trader/ground_truth/report.py's fetch_crypto_close (the existing "never log the secret itself, log only the failure" T-00-08 convention this mirrors)</read_first>
  <behavior>
    - send_telegram_alert("tok", "chat", "hello") with a mocked requests.post
      returning 200 returns True
    - send_telegram_alert with a mocked requests.post raising
      requests.exceptions.Timeout returns False and never raises
    - the log output of a failed send never contains the literal token string
    - notify("fill", "AAPL filled at 190.00") with TELEGRAM_BOT_TOKEN unset
      in the environment returns False and appends an ops_log entry instead
      of raising
    - append_ops_log("scheduled_auth", "weekly IBKR 2FA tap, D-13") followed
      by compute_run_coverage over a window containing it reports that
      entry with entry_type == "scheduled_auth", distinct from
      "manual_restart_required"
    - running `python -m trader.paper.ops_log --entry-type scheduled_auth
      --message "weekly 2FA tap"` (argv form, invoked directly in the test
      via ops_log.main(["--entry-type", "scheduled_auth", "--message",
      "weekly 2FA tap"])) appends exactly one 'scheduled_auth' line to the
      log and exits 0 (BLOCKER 2)
    - ops_log.main with an --entry-type outside _KNOWN_ENTRY_TYPES exits
      non-zero with a clear argparse-level error, never a raw traceback
  </behavior>
  <action>
    trader/paper/alerts.py: send_telegram_alert(token: str, chat_id: str,
    text: str) -> bool exactly per the verified 05-RESEARCH.md example
    (requests.post to f"https://api.telegram.org/bot{token}/sendMessage",
    json={"chat_id": chat_id, "text": text}, timeout=5,
    response.raise_for_status(), except Exception: log.warning with
    type(error).__name__ and the message text only -- never token or
    chat_id -- return False). notify(entry_type: str, message: str) -> bool:
    load_dotenv(), read config.TELEGRAM_BOT_TOKEN_ENV/TELEGRAM_CHAT_ID_ENV
    from os.environ; if either is missing, call
    ops_log.append_ops_log(entry_type, message) and return False (never
    raise -- D-11's "alert failure never blocks trading logic", including
    the "not configured yet" case before the 05-08 checkpoint runs);
    otherwise call send_telegram_alert and ALSO always call
    ops_log.append_ops_log(entry_type, message) regardless of the Telegram
    result, so the ops log is the durable source of truth even when
    Telegram is reachable.

    trader/paper/ops_log.py: append_ops_log(entry_type: str, message: str,
    log_path: str = "ops/paper_trading.log") -> None using
    logging.handlers.TimedRotatingFileHandler(log_path, when="midnight",
    backupCount=30) on a dedicated logger (module-level, configured once);
    write one line per call as "{iso_ts}|{entry_type}|{message}" (a
    pipe-delimited format chosen for compute_run_coverage's cheap regex
    parse, Claude's discretion on log format). entry_type is one of
    'scheduled_run', 'scheduled_auth' (D-13's weekly IBKR 2FA exception --
    distinct from 'manual_restart_required' so the two-week exit gate's
    "zero manual interventions" tally never conflates them), 'fill',
    'error', 'heartbeat', 'manual_restart_required' -- validated with a
    plain `if entry_type not in _KNOWN_ENTRY_TYPES: raise ValueError`, not a
    DB CHECK constraint (this is a file, not a table).
    compute_run_coverage(log_path: str, window_start: datetime,
    window_end: datetime, expected_cadence_minutes: int) -> dict: parse
    every 'scheduled_run' line's timestamp within [window_start,
    window_end], compute expected = (window_end - window_start) /
    cadence, completed = len(matching lines), return
    {"expected": int(expected), "completed": completed, "pct": completed /
    expected * 100 if expected else 0.0} -- mirrors
    trader.ground_truth.db.query_poll_run_coverage's shape exactly (same
    field names) so Plan 05-07's daily-report section can reuse the same
    rendering code Phase 0 already has.

    main(argv=None) -> None (NEW, BLOCKER 2 -- D-13's scheduled_auth
    producer and the general-purpose manual-entry CLI): argparse with
    --entry-type (choices=sorted(_KNOWN_ENTRY_TYPES), required) and
    --message (required, str) and --log-path (default
    "ops/paper_trading.log"). Calls append_ops_log(args.entry_type,
    args.message, args.log_path), prints a one-line confirmation ("Logged
    {entry_type} to {log_path}"), and exits 0. argparse's own `choices=`
    validation means an unknown --entry-type is rejected before
    append_ops_log's ValueError path is ever reached -- a clean CLI-level
    error, not a traceback. Invoked by the owner as:
    `python -m trader.paper.ops_log --entry-type scheduled_auth --message
    "weekly IBKR 2FA tap confirmed"` after every weekly 2FA approval
    (D-13), and as `python -m trader.paper.ops_log --entry-type
    manual_restart_required --message "<why>"` after clearing a Phase 4
    breaker via `trader.risk.clear_breaker` (BLOCKER 3's thin-wrapper note
    -- clear_breaker.py itself stays unmodified since Phase 4 already
    shipped; this CLI is the missing ops-log half of that existing clear
    path). Add `if __name__ == "__main__": main()` at module end.
  </action>
  <acceptance_criteria>
    - grep -c token trader/paper/alerts.py's log.warning call sites confirms
      no f-string embeds the token/chat_id variables directly
    - append_ops_log raises ValueError for any entry_type outside the six
      listed
    - ops_log.main(["--entry-type", "scheduled_auth", "--message", "x"])
      appends exactly one line and returns/exits without raising
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_alerts.py -x -q</automated>
  </verify>
  <done>tests/test_alerts.py passes, including the new CLI test case; no test performs a real network call (requests.post is always mocked)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trader/paper/alerts.py -> api.telegram.org | Outbound only; bearer token in URL path, never logged |
| trader/paper/ops_log.py -> ops/paper_trading.log | Local file, append-only, never parsed as executable |
| trader/paper/ops_log.py main() -> owner's shell | A human-invoked CLI; --message is free text but only ever appended to a log line, never executed or interpolated into SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Information Disclosure | trader/paper/alerts.py | mitigate | Every exception handler logs only `type(error).__name__` and the alert text, never the token/chat_id variables (standing rule 3, T-00-08 precedent) |
| T-05-07 | Denial of Service | trader/paper/alerts.py | mitigate | `send_telegram_alert` catches every `Exception`, 5s timeout, always returns bool, never raises into the caller's trading logic (D-11) |
</threat_model>

<verification>
python -m pytest tests/test_calendar.py tests/test_alerts.py -x -q
</verification>

<success_criteria>
trader/paper/calendar_.py correctly gates a real NYSE holiday; trader/paper/alerts.py never raises on failure and never logs a secret; trader/paper/ops_log.py distinguishes scheduled_auth from manual_restart_required and exposes both via a runnable CLI (BLOCKER 2).
</success_criteria>

<output>
Create `.planning/phases/05-paper-trading-loop/05-02-SUMMARY.md` when done
</output>
