---
phase: 05
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/0005_paper_trading.sql
  - trader/paper/__init__.py
  - trader/paper/config.py
  - trader/paper/idempotency.py
  - trader/paper/ledger.py
  - tests/conftest.py
  - tests/test_idempotency.py
  - tests/test_paper_ledger.py
autonomous: true
requirements: [PAPER-03, PAPER-05]

must_haves:
  truths:
    - "A deterministic order_ref built from the same (strategy_id, symbol, date, side, intent) tuple is always identical, and never collides across different tuples in the test fixture matrix"
    - "paper_orders, paper_positions, paper_trades, reconciliation_log, strategy_kill_state exist after migration 0005 applies and accept real-trade-format inserts"
    - "A strategy_id present in strategy_kill_state is reported retired; one absent is not"
  artifacts:
    - path: "migrations/0005_paper_trading.sql"
      provides: "Phase 5's five tables, additive to the shared data/trader.db"
      contains: "CREATE TABLE IF NOT EXISTS paper_orders"
    - path: "trader/paper/config.py"
      provides: "Frozen Phase 5 constants: IBKR port/host, cadences, the 5 live strategy configs parsed from KILL-CONDITIONS.md/oos_results_v2.json"
      contains: "IBKR_PAPER_PORT = 4002"
    - path: "trader/paper/idempotency.py"
      provides: "build_order_ref and the pure existing-order matcher"
      contains: "def build_order_ref"
    - path: "trader/paper/ledger.py"
      provides: "All Phase 5 table read/write functions other plans import directly"
      contains: "def record_from_live_fill"
  key_links:
    - from: "trader/paper/ledger.py"
      to: "migrations/0005_paper_trading.sql"
      via: "parameterized INSERT/SELECT against the five new tables"
      pattern: "INSERT INTO paper_"
    - from: "trader/paper/config.py"
      to: ".planning/phases/03-strategy-lab/KILL-CONDITIONS.md"
      via: "hardcoded LIVE_STRATEGY_CONFIGS mirroring the 5 committed survivor configs"
      pattern: "LIVE_STRATEGY_CONFIGS"
---

<objective>
Lay down Phase 5's foundation contracts: the migration 0005 schema (five new
tables), the frozen `trader/paper/config.py` constants (including the five
live D-01 strategy configs hardcoded from KILL-CONDITIONS.md and
`reports/backtests/oos_results_v2.json`), the pure idempotency-key builder
(D-06), and `trader/paper/ledger.py`'s full read/write surface. Every later
Phase 5 plan (broker adapters, reconciliation, guardian, entry pipeline)
imports these contracts directly — this plan's job is Interface-First: no
downstream plan should need to explore this module's internals.

Purpose: give every subsequent plan a stable, pre-tested contract so the
guardian/entry-pipeline/reconciliation logic (built against a MOCKED broker)
can be fully TDD'd before any human checkpoint.
Output: migrations/0005_paper_trading.sql, trader/paper/config.py,
trader/paper/idempotency.py, trader/paper/ledger.py, shared pytest fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-paper-trading-loop/05-CONTEXT.md
@.planning/phases/05-paper-trading-loop/05-RESEARCH.md
@.planning/phases/03-strategy-lab/KILL-CONDITIONS.md
@trader/backtest/config.py
@trader/data/db.py
@migrations/0004_risk_breakers.sql
@tests/conftest.py

<interfaces>
From trader/backtest/config.py (EXIT_PROFILE dataclass every position uses):
    @dataclass(frozen=True)
    class EXIT_PROFILE:
        stop_pct: float | None
        tp_pct: float | None
        scale_out: tuple[tuple[float, float], ...]
        trailing_pct: float | None
        max_hold_days: int | None
        eod_flat: bool

From trader/data/db.py (the migration runner every module already uses):
    def get_connection(db_path: str = "data/trader.db") -> sqlite3.Connection
    apply_migrations() scans migrations/*.sql by leading 4-digit filename
    version and applies each once — migrations/0005_paper_trading.sql needs
    no code registration, only the correctly-numbered file.

The five real D-01 survivor configs (verified directly from
reports/backtests/oos_results_v2.json this session — all five share
strategy="momentum_stock", entry_variant="loose", regime="choppy_v2",
bucket="stock", tp_pct=0.2, trailing_pct=None, scale_out=(), eod_flat=False):

  1. profile_name="momentum_stock_stock_choppy_v2_loose_tune_stop-0.3_tp0.2_trailNone_holdNone"
     stop_pct=-0.3, max_hold_days=None, max_dd_kill=-0.0096
  2. profile_name="momentum_stock_stock_choppy_v2_loose_tune_stop-0.25_tp0.2_trailNone_holdNone"
     stop_pct=-0.25, max_hold_days=None, max_dd_kill=-0.0377
  3. profile_name="momentum_stock_stock_choppy_v2_loose_tune_stop-0.3_tp0.2_trailNone_hold30"
     stop_pct=-0.3, max_hold_days=30, max_dd_kill=-0.0706
  4. profile_name="momentum_stock_stock_choppy_v2_loose_tune_stop-0.25_tp0.2_trailNone_hold30"
     stop_pct=-0.25, max_hold_days=30, max_dd_kill=-0.0714
  5. profile_name="momentum_stock_stock_choppy_v2_loose_tune_stop-0.2_tp0.2_trailNone_hold30"
     stop_pct=-0.2, max_hold_days=30, max_dd_kill=-0.0680

  All five: pf_floor=0.9 (rolling 30-trade profit factor), consecutive_loss_kill=8
  (KILL-CONDITIONS.md, PF_FLOOR/CONSECUTIVE_LOSS_KILL from
  trader.backtest.write_kill_conditions, unchanged v1->v2).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0005 + frozen config.py (contracts)</name>
  <files>migrations/0005_paper_trading.sql, trader/paper/__init__.py, trader/paper/config.py</files>
  <read_first>
    trader/backtest/config.py (EXIT_PROFILE), migrations/0004_risk_breakers.sql
    (append-only event-log + re-derived-state-view pattern to mirror),
    .planning/phases/03-strategy-lab/KILL-CONDITIONS.md
  </read_first>
  <action>
    Write migrations/0005_paper_trading.sql with five tables (D-09), following
    migrations/0004_risk_breakers.sql's append-only/re-derive-state
    convention exactly where state matters (reconciliation_log,
    strategy_kill_state), never a mutable "current state" column trusted
    directly (standing rule 4):

    paper_orders: order_id INTEGER PK AUTOINCREMENT; order_ref TEXT NOT NULL
    UNIQUE (the D-06 idempotency key); strategy_id TEXT NOT NULL (a
    profile_name from LIVE_STRATEGY_CONFIGS, or 'crypto_sim'); symbol TEXT
    NOT NULL; venue TEXT NOT NULL CHECK IN ('ibkr_paper','crypto_sim'); side
    TEXT NOT NULL CHECK IN ('buy','sell'); intent TEXT NOT NULL CHECK IN
    ('entry','exit_stop','exit_take_profit','exit_trailing_stop',
    'exit_scale_out','exit_time_stop','exit_eod_flat'); qty REAL NOT NULL;
    status TEXT NOT NULL DEFAULT 'submitted' CHECK IN
    ('submitted','filled','rejected'); perm_id INTEGER (IBKR's durable
    identifier, D-14, NULL for crypto_sim); submitted_ts TEXT NOT NULL
    DEFAULT (datetime('now')); filled_ts TEXT; fill_price REAL. Index on
    order_ref and on symbol.

    paper_positions: position_id INTEGER PK AUTOINCREMENT; strategy_id TEXT
    NOT NULL; symbol TEXT NOT NULL; venue TEXT NOT NULL CHECK IN
    ('ibkr_paper','crypto_sim'); asset_class TEXT NOT NULL; qty REAL NOT
    NULL; entry_price REAL NOT NULL; entry_ts TEXT NOT NULL;
    entry_order_ref TEXT NOT NULL; stop_pct REAL; tp_pct REAL;
    scale_out_json TEXT NOT NULL DEFAULT '[]' (JSON array of [gain_pct,
    fraction] pairs -- the locked EXIT_PROFILE serialized so the guardian
    can rebuild it later without ever re-deriving it from anywhere else,
    standing rule 2); trailing_pct REAL; max_hold_days INTEGER; eod_flat
    INTEGER NOT NULL DEFAULT 0; watermark REAL; status TEXT NOT NULL
    DEFAULT 'open' CHECK IN ('open','closed'). Index on status and on
    strategy_id.

    paper_trades: trade_id INTEGER PK AUTOINCREMENT; strategy_id TEXT NOT
    NULL; profile_name TEXT NOT NULL; symbol TEXT NOT NULL; venue TEXT NOT
    NULL CHECK IN ('ibkr_paper','crypto_sim'); asset_class TEXT NOT NULL;
    entry_ts TEXT NOT NULL; entry_price REAL NOT NULL; exit_ts TEXT NOT
    NULL; exit_price REAL NOT NULL; exit_reason TEXT NOT NULL CHECK IN
    ('stop','take_profit','trailing_stop','scale_out','time_stop',
    'eod_flat') (matches migrations/0003_backtest.sql's exit_reason values
    verbatim); qty REAL NOT NULL; fees REAL NOT NULL; slippage_cost REAL
    NOT NULL; pnl REAL NOT NULL; nzd_rate REAL (Phase 9 tax-logger hook,
    D-10, always NULL in Phase 5); nzd_pnl REAL (same); entry_order_ref
    TEXT NOT NULL; exit_order_ref TEXT NOT NULL. Index on (strategy_id,
    exit_ts).

    reconciliation_log: event_id INTEGER PK AUTOINCREMENT; ts TEXT NOT NULL
    DEFAULT (datetime('now')); symbol TEXT; venue TEXT NOT NULL CHECK IN
    ('ibkr_paper','crypto_sim','kraken_readonly'); local_qty REAL;
    broker_qty REAL; classification TEXT NOT NULL CHECK IN
    ('explainable','unexplained','informational'); action TEXT NOT NULL
    CHECK IN ('log','halt','clear'); reason TEXT; actor TEXT NOT NULL
    DEFAULT 'system' CHECK IN ('system','human'). Index on action. Add
    view reconciliation_halt_state: SELECT action AS current_action, ts AS
    since, reason FROM reconciliation_log WHERE action IN ('halt','clear')
    ORDER BY event_id DESC LIMIT 1 -- entries are halted iff this view's
    current_action = 'halt' (re-derived every read, standing rule 4, exact
    mirror of migrations/0004's breaker_state_current view).

    strategy_kill_state: strategy_id TEXT PRIMARY KEY (a profile_name);
    retired_at TEXT NOT NULL DEFAULT (datetime('now')); reason TEXT NOT
    NULL CHECK IN ('profit_factor_floor','max_drawdown',
    'consecutive_losses'); trigger_value REAL.

    Create trader/paper/__init__.py (empty). Create trader/paper/config.py:
    a frozen-constants-only module (no engine logic, per trader/risk/config.py's
    docstring convention). Define IBKR_HOST env var name "IBKR_HOST"
    (default "127.0.0.1"), IBKR_PAPER_PORT = 4002 (a literal int constant,
    documented as "standing rule 6 -- never 4001, this is the ONLY port
    Phase 5 code is allowed to connect to"), IBKR_CLIENT_ID_ENV =
    "IBKR_CLIENT_ID" (default 5), TELEGRAM_BOT_TOKEN_ENV =
    "TELEGRAM_BOT_TOKEN", TELEGRAM_CHAT_ID_ENV = "TELEGRAM_CHAT_ID",
    KRAKEN_API_KEY_ENV = "KRAKEN_API_KEY", KRAKEN_API_SECRET_ENV =
    "KRAKEN_API_SECRET" (D-04: read-only, optional, informational-only),
    GUARDIAN_CADENCE_MINUTES = 5, RECONCILE_CADENCE_SECONDS = 60,
    PAPER_ACCOUNT_EQUITY = 100_000.0 (Claude's discretion: the fake paper
    equity denominator the sizer's weight fractions are computed against,
    matching trader/backtest/config.py's DEFAULT_NOTIONAL/starting_equity
    convention for consistency; document as an assumption the owner may
    override once IBKR Gateway shows the real paper account's actual
    equity at the 05-08 ops checkpoint). Define a frozen
    @dataclass(frozen=True) LiveStrategyConfig with fields strategy_id,
    profile_name, exit_profile (an EXIT_PROFILE instance, imported from
    trader.backtest.config), pf_floor, max_dd_kill, consecutive_loss_kill.
    Build LIVE_STRATEGY_CONFIGS as a tuple of exactly the five
    LiveStrategyConfig instances listed in this plan's context block
    (strategy_id="momentum_stock" for all five; each EXIT_PROFILE
    constructed with scale_out=(), eod_flat=False per the verified
    oos_results_v2.json values -- do not invent or approximate any number,
    copy them verbatim). Build
    LIVE_STRATEGY_CONFIGS_BY_PROFILE_NAME: dict[str, LiveStrategyConfig]
    keyed by profile_name for O(1) lookup.
  </action>
  <acceptance_criteria>
    - migrations/0005_paper_trading.sql defines exactly the five tables
      above plus the reconciliation_halt_state view; every CHECK
      constraint listed is present verbatim
    - trader/paper/config.py has zero engine logic, zero I/O, and exactly
      five LiveStrategyConfig entries with the stop_pct/max_hold_days/
      max_dd_kill values transcribed exactly as listed in this plan's
      context block
    - IBKR_PAPER_PORT equals 4002 and is never referenced as a variable
      elsewhere in this file (a hardcoded literal, not derived)
  </acceptance_criteria>
  <verify>
    <automated>python -c "from trader.paper import config; assert config.IBKR_PAPER_PORT == 4002; assert len(config.LIVE_STRATEGY_CONFIGS) == 5; print('OK')"</automated>
  </verify>
  <done>migrations/0005_paper_trading.sql and trader/paper/config.py exist and import cleanly with the five live configs present</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Idempotency key builder + ledger read/write surface</name>
  <files>trader/paper/idempotency.py, trader/paper/ledger.py, tests/conftest.py, tests/test_idempotency.py, tests/test_paper_ledger.py</files>
  <read_first>
    trader/risk/breakers.py (append_breaker_event/read_breaker_state's
    parameterized-SQL, re-derive-from-event-log pattern to mirror for
    strategy_kill_state), trader/data/db.py (upsert_instrument's
    parameterized INSERT ... ON CONFLICT pattern)
  </read_first>
  <behavior>
    - build_order_ref("momentum_stock_..._holdNone", "AAPL", "2026-07-27", "buy", "entry")
      returns the same string every call; a different symbol or date
      produces a different string
    - find_existing_order(order_ref, local_orders={}, broker_fills=[]) returns None
    - find_existing_order(order_ref, local_orders={order_ref: {...}}, broker_fills=[])
      returns the local_orders entry without inspecting broker_fills
    - find_existing_order(order_ref, local_orders={}, broker_fills=[{"orderRef": order_ref, "execId": "x"}])
      returns that broker fill dict
    - ledger.open_position followed by ledger.get_open_positions round-trips
      stop_pct/tp_pct/scale_out (tuple, not list)/trailing_pct/max_hold_days/eod_flat
    - ledger.close_position marks the position 'closed' and inserts exactly
      one paper_trades row visible via ledger.get_recent_trades
    - ledger.retire_strategy is idempotent: calling it twice for the same
      strategy_id never raises and never creates a second row
    - ledger.is_strategy_retired returns False for an unretired strategy_id
      and True after ledger.retire_strategy
  </behavior>
  <action>
    trader/paper/idempotency.py: build_order_ref(strategy_id: str, symbol:
    str, trade_date: str, side: str, intent: str) -> str returning
    f"{strategy_id}:{symbol}:{trade_date}:{side}:{intent}" (pure, zero I/O,
    D-06). find_existing_order(order_ref: str, local_orders: dict[str,
    dict], broker_fills: list[dict]) -> dict | None: local_orders lookup
    first (a caller-supplied {order_ref: row} map from
    ledger.get_order_by_ref or a bulk fetch), else scan broker_fills for a
    dict whose "orderRef" key equals order_ref, else None. Zero I/O -- the
    caller fetches local_orders/broker_fills first.

    trader/paper/ledger.py, every function parameterized (ASVS V5, never
    string-interpolated SQL): record_order(conn, order_ref, strategy_id,
    symbol, venue, side, intent, qty, perm_id=None, status="submitted") ->
    None (INSERT INTO paper_orders). record_from_live_fill(conn, order_ref,
    strategy_id, symbol, venue, side, intent, qty, perm_id, fill_price,
    filled_ts) -> None (INSERT OR IGNORE INTO paper_orders with
    status='filled' -- the crash-heal path: a broker-confirmed fill this
    process never locally recorded before a crash). update_order_status(conn,
    order_ref, status, filled_ts=None, fill_price=None, perm_id=None) ->
    None. get_order_by_ref(conn, order_ref) -> dict | None.
    open_position(conn, strategy_id, symbol, venue, asset_class, qty,
    entry_price, entry_ts, entry_order_ref, exit_profile) -> int
    (position_id; json.dumps(list(exit_profile.scale_out)) into
    scale_out_json; int(exit_profile.eod_flat) into eod_flat).
    get_open_positions(conn, strategy_id=None) -> list[dict] (WHERE
    status='open', optionally filtered by strategy_id; json.loads
    scale_out_json back into a tuple of tuples before returning).
    close_position(conn, position_id, exit_ts, exit_price, exit_reason,
    exit_order_ref, fees, slippage_cost, pnl) -> None (UPDATE
    paper_positions SET status='closed' WHERE position_id=?, then INSERT
    INTO paper_trades reading strategy_id/symbol/venue/asset_class/entry_ts/
    entry_price/qty/profile_name(=strategy_id) off the paper_positions row
    itself so the caller never re-passes duplicate data). update_watermark(conn,
    position_id, watermark) -> None. get_recent_trades(conn, strategy_id,
    limit=30) -> list[dict] (ORDER BY exit_ts DESC LIMIT ?). is_strategy_retired(conn,
    strategy_id) -> bool (SELECT 1 FROM strategy_kill_state WHERE
    strategy_id=?). retire_strategy(conn, strategy_id, reason,
    trigger_value) -> None (INSERT OR IGNORE INTO strategy_kill_state --
    PK on strategy_id makes a second call a no-op, matching
    trader/risk/breakers.py's "never silently drops a real transition, but
    never double-fires either" discipline). get_kill_state(conn) ->
    dict[str, dict] (all strategy_kill_state rows keyed by strategy_id).

    Add a `paper_conn` fixture to tests/conftest.py: a live
    trader.data.db.get_connection(str(tmp_path / "trader.db")) connection
    (applies every migration including 0005), yielded then closed --
    parallel to the existing ground_truth `conn` fixture but using
    trader.data.db (the migration runner every Phase 1+ module shares),
    not trader.ground_truth.db.
  </action>
  <acceptance_criteria>
    - Every ledger.py function is parameterized SQL only -- no f-string or
      %-formatted SQL anywhere in this file
    - retire_strategy called twice for the same strategy_id leaves exactly
      one strategy_kill_state row (verified by test, not by inspection)
    - get_open_positions returns scale_out as a tuple, never a list
      (standing rule 2's immutability contract)
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_idempotency.py tests/test_paper_ledger.py -x -q</automated>
  </verify>
  <done>tests/test_idempotency.py and tests/test_paper_ledger.py pass; ledger.py's full surface is covered by at least one round-trip test</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trader/paper/ledger.py -> SQLite | Every write must be parameterized; symbol/reason strings can originate from scanner feeds |
| trader/paper/config.py -> KILL-CONDITIONS.md | The five live configs are a one-time transcription -- must never silently drift from the committed file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | trader/paper/ledger.py | mitigate | Every INSERT/UPDATE/SELECT uses `?` placeholders, never f-string/`.format()` interpolation of caller-supplied values (ASVS V5) |
| T-05-09 | Tampering | trader/paper/config.py | mitigate | LIVE_STRATEGY_CONFIGS is a frozen tuple of frozen dataclasses transcribed once from the committed KILL-CONDITIONS.md; no code path recomputes or edits these values from live/paper results (standing rule 1) |
| T-05-SC | Tampering | pip installs (ib_async, pandas-market-calendars — used by later plans) | accept | Both already `[OK]`/Approved in 05-RESEARCH.md's Package Legitimacy Audit; no [ASSUMED]/[SUS] flag, so no additional blocking checkpoint required for these two specific packages |
</threat_model>

<verification>
python -m pytest tests/test_idempotency.py tests/test_paper_ledger.py -x -q
python -m pytest -q --deselect tests/test_backtest_sanity.py
</verification>

<success_criteria>
migrations/0005_paper_trading.sql applies cleanly on a fresh DB; trader/paper/config.py, idempotency.py, ledger.py exist with the documented function surface; both new test files pass; full suite stays green.
</success_criteria>

<output>
Create `.planning/phases/05-paper-trading-loop/05-01-SUMMARY.md` when done
</output>
