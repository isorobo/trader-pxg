---
phase: 05
plan: 09
type: execute
wave: 7
depends_on: ["05-08"]
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [PAPER-01, PAPER-03, PAPER-04, PAPER-07]

must_haves:
  truths:
    - "All three Task Scheduler tasks (entry, guardian, reconcile) are registered and visible in Task Scheduler, each pointed at the correct .bat/.xml pair"
    - "One real paper order fills on the owner's DUR285675 account, its permId is recorded in paper_orders, and reconciliation reports zero unexplained divergence for it"
    - "The two-week unattended monitoring window is open, logged in STATE.md, with D-13's weekly 2FA tap pre-registered as a scheduled_auth exception, not a manual intervention"
    - "The owner has an explicit, written runbook step to run `python -m trader.paper.ops_log --entry-type scheduled_auth ...` after every weekly IBKR 2FA tap (REVISED: BLOCKER 2)"
    - "The owner's runbook explicitly covers both halt-clear paths: trader.paper.clear_halt (auto-logs manual_restart_required, BLOCKER 3) and trader.risk.clear_breaker (does NOT auto-log -- the owner must separately run the ops_log CLI with --entry-type manual_restart_required, BLOCKER 3's thin-wrapper note)"
    - "No fourth Task Scheduler task is registered for the heartbeat -- it rides the existing 5-minute guardian cadence (REVISED: BLOCKER 4), and STATE.md documents this so it is never mistaken for a missing task"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "The phase's monitoring-window entry, mirroring Phase 0's 00-05 pattern, plus the full ops runbook (scheduled_auth, both halt-clear paths, heartbeat cadence)"
      contains: "Phase 5"
  key_links:
    - from: "scripts/paper_entry_task.xml / paper_guardian_task.xml / paper_reconcile_task.xml"
      to: "Windows Task Scheduler"
      via: "schtasks /create /xml, run by the human per this phase's structure guidance"
      pattern: "schtasks /create"
    - from: "STATE.md runbook"
      to: "python -m trader.paper.ops_log --entry-type scheduled_auth/manual_restart_required"
      via: "the owner's routine command after every weekly 2FA tap and after every breaker clear (BLOCKER 2/3)"
      pattern: "ops_log --entry-type"
---

<objective>
Register the three Task Scheduler tasks, witness one real supervised paper
order round-trip on the owner's IBKR paper account, and open the two-week
unattended monitoring window -- the phase's wall-clock exit gate, exactly
like Phase 0's 00-05 checkpoint. This plan's monitoring-window documentation
now also carries the complete ops runbook the plan-checker required: the
`scheduled_auth` CLI step for the weekly 2FA tap (BLOCKER 2), the two
distinct halt-clear paths and which one requires a manual follow-up ops-log
call (BLOCKER 3), and an explicit note that the twice-daily heartbeat needs
no scheduling action at all -- it already rides the guardian's existing
5-minute cadence (BLOCKER 4).

Purpose: PAPER-07's "unattended overnight" requirement can only be proven
by running unattended; this plan is the last human-in-the-loop step before
that clock starts, and the runbook it leaves behind is what keeps the
two-week audit honest.
Output: three registered scheduled tasks; one witnessed live paper order;
STATE.md/ROADMAP.md updated to reflect the open monitoring window and the
full ops runbook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-paper-trading-loop/05-CONTEXT.md
@.planning/phases/00-ground-truth/00-05-PLAN.md
@scripts/poll_task.xml
@trader/paper/entry_pipeline.py

<interfaces>
From trader/paper/ops_log.py (Plan 05-02, includes the BLOCKER 2 CLI):
    python -m trader.paper.ops_log --entry-type scheduled_auth --message "<msg>"
    python -m trader.paper.ops_log --entry-type manual_restart_required --message "<msg>"

From trader/paper/clear_halt.py (Plan 05-04, includes the BLOCKER 3 revision):
    python -m trader.paper.clear_halt --reason "<why>"  # ALREADY appends
        manual_restart_required automatically -- no follow-up CLI call needed

From trader/risk/clear_breaker.py (Phase 4, unmodified, existing):
    python -m trader.risk.clear_breaker --reason "<why>"  # does NOT touch
        the Phase 5 ops log -- the owner must separately run
        `python -m trader.paper.ops_log --entry-type manual_restart_required
        --message "<why>"` immediately after (BLOCKER 3's thin-wrapper note)

From trader/paper/guardian.py (Plan 05-05, includes the BLOCKER 4 revision):
    The twice-daily heartbeat and the account-level breaker evaluation both
    ride the existing 5-minute Task Scheduler cadence already registered in
    Task 1 below -- no separate registration step exists or is needed.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>scripts/paper_entry.bat + paper_entry_task.xml (Plan 05-06), scripts/paper_guardian.bat + paper_guardian_task.xml (Plan 05-05), scripts/paper_reconcile.bat + paper_reconcile_task.xml (Plan 05-04) all exist and are ready to register -- per this phase's structure guidance, registration itself is your command to run, mirroring Phase 0's D-07 precedent. Exactly three tasks are needed; the twice-daily heartbeat (BLOCKER 4) rides the guardian task already in this list and needs no fourth registration.</what-built>
  <how-to-verify>
    Run these three commands from the repo root (PowerShell or cmd),
    adjusting the path if your checkout differs:

    schtasks /create /xml "scripts\paper_entry_task.xml" /tn "TraderAI_PaperEntry"
    schtasks /create /xml "scripts\paper_guardian_task.xml" /tn "TraderAI_PaperGuardian"
    schtasks /create /xml "scripts\paper_reconcile_task.xml" /tn "TraderAI_PaperReconcile"

    Confirm all three appear in Task Scheduler (taskschd.msc) under the
    Task Scheduler Library, each Enabled, each pointed at the correct
    .bat file and working directory. Confirm there is no fourth task to
    create for alerts/heartbeats -- there isn't one.
  </how-to-verify>
  <resume-signal>Reply "tasks registered" once all three appear Enabled in Task Scheduler.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full mocked-broker-tested pipeline (Plans 05-01 through 05-07) is ready for one real, supervised run against the owner's actual IB Gateway paper connection.</what-built>
  <how-to-verify>
    1. With IB Gateway running and logged in, Claude will run
       `python -m trader.paper.entry_pipeline --once` directly (a real
       call, not a test) on a live US trading day during/near market
       hours.
    2. Check IB Gateway's own Account/Trades window: confirm one order
       appears with a status consistent with the printed summary.
    3. Check the Telegram chat: confirm a "fill"-type alert arrived.
    4. Claude will then run `python -m trader.paper.reconcile --once` and
       report its output -- confirm it shows zero "unexplained"
       divergences for this order.
    5. Claude will query `paper_orders` for this order_ref and show you
       its recorded `perm_id` -- confirm it is a non-null integer.
  </how-to-verify>
  <resume-signal>Reply "order confirmed" once you've verified the fill in Gateway, the Telegram alert, and the recorded perm_id.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Open the two-week unattended monitoring window + write the full ops runbook</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>.planning/phases/00-ground-truth/00-05-PLAN.md (the exact monitoring-window pattern this mirrors)</read_first>
  <action>
    Update .planning/STATE.md's Current Position/Blockers sections: record
    that Phase 5's code is complete and verified, the three Task Scheduler
    tasks are registered, one supervised paper order round-tripped
    successfully, and the two-consecutive-week unattended monitoring
    window opened today's date.

    Write the following ops runbook verbatim into STATE.md (this is the
    plan-checker's required wiring for BLOCKER 2/3/4 -- do not summarize it
    away):

    1. Weekly IBKR 2FA tap (D-13, BLOCKER 2): every time the mobile app
       2FA approval happens (expected roughly weekly, Sunday ~01:00 ET),
       run `python -m trader.paper.ops_log --entry-type scheduled_auth
       --message "weekly IBKR 2FA tap confirmed"` immediately after. This
       is a pre-registered exception, not counted against "zero manual
       interventions" -- but only if it is actually logged this way.

    2. Clearing a reconciliation halt (trader.paper.clear_halt): run
       `python -m trader.paper.clear_halt --reason "<why>"`. This ALREADY
       appends a `manual_restart_required` ops-log entry automatically
       (BLOCKER 3) -- no separate ops_log call is needed for this path.

    3. Clearing a Phase 4 circuit breaker (trader.risk.clear_breaker,
       unmodified from Phase 4): run `python -m trader.risk.clear_breaker
       --reason "<why>"`, THEN separately run `python -m
       trader.paper.ops_log --entry-type manual_restart_required --message
       "<why>"` right after -- this second command is REQUIRED for this
       path specifically, since clear_breaker.py has no Phase 5 ops-log
       awareness and was intentionally left unmodified (BLOCKER 3's
       thin-wrapper note). Forgetting this step is the one way a real
       intervention could go unlogged -- call this out explicitly.

    4. Heartbeat (D-11, BLOCKER 4): no action needed -- the guardian's
       existing 5-minute Task Scheduler task already sends a heartbeat
       alert (and ops-log entry) at least once every 12 hours on its own.
       There is no fourth scheduled task and none should ever be created
       for this.

    Note D-13 explicitly: the weekly IBKR mobile 2FA tap, LOGGED per step 1
    above, is a pre-registered `scheduled_auth` ops-log exception, not
    counted against the "zero manual interventions" exit criterion; any
    OTHER manual action (a `manual_restart_required` breaker/halt clear per
    steps 2-3, a code fix, a Gateway crash requiring a manual restart) DOES
    count and resets nothing but must be logged and explained per
    05-CONTEXT.md's Specific Ideas -- and per Plan 05-07's daily-report
    Manual Interventions tally (W2), every one of these is independently
    auditable from `reconciliation_log`/`breaker_events` without relying on
    memory. Update .planning/ROADMAP.md's Phase 5 entry: mark all 9 plans
    checked off in the Plans list, leave Phase 5's checkbox itself
    unchecked until the two-week window's exit criteria (05-CONTEXT.md's
    domain section: two consecutive weeks unattended, zero unexplained
    divergences) are independently verified via `/gsd:verify-work` after
    the window closes.
  </action>
  <acceptance_criteria>
    - STATE.md names an explicit calendar date the monitoring window opened
    - STATE.md distinguishes scheduled_auth from manual_restart_required
      in its own words, not just by reference
    - STATE.md's runbook explicitly states that trader.risk.clear_breaker
      requires a SEPARATE ops_log call while trader.paper.clear_halt does
      NOT (the two paths are not symmetric and the runbook must say so)
    - STATE.md explicitly states no fourth Task Scheduler task exists or
      is needed for the heartbeat
  </acceptance_criteria>
  <verify>
    <human-check>Read the updated STATE.md entry and confirm the monitoring-window date, the D-13 exception wording, and all four runbook steps (scheduled_auth, clear_halt, clear_breaker, heartbeat) match your understanding of the exit gate.</human-check>
  </verify>
  <done>STATE.md and ROADMAP.md reflect an open two-week monitoring window with D-13's exception and the full BLOCKER 2/3/4 runbook explicitly documented</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Windows Task Scheduler -> the three .bat entrypoints | Runs under the human's own Windows account, LeastPrivilege, matching Phase 0's proven pattern |
| IB Gateway paper account -> real order placement | The first REAL (non-mocked) order this phase places; supervised, not autonomous |
| Owner's runbook -> ops log | The only place a Phase 4 breaker-clear's manual_restart_required entry gets recorded; a skipped step here is the one honest gap in this design |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Tampering | The first live paper order | mitigate | Placed only after Plan 05-08's port-4002 smoke test passed and only against the confirmed paper account DUR285675, witnessed by the human before the unattended window opens |
| T-05-14 | Repudiation | STATE.md runbook / trader.risk.clear_breaker path | accept | `clear_breaker.py` itself is out of Phase 5's scope to modify (Phase 4 already shipped); the runbook's explicit, called-out second step is the mitigation, not code -- a residual human-process risk, documented rather than silently assumed away |
</threat_model>

<verification>
Manual: Task Scheduler shows three Enabled tasks; one witnessed fill; STATE.md records the monitoring-window open date and the full four-step runbook.
</verification>

<success_criteria>
Scheduling is live, one real paper order is proven end to end (idempotency + reconciliation both confirmed against the real broker), and the two-week wall-clock exit gate is open and auditable, with a complete, explicit runbook for scheduled_auth, both halt-clear paths, and the heartbeat.
</success_criteria>

<output>
Create `.planning/phases/05-paper-trading-loop/05-09-SUMMARY.md` when done
</output>
