---
phase: 05
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - requirements.txt
  - trader/paper/broker_ibkr.py
  - trader/paper/broker_crypto_sim.py
  - tests/conftest.py
  - tests/test_broker_ibkr.py
  - tests/test_broker_crypto_sim.py
autonomous: true
requirements: [PAPER-01, PAPER-02]

must_haves:
  truths:
    - "The IBKR adapter only ever connects to port 4002 -- there is no code path that accepts or defaults to 4001"
    - "A stock sizer dollar allocation that would produce a fractional share count is rounded DOWN, never up, before an order is built"
    - "The crypto sim adapter never calls any Kraken/ccxt order-placement method -- price-only, fills are locally simulated"
  artifacts:
    - path: "trader/paper/broker_ibkr.py"
      provides: "Mockable thin ib_async adapter: connect/disconnect/place_order/snapshot/latest_price + round_shares_down"
      contains: "IBKR_PAPER_PORT"
    - path: "trader/paper/broker_crypto_sim.py"
      provides: "ccxt.binance() price fetch + Phase 2 fee/slippage simulated fill"
      contains: "def simulate_fill"
  key_links:
    - from: "trader/paper/broker_ibkr.py"
      to: "ib_async.IB"
      via: "constructor-injected ib_client, defaulting to a lazily-imported real IB() only on connect()"
      pattern: "def __init__"
    - from: "trader/paper/broker_crypto_sim.py"
      to: "trader.backtest.fills"
      via: "fee_for/apply_slippage reused verbatim for crypto_major/memecoin fills"
      pattern: "from trader.backtest.fills import"
---

<objective>
Build the two broker-facing adapters behind a mockable interface: the IBKR
paper-account adapter (`ib_async`, port 4002 only, D-03) and the crypto simulated (D-04)
ledger's price/fill adapter (`ccxt.binance()` + Phase 2's fee/slippage
model). Both are constructor-injectable so every later plan's tests run
against a fake client with zero network/Gateway dependency.

Purpose: the single place D-14's `permId`/`orderRef` semantics and the
fractional-share rounding pitfall are handled, so entry_pipeline (05-06)
and guardian (05-05) never touch `ib_async` objects directly.
Output: trader/paper/broker_ibkr.py, trader/paper/broker_crypto_sim.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-paper-trading-loop/05-CONTEXT.md
@.planning/phases/05-paper-trading-loop/05-RESEARCH.md
@trader/backtest/fills.py
@trader/paper/config.py

<interfaces>
From trader/paper/config.py (Plan 05-01, already committed):
    IBKR_HOST = "IBKR_HOST" (env var name), IBKR_PAPER_PORT = 4002,
    IBKR_CLIENT_ID_ENV = "IBKR_CLIENT_ID"

From trader/backtest/fills.py (reuse verbatim, never re-derive):
    def fee_for(asset_class: str, qty: float, price: float) -> float
    def apply_slippage(price: float, side: str, asset_class: str) -> float
    Raises KeyError for any asset_class outside
    {"stock","crypto_major","memecoin"} -- never silently defaults.

IBKR connection pattern (05-RESEARCH.md "Code Examples", verified against
official docs):
    from ib_async import IB
    ib = IB()
    ib.connect("127.0.0.1", 4002, clientId=5)
    permId/orderRef persist across reconnects; orderId/clientId do not
    (interactivebrokers.github.io/tws-api/open_orders.html).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IBKR paper-broker adapter (mockable) + whole-share rounding</name>
  <files>requirements.txt, trader/paper/broker_ibkr.py, tests/conftest.py, tests/test_broker_ibkr.py</files>
  <read_first>05-RESEARCH.md's Pattern 1 (idempotency dual-check) and Pitfall 2/5 (fractional shares, orderRef vs orderId/permId)</read_first>
  <behavior>
    - IBKRBrokerAdapter(ib_client=fake_ib).connect() calls fake_ib.connect
      with exactly (host, 4002, clientId=...) -- never any other port
    - place_order(symbol="AAPL", action="BUY", quantity=10, order_ref="x")
      against a fake_ib that returns a Trade-like object with
      order.orderRef and a permId sets both on the returned dict
    - snapshot() returns {"positions": {symbol: qty}, "open_orders": [...],
      "fills": [...]} built from fake_ib.positions()/openTrades()/fills()
    - round_shares_down(3412.50, 87.30) returns 39 (never 39.09, never 40)
    - round_shares_down(0.0, 87.30) returns 0
  </behavior>
  <action>
    Add `ib_async==2.1.0` to requirements.txt (verified current on PyPI,
    slopcheck [OK]/Approved per 05-RESEARCH.md's audit -- no install
    checkpoint required). Write trader/paper/broker_ibkr.py:
    IBKRBrokerAdapter.__init__(self, host=None, port=None, client_id=None,
    ib_client=None) storing host/port/client_id (falling back to
    config.IBKR_HOST/IBKR_PAPER_PORT/IBKR_CLIENT_ID_ENV-resolved-from-env
    only when the corresponding argument is None) and self._ib_client =
    ib_client (constructor injection point for tests -- production callers
    pass None and get a lazily-imported `from ib_async import IB; IB()`
    only inside connect(), never at import time, so importing this module
    never requires ib_async to be installed for pure-logic tests).
    connect(self) -> None: asserts self.port == 4002 (raise ValueError
    otherwise -- standing rule 6, defence in depth even though the
    constructor already pins it), calls
    self._ib_client.connect(self.host, self.port,
    clientId=self.client_id). disconnect(self) -> None:
    self._ib_client.disconnect(). place_order(self, symbol: str, action:
    str, quantity: int, order_ref: str) -> dict: builds a contract/MarketOrder
    via ib_async's Stock/MarketOrder (only when self._ib_client is a real
    IB -- tests inject a fake whose placeOrder(contract, order) returns a
    fake Trade with .order.orderRef and .order.permId already set to
    order_ref), sets order.orderRef = order_ref before calling placeOrder,
    returns {"order_ref": order_ref, "perm_id": trade.order.permId,
    "status": "submitted"}. snapshot(self) -> dict: calls
    self._ib_client.reqOpenOrders() then reads
    self._ib_client.positions()/self._ib_client.openTrades()/
    self._ib_client.fills(), returning {"positions": {p.contract.symbol:
    p.position for p in positions}, "open_orders": [...], "fills": [{...
    "orderRef": f.execution.orderRef, "permId": f.execution.permId, "symbol":
    f.contract.symbol, "qty": f.execution.shares} for f in fills]}.
    latest_price(self, symbol: str) -> float: reqMktData-based delayed
    price lookup (self._ib_client.reqMktData then read .marketPrice() or
    .last, whichever the injected fake exposes) -- document inline that
    paper accounts default to 15-20 minute delayed data (Pitfall 4,
    accepted, not silently treated as real-time).

    Module-level pure function round_shares_down(dollar_amount: float,
    price: float) -> int: return int(dollar_amount // price) if price > 0
    else 0 (floor division, never math.floor(x + 0.5) or round() --
    Pitfall 2: rounding UP or to-nearest can push notional over the
    sizer's cap, rounding down never can).

    Extend tests/conftest.py with a `fake_ib` fixture: a
    unittest.mock.MagicMock configured with .positions.return_value=[],
    .openTrades.return_value=[], .fills.return_value=[],
    .placeOrder.return_value=a MagicMock whose .order.orderRef and
    .order.permId are settable per-test -- shared by this plan's tests and
    every later plan (guardian, entry_pipeline, reconciliation) that needs
    a fake broker.
  </action>
  <acceptance_criteria>
    - connect() raises ValueError if constructed with port=4001 (or any
      value other than 4002) -- proven by a dedicated test, not just by
      the default
    - round_shares_down never returns a value that, multiplied by price,
      exceeds dollar_amount
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_broker_ibkr.py -x -q</automated>
  </verify>
  <done>tests/test_broker_ibkr.py passes entirely against the fake_ib fixture -- no real ib_async connection attempted</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crypto simulated-ledger price/fill adapter</name>
  <files>trader/paper/broker_crypto_sim.py, tests/test_broker_crypto_sim.py</files>
  <read_first>trader/data/crypto_source.py (the existing ccxt.binance() usage pattern this reuses), trader/backtest/fills.py</read_first>
  <behavior>
    - fetch_price("BTC/USDT", client=fake_ccxt) returns fake_ccxt's
      fetch_ticker("BTC/USDT")["last"]
    - simulate_fill("BTC/USDT", "buy", 0.01, 65000.0, "crypto_major") returns
      a dict whose fill_price reflects apply_slippage("buy") and whose fee
      equals fee_for("crypto_major", 0.01, 65000.0)
    - simulate_fill never imports or calls any ccxt method with "order" or
      "createOrder" in its name (crypto sim never places a real order,
      D-04)
  </behavior>
  <action>
    Write trader/paper/broker_crypto_sim.py: fetch_price(symbol: str,
    client=None) -> float: client = client or ccxt.binance() (lazy
    import inside the function body, matching trader/data/crypto_source.py's
    existing pattern); return client.fetch_ticker(symbol)["last"].
    simulate_fill(symbol: str, side: str, qty: float, reference_price:
    float, asset_class: str) -> dict: fill_price =
    fills.apply_slippage(reference_price, side, asset_class); fee =
    fills.fee_for(asset_class, qty, fill_price); return {"fill_price":
    fill_price, "fee": fee, "qty": qty} -- pure, zero I/O, reusing
    trader.backtest.fills verbatim (never re-implementing fee/slippage
    math here). Read-only Kraken sanity hook (D-04): a small
    kraken_balance_check(api_key, api_secret) -> dict | None function that
    returns None if either credential env var is unset (D-04's "if
    present" -- never a hard blocker), otherwise calls ccxt.kraken's
    fetch_balance() read-only and returns the raw balance dict;
    informational only, never wired to any halt path (Open Question 2's
    locked default).
  </action>
  <acceptance_criteria>
    - grep -i "createorder\|placeorder" trader/paper/broker_crypto_sim.py
      returns no matches
    - kraken_balance_check returns None (not an exception) when
      KRAKEN_API_KEY is unset
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_broker_crypto_sim.py -x -q</automated>
  </verify>
  <done>tests/test_broker_crypto_sim.py passes; no test performs a real network call (ccxt client is always mocked)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trader/paper/broker_ibkr.py -> IB Gateway (127.0.0.1:4002) | Localhost-only paper connection; never 4001 |
| trader/paper/broker_crypto_sim.py -> ccxt.binance() | Public, unauthenticated price feed only |
| trader/paper/broker_crypto_sim.py -> ccxt.kraken() | Read-only balance check only, D-04 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Tampering | trader/paper/broker_ibkr.py | mitigate | `connect()` raises `ValueError` if `port != 4002`, checked at call time in addition to the constructor default (standing rule 6, defence in depth) |
| T-05-10 | Tampering | trader/paper/broker_crypto_sim.py | mitigate | Module never imports or calls any ccxt order-placement method; `simulate_fill` is pure arithmetic reusing `trader.backtest.fills` (D-04 -- no Kraken order API calls in Phase 5 at all) |
</threat_model>

<verification>
python -m pytest tests/test_broker_ibkr.py tests/test_broker_crypto_sim.py -x -q
</verification>

<success_criteria>
Both broker adapters exist, are fully mockable, whole-share rounding is pinned by a dedicated test, and the crypto sim never places a real order.
</success_criteria>

<output>
Create `.planning/phases/05-paper-trading-loop/05-03-SUMMARY.md` when done
</output>
