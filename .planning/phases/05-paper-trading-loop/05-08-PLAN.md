---
phase: 05
plan: 08
type: execute
wave: 6
depends_on: ["05-07"]
files_modified:
  - .env.example
autonomous: false
requirements: [PAPER-01, PAPER-06]
user_setup:
  - service: ibkr_gateway
    why: "PAPER-01's stock leg (D-03) needs a live paper-account connection on port 4002; paper accounts cannot disable 2FA (05-RESEARCH.md Pitfall 1)"
    env_vars:
      - name: IBKR_HOST
        source: "Defaults to 127.0.0.1 -- only set if Gateway runs on a different host"
      - name: IBKR_CLIENT_ID
        source: "Any small integer not already in use by another Gateway API client, e.g. 5"
    dashboard_config:
      - task: "Install IB Gateway, log into paper account DUR285675, enable API access at Configure > Settings > API > Settings (Socket port 4002, trust 127.0.0.1, disable read-only API)"
        location: "IB Gateway desktop application"
  - service: telegram
    why: "PAPER-06's fill/stop/breaker/reconciliation/heartbeat alerts"
    env_vars:
      - name: TELEGRAM_BOT_TOKEN
        source: "@BotFather in Telegram: /newbot, follow prompts"
      - name: TELEGRAM_CHAT_ID
        source: "Message the new bot once, then Claude reads it via getUpdates"
    dashboard_config:
      - task: "Create the bot via @BotFather and send it one message so getUpdates can resolve chat_id"
        location: "Telegram app"

must_haves:
  truths:
    - ".env contains IBKR_CLIENT_ID, TELEGRAM_BOT_TOKEN, and TELEGRAM_CHAT_ID with no placeholder values remaining"
    - "A real (side-effect-free) IB Gateway connect/disconnect smoke test succeeds against port 4002"
    - "A real Telegram test message is sent and confirmed received by the owner"
  artifacts:
    - path: ".env.example"
      provides: "The documented key list (never real secrets) for this phase's external services"
      contains: "TELEGRAM_BOT_TOKEN"
  key_links:
    - from: ".env"
      to: "trader/paper/broker_ibkr.py + trader/paper/alerts.py"
      via: "python-dotenv load_dotenv() at process start (Phase 1's established secrets pattern)"
      pattern: "load_dotenv"
---

<objective>
Get the two external services this phase depends on into a working state:
IB Gateway installed, logged into the paper account, and API-enabled on
port 4002; a Telegram bot created and its token/chat_id in `.env`. Claude
automates every verification step it can (connection smoke test, test
alert send) immediately after each human action, per the automation-first
rule -- this checkpoint is not a substitute for automation, it exists only
for the two actions Claude genuinely cannot perform (GUI install/2FA login,
BotFather conversation).

Purpose: unblock PAPER-01 (broker connection) and PAPER-06 (alerts) for the
first supervised live paper order in Plan 05-09.
Output: a running, API-enabled IB Gateway; a working Telegram bot; a
populated `.env`; `.env.example` documenting the key list.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-paper-trading-loop/05-CONTEXT.md
@.planning/phases/05-paper-trading-loop/05-RESEARCH.md
@trader/paper/broker_ibkr.py
@trader/paper/alerts.py
@trader/paper/config.py
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>trader/paper/broker_ibkr.py (Plan 05-03) is ready to connect the moment a Gateway is reachable on 127.0.0.1:4002; nothing in this codebase can install or log into IB Gateway for you.</what-built>
  <how-to-verify>
    1. Install IB Gateway (not full TWS) from IBKR's website; choose the
       "Stable" channel.
    2. Launch it, select "Paper Trading", log in with paper account
       DUR285675's credentials, and complete the mobile-app 2FA approval
       when prompted (05-RESEARCH.md Pitfall 1 -- this cannot be
       automated on a paper account).
    3. Open Configure -> Settings -> API -> Settings. Set Socket port to
       4002 (never 4001). Add 127.0.0.1 to Trusted IPs. Uncheck
       "Read-Only API" (Phase 5 needs to place paper orders). Leave
       "Enable ActiveX and Socket Clients" checked.
    4. Leave Gateway running.
    5. Reply here once Gateway shows "API" as enabled and connected to the
       paper account.
  </how-to-verify>
  <resume-signal>Reply "gateway ready" once steps 1-4 are complete and Gateway is left running.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>trader/paper/alerts.py (Plan 05-02) is ready to send the moment TELEGRAM_BOT_TOKEN/TELEGRAM_CHAT_ID exist; creating a Telegram bot requires your own Telegram account and cannot be scripted.</what-built>
  <how-to-verify>
    1. In Telegram, message @BotFather: send `/newbot`, follow the
       prompts (choose a name and a username ending in "bot"). BotFather
       returns a token shaped like `123456789:AA...`.
    2. Send your new bot any one message (e.g. "hi") from your own
       Telegram account so it has a chat to reply into.
    3. Paste the token here (or add it directly to `.env` as
       TELEGRAM_BOT_TOKEN yourself, whichever you prefer -- Claude never
       needs to see it to proceed, only to confirm it's present in
       `.env`).
    4. Reply here once step 1-2 are done and the token is in `.env`.
  </how-to-verify>
  <resume-signal>Reply "telegram ready" (or paste the token if you'd rather Claude write .env for you) once the bot exists and you've messaged it once.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Automated connectivity + alert smoke test</name>
  <files>.env.example</files>
  <read_first>trader/paper/broker_ibkr.py's IBKRBrokerAdapter, trader/paper/alerts.py's notify</read_first>
  <action>
    Write .env.example (never .env itself, which stays uncommitted per
    standing rule 3) documenting: IBKR_HOST (optional, default
    127.0.0.1), IBKR_CLIENT_ID (e.g. 5), TELEGRAM_BOT_TOKEN,
    TELEGRAM_CHAT_ID, KRAKEN_API_KEY (optional, D-04 read-only),
    KRAKEN_API_SECRET (optional) -- placeholder values only
    ("your-token-here"), never real secrets. Once both checkpoint tasks
    above are confirmed ("gateway ready" and "telegram ready"/token
    provided), run these two side-effect-free real checks directly (not
    mocked -- this is the one place in Phase 5 planning where a real
    network/Gateway call is expected and correct): (a) resolve
    TELEGRAM_CHAT_ID automatically if not yet known, by calling
    `https://api.telegram.org/bot<token>/getUpdates` and extracting the
    numeric chat id from the most recent message the owner sent in step 2
    above, writing it into `.env`; (b) construct a real
    IBKRBrokerAdapter(), call .connect(), immediately call .disconnect()
    (a pure connectivity smoke test, no order placed, no subscription
    requested); (c) call alerts.notify("heartbeat", "Phase 5 ops setup
    checkpoint: connectivity smoke test") and confirm it returns True (a
    real Telegram message is sent). Report the three results plainly;
    if any fails, stop and surface the exact error rather than proceeding
    to Plan 05-09.
  </action>
  <acceptance_criteria>
    - .env.example lists every key above with placeholder values only,
      zero real secrets
    - the IB Gateway connect/disconnect smoke test and the Telegram
      heartbeat send both succeed before this plan is marked complete
  </acceptance_criteria>
  <verify>
    <human-check>Confirm you received the "Phase 5 ops setup checkpoint" Telegram heartbeat message on your phone, and that IB Gateway's own log shows one connect+disconnect from clientId matching IBKR_CLIENT_ID.</human-check>
  </verify>
  <done>IB Gateway is reachable on 4002, Telegram delivers a real message, and .env holds no placeholder values</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human <-> IB Gateway | Credentials and 2FA never touch this codebase; Gateway holds the session, this code only connects to localhost |
| Human <-> Telegram/BotFather | Bot token is owner-controlled; Claude never needs to see or log it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Information Disclosure | .env / .env.example | mitigate | `.env.example` contains placeholder values only; `.env` itself is already `.gitignore`d per standing rule 3 (verified, not re-added here) |
| T-05-06 | Tampering | IB Gateway API settings | mitigate | Step 3's Socket port is fixed at 4002 in the human instructions themselves, matching `trader/paper/broker_ibkr.py`'s hardcoded assertion |
</threat_model>

<verification>
Manual: Telegram heartbeat received; IB Gateway shows a connect+disconnect log line.
</verification>

<success_criteria>
IB Gateway is running, paper-logged-in, API-enabled on 4002; .env holds a working Telegram bot token and chat_id; both are proven live by a real (harmless) connectivity check, not merely "should work."
</success_criteria>

<output>
Create `.planning/phases/05-paper-trading-loop/05-08-SUMMARY.md` when done
</output>
