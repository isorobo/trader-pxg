---
phase: 04-risk-gate-sizer
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - trader/risk/sizer.py
  - tests/test_position_sizer.py
  - requirements.txt
autonomous: true
requirements: [RISK-02]

must_haves:
  truths:
    - "size_positions never produces a single-position weight above 0.50, a memecoin aggregate weight above 0.10, or a total invested weight above 0.90, for ANY input -- proven by a hypothesis property test, not just hand-picked examples"
    - "Capital freed by a binding cap always flows to cash, never redistributed to a surviving position (orchestrator-locked design, 04-RESEARCH.md Assumption A6)"
    - "The worked example in 04-RESEARCH.md Q3 is reproduced exactly as a golden-fixture regression test"
  artifacts:
    - path: "trader/risk/sizer.py"
      provides: "RISK-02's deterministic position sizer: select -> weight -> normalize -> cap -> re-cap -> cash-absorbs-remainder"
      exports: ["compute_volatility", "size_positions"]
      min_lines: 60
    - path: "tests/test_position_sizer.py"
      provides: "Golden-fixture regression test plus hypothesis property tests for the cap invariants"
    - path: "requirements.txt"
      provides: "hypothesis==6.161.5 pinned as a dev dependency, scoped to this plan's tests only"
  key_links:
    - from: "trader/risk/sizer.py"
      to: "trader/risk/config.py"
      via: "imports SIZER_TOP_N, SIZER_VOLATILITY_WINDOW_DAYS, SIZER_SINGLE_POSITION_CAP, SIZER_MEMECOIN_CAP, SIZER_CASH_RESERVE"
      pattern: "SIZER_"
    - from: "tests/test_position_sizer.py"
      to: "hypothesis"
      via: "property tests scoped locally via @settings(max_examples=..., deadline=None), no conftest.py or pyproject.toml change"
      pattern: "@settings\\("
---

<objective>
Implement RISK-02: the deterministic position sizer (D-03) -- top-3 concurrent cap, score x inverse-volatility weighting, 50% single-position cap, 10% memecoin aggregate cap, 10% cash reserve -- following 04-RESEARCH.md Q3's exact select -> weight -> normalize -> cap -> re-cap -> cash-absorbs-remainder order, proven correct both by a golden-fixture regression test and by hypothesis property tests.

Purpose: this is the phase's second safety layer -- "the code that must never be wrong" per CONTEXT.md's own framing; Plan 04-05's D-07 acceptance test depends on this plan's exact clip behavior.
Output: `trader/risk/sizer.py`, `tests/test_position_sizer.py`, `hypothesis==6.161.5` pinned in `requirements.txt`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-risk-gate-sizer/04-CONTEXT.md
@.planning/phases/04-risk-gate-sizer/04-RESEARCH.md
@trader/risk/config.py

<interfaces>
From trader/risk/config.py (Plan 04-01, consume only):
  SIZER_TOP_N=3, SIZER_VOLATILITY_WINDOW_DAYS=20, SIZER_SINGLE_POSITION_CAP=0.50, SIZER_MEMECOIN_CAP=0.10, SIZER_CASH_RESERVE=0.10

From trader/backtest/metrics.py (existing, convention reference only):
  `statistics.stdev` used with ddof=1 (sample standard deviation) throughout the project -- compute_volatility must match this convention, not population stdev
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: compute_volatility + size_positions -- deterministic order, golden fixture, open-positions generalization</name>
  <files>trader/risk/sizer.py, tests/test_position_sizer.py</files>
  <behavior>
    - compute_volatility(bars, window=20) returns the sample stdev (ddof=1) of the trailing 20-day daily close-to-close returns.
    - The exact 04-RESEARCH.md Q3 worked example (candidates A/B/C with scores 0.90/0.70/0.95 and vols 0.02/0.04/0.15, open_positions=[]) reproduces w_A=0.50 (capped), w_B~0.2289, w_C~0.0827, cash~0.1884, matching the research's hand-computed numbers to 4 decimal places.
    - A memecoin candidate whose post-normalization weight would exceed SIZER_MEMECOIN_CAP is scaled down so the memecoin aggregate equals exactly the cap, with freed weight flowing to cash, not to the other positions.
    - With open_positions already holding weight (e.g. one existing stock position at 0.30), the sizer only allocates the remaining budget (1 - cash_reserve - sum(open weights)) among new top-(SIZER_TOP_N - len(open_positions)) candidates, and never re-sizes the existing open positions.
    - The memecoin aggregate cap considers BOTH open_positions' existing memecoin weight and new memecoin candidates' weight together, never just the new ones in isolation.
    - sum(all returned position weights) + cash_weight == 1.0 exactly (tight tolerance), for both the golden fixture and the open-positions case.
  </behavior>
  <read_first>
    .planning/phases/04-risk-gate-sizer/04-RESEARCH.md Q3 in full (formula, worked example table, deterministic order steps 1-6, Pitfall 3)
    trader/backtest/metrics.py's `_sharpe_from_returns` (ddof=1 convention to mirror for compute_volatility)
    trader/risk/config.py's SIZER_* constants
  </read_first>
  <acceptance_criteria>
    - `python -m pytest tests/test_position_sizer.py -k "not hypothesis" -q -x` passes
    - The golden-fixture test's numeric assertions match 04-RESEARCH.md Q3's worked example table exactly via `pytest.approx` (tight tolerance)
    - No test asserts a "redistribute to survivors" behavior anywhere -- freed weight always lands in `cash_weight`
  </acceptance_criteria>
  <action>
    Create `trader/risk/sizer.py`. Implement `compute_volatility(bars: list[dict], window: int = config.SIZER_VOLATILITY_WINDOW_DAYS) -> float` using pandas: build a close-price Series from `bars` (ascending by `ts`, same `list[dict]` shape as `trader.data.db.read_bars_cache`), compute `.pct_change().tail(window).std(ddof=1)`. Implement `size_positions(scored_candidates: list[dict], equity: float, open_positions: list[dict], config=risk_config) -> dict` returning `{"positions": [...], "cash_weight": float}`. Each `scored_candidates` entry requires `symbol, venue, asset_class, score, volatility` (sigma > 0); each `open_positions` entry requires `symbol, venue, asset_class, weight` (fraction of equity already committed). Implement the deterministic order exactly per 04-RESEARCH.md Q3: (1) `available_slots = max(0, config.SIZER_TOP_N - len(open_positions))`; select the top `available_slots` scored_candidates by score descending; (2) `raw_i = score_i / volatility_i` for the selected set only; (3) `available_weight = max(0.0, 1.0 - config.SIZER_CASH_RESERVE - sum(op["weight"] for op in open_positions))`; normalize the raw weights so they sum to `available_weight` (this generalizes the worked example, which is the `open_positions=[]` special case where `available_weight == 0.90` exactly) -- `preliminary_i`; (4) single-position cap: `capped_i = min(preliminary_i, config.SIZER_SINGLE_POSITION_CAP)`; (5) memecoin aggregate cap across BOTH new memecoin candidates' capped weights AND `open_positions`' existing memecoin weight sum -- if the combined total exceeds `config.SIZER_MEMECOIN_CAP`, scale every NEW memecoin candidate's weight down proportionally so the combined total equals exactly the cap (existing open memecoin positions are never touched, only new ones absorb the scale-down); (6) `cash_weight = 1.0 - sum(op["weight"] for op in open_positions) - sum(final new-position weights)` -- capital freed by steps 4-5 flows here, never redistributed (document this explicitly in the docstring as the single highest-impact design decision per 04-RESEARCH.md Assumption A6). Return `{"positions": [{"symbol", "venue", "asset_class", "weight"} for each new candidate], "cash_weight": cash_weight}`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_position_sizer.py -k "not hypothesis" -q -x</automated>
  </verify>
  <done>compute_volatility and size_positions implemented exactly per the deterministic order; golden fixture matches research's worked example; open-positions generalization tested and correct; freed weight always lands in cash.</done>
</task>

<task type="auto">
  <name>Task 2: hypothesis property tests for cap invariants + requirements.txt pin</name>
  <files>tests/test_position_sizer.py, requirements.txt</files>
  <read_first>
    .planning/phases/04-risk-gate-sizer/04-RESEARCH.md Q5 (hypothesis scope recommendation, CI-speed profile guidance) and Package Legitimacy Audit (hypothesis already Approved -- no blocking checkpoint required)
    tests/test_position_sizer.py (Task 1's implementation this task tests against)
  </read_first>
  <acceptance_criteria>
    - `python -m pytest tests/test_position_sizer.py -q -x` passes in full, including hypothesis-based tests
    - `requirements.txt` contains a new line `hypothesis==6.161.5`
    - Every hypothesis test uses `@settings(max_examples=..., deadline=None)` for a CI-speed profile -- no test relying on default example counts or deadlines that could flake in CI; no modification to `tests/conftest.py` or `pyproject.toml`
    - The property test asserts, for every generated `(score, volatility, asset_class)` combination across 1-3 candidates and 0-2 open_positions: no returned position weight exceeds `config.SIZER_SINGLE_POSITION_CAP`, total memecoin weight (new + open) never exceeds `config.SIZER_MEMECOIN_CAP`, `cash_weight` is never negative, and `sum(all weights) + cash_weight == 1.0` within a tight floating-point tolerance
  </acceptance_criteria>
  <action>
    Add `hypothesis==6.161.5` to `requirements.txt` (already verified/Approved per 04-RESEARCH.md's Package Legitimacy Audit -- PyPI, 10+ years established, slopcheck [OK], no [ASSUMED]/[SUS]/[SLOP] verdict, so no blocking human-verify checkpoint is required) and run `pip install hypothesis==6.161.5` in the project's active environment. In `tests/test_position_sizer.py`, add hypothesis-based tests using `st.floats` (bounded, finite, `allow_nan=False, allow_infinity=False`) for score (0.01-1.0) and volatility (0.001-1.0, strictly positive to avoid division by zero), `st.sampled_from(["stock", "crypto_major", "memecoin"])` for asset_class, and `st.lists` (size 1-3) to build `scored_candidates`, plus a similarly generated small `open_positions` list (size 0-2, weights summing to at most 0.8 to leave room). Decorate each hypothesis test with `@settings(max_examples=50, deadline=None)` -- a CI-speed profile scoped locally per test via the decorator, not a global profile registered in `conftest.py`. Assert the four invariants above via plain `assert` statements inside each `@given`-decorated test body.
  </action>
  <verify>
    <automated>python -m pytest tests/test_position_sizer.py -q -x</automated>
  </verify>
  <done>hypothesis installed and pinned; property tests pass under the CI-speed profile; RISK-02's "must never be wrong" bar is met with a property-tested guarantee, not just hand-picked examples.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sizer.py inputs | Fully in-process, pure function; no network/user input at this phase |
| pip install hypothesis | Local dev-dependency install, already audited |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering | sizer.py inputs | accept | Pure in-process function, no network/user input at this phase |
| T-04-07 | Tampering | pip install -- hypothesis | accept | Already audited in 04-RESEARCH.md's Package Legitimacy Audit: PyPI, 10+ years established, millions/month downloads, slopcheck [OK], disposition Approved -- no blocking human-verify checkpoint required |
| T-04-08 | Repudiation | cap-violating output | mitigate | hypothesis property test (Task 2) proves the four cap invariants hold across generated inputs, not just hand-picked scenarios |
</threat_model>

<verification>
`python -m pytest tests/test_position_sizer.py -q` passes (golden fixture + property tests); `python -m pytest -q` (full suite) stays green.
</verification>

<success_criteria>
RISK-02 is fully implemented: the deterministic cap order matches 04-RESEARCH.md Q3 exactly; cap invariants are proven by property tests; freed weight always flows to cash.
</success_criteria>

<output>
Create `.planning/phases/04-risk-gate-sizer/04-03-SUMMARY.md` when done.
</output>
