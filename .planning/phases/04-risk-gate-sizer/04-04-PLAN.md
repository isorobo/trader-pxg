---
phase: 04-risk-gate-sizer
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - trader/risk/breakers.py
  - trader/risk/clear_breaker.py
  - tests/test_breakers.py
autonomous: true
requirements: [RISK-03]

must_haves:
  truths:
    - "evaluate_breakers never sees future equity points when evaluating 'as of' a given day -- proven by a no-lookahead test that would fail if a future recovery masked a real trip"
    - "All three breakers (daily_loss, drawdown, consecutive_loss) fire correctly against a real Phase 2 harness equity curve built via trader.backtest.metrics._build_daily_equity_curve"
    - "manual_restart_required can ONLY be cleared by trader.risk.breakers.clear_manual_restart, invoked as a human-run command (python -m trader.risk.clear_breaker) -- no other code path ever appends a manual_restart action or clears drawdown's tripped state"
    - "If the pure evaluation and the persisted breaker_state_current view disagree, the persistence layer writes the real transition rather than silently dropping it (standing rule 4)"
  artifacts:
    - path: "trader/risk/breakers.py"
      provides: "RISK-03's pure breaker evaluation plus thin, parameterized, append-only persistence"
      exports: ["evaluate_breakers", "append_breaker_event", "read_breaker_state", "record_breaker_transitions", "clear_manual_restart"]
      min_lines: 80
    - path: "trader/risk/clear_breaker.py"
      provides: "The sole human-invoked CLI entrypoint that clears manual_restart_required"
      exports: ["main"]
    - path: "tests/test_breakers.py"
      provides: "Unit + simulation coverage: incremental HWM, no-lookahead regression, persistence, and the manual-restart-only-clear-path invariant"
  key_links:
    - from: "trader/risk/breakers.py evaluate_breakers"
      to: "trader/backtest/metrics.py max_drawdown / _build_daily_equity_curve"
      via: "incremental HWM re-derivation mirrors the peak-tracking pattern, but is called once per new equity point, never over a future-inclusive curve"
      pattern: "_build_daily_equity_curve"
    - from: "trader/risk/breakers.py append_breaker_event / read_breaker_state"
      to: "migrations/0004_risk_breakers.sql"
      via: "reads/writes breaker_events and breaker_state_current, created by Plan 04-01"
      pattern: "breaker_events|breaker_state_current"
    - from: "trader/risk/clear_breaker.py"
      to: "trader/risk/breakers.py clear_manual_restart"
      via: "the ONLY caller of clear_manual_restart in the whole codebase"
      pattern: "clear_manual_restart"
---

<objective>
Implement RISK-03: the three circuit breakers (D-04, D-05) -- daily-loss halt, drawdown halt with manual restart, consecutive-loss halt -- as a pure evaluation function plus a thin, parameterized, append-only persistence layer, with exactly one human-invoked clear path for the drawdown breaker's `manual_restart_required` flag.

Purpose: this is the phase's third safety layer; the ASVS L1 security requirement for this phase centres entirely on this plan (no auto-clear path for manual_restart_required).
Output: `trader/risk/breakers.py`, `trader/risk/clear_breaker.py`, `tests/test_breakers.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-risk-gate-sizer/04-CONTEXT.md
@.planning/phases/04-risk-gate-sizer/04-RESEARCH.md
@trader/risk/config.py
@trader/backtest/metrics.py
@trader/data/db.py
@migrations/0004_risk_breakers.sql

<interfaces>
From trader/risk/config.py (Plan 04-01, consume only):
  BREAKER_DAILY_LOSS_PCT=-0.03, BREAKER_DRAWDOWN_PCT=-0.10, BREAKER_CONSECUTIVE_LOSSES=6

From migrations/0004_risk_breakers.sql (Plan 04-01, consume only):
  breaker_events(event_id, ts, breaker_type IN ('daily_loss','drawdown','consecutive_loss'), action IN ('trip','reset','manual_restart'), trigger_value, reason, actor IN ('system','human'))
  breaker_state_current(breaker_type, current_action, since, reason)  -- one row per breaker_type with events

From trader/backtest/metrics.py (existing, consume only -- DO NOT call max_drawdown directly inside evaluate_breakers, see Pitfall 2):
  _build_daily_equity_curve(trades: list[dict], starting_equity: float) -> list[float]   # trades need exit_ts + pnl keys
  max_drawdown(equity_curve: list[float]) -> float | None   # retrospective form; safe only over a COMPLETE curve

From trader/data/db.py (existing, consume only):
  get_connection(db_path) -> sqlite3.Connection
  upsert_instrument / write_bars_cache -- parameterized-query style to mirror exactly (ASVS V5)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: evaluate_breakers -- pure, incremental, no-lookahead</name>
  <files>trader/risk/breakers.py, tests/test_breakers.py</files>
  <behavior>
    - daily_loss trips when (equity[t]-equity[t-1])/equity[t-1] <= BREAKER_DAILY_LOSS_PCT for the LAST point in the equity_curve passed in; a smaller single-day loss does not trip it.
    - drawdown trips when (equity[t]-HWM)/HWM <= BREAKER_DRAWDOWN_PCT where HWM = max(equity_curve up to and including t), computed incrementally.
    - No-lookahead regression: a curve that dips below the drawdown threshold at index t then recovers above the prior peak by the final index -- calling evaluate_breakers with the curve truncated to [:t+1] reports drawdown_tripped=True at t, proving the breaker cannot "see" the future recovery (Pitfall 2).
    - consecutive_loss trips when the trailing N entries of trade_pnls (chronological order) are all negative and N >= BREAKER_CONSECUTIVE_LOSSES; a single winning trade anywhere resets the count from that point forward.
    - Simulation against a REAL Phase 2 harness equity curve: build synthetic trades (exit_ts + pnl), call trader.backtest.metrics._build_daily_equity_curve(trades, starting_equity), then step through the resulting curve day by day calling evaluate_breakers(curve[:i+1], pnls_closed_by_i, config) for each i, asserting each breaker trips on the exact day its condition is first met and not before.
  </behavior>
  <read_first>
    .planning/phases/04-risk-gate-sizer/04-RESEARCH.md Q4 in full (schema rationale, day-boundary caveat, HWM incremental-recompute guidance) and Pitfall 2 (lookahead bug)
    trader/backtest/metrics.py's `max_drawdown` / `_build_daily_equity_curve` / `_sharpe_from_returns` (peak-tracking pattern to mirror incrementally; ddof=1 convention)
    trader/risk/config.py's BREAKER_* constants
  </read_first>
  <acceptance_criteria>
    - `python -m pytest tests/test_breakers.py -k "evaluate" -q -x` passes
    - The no-lookahead test is written so it would fail if evaluate_breakers ever regressed to the retrospective `max_drawdown()` pattern (i.e. it asserts the truncated-curve call trips at the correct earlier index, not the full-curve final state)
    - `evaluate_breakers` performs zero DB/file I/O -- `sqlite3` / `trader.data.db` are never imported inside its call path
  </acceptance_criteria>
  <action>
    Create `trader/risk/breakers.py`. Implement `evaluate_breakers(equity_curve: list[float], trade_pnls: list[float], config=risk_config) -> dict` returning `{"daily_loss_tripped": bool, "daily_loss_value": float | None, "drawdown_tripped": bool, "drawdown_value": float | None, "consecutive_loss_tripped": bool, "consecutive_loss_count": int}`. `equity_curve` is assumed already truncated by the caller to "as of now" (one point per UTC calendar day, matching `_build_daily_equity_curve`'s daily granularity) -- document loudly in the docstring that the safety of the HWM line depends entirely on the caller never passing future points (Pitfall 2). Daily loss: if `len(equity_curve) >= 2`, compute `(equity_curve[-1] - equity_curve[-2]) / equity_curve[-2]`; trip if `<= config.BREAKER_DAILY_LOSS_PCT`; else `daily_loss_value = None`, not tripped. Drawdown: `hwm = max(equity_curve)` (this IS the incremental HWM precisely because the caller only ever passes points up to "now"), then `(equity_curve[-1] - hwm) / hwm`; trip if `<= config.BREAKER_DRAWDOWN_PCT`. Consecutive loss: walk `trade_pnls` from the end backwards counting consecutive `pnl < 0` entries until a non-negative pnl or the list start; trip if the count `>= config.BREAKER_CONSECUTIVE_LOSSES`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_breakers.py -k "evaluate" -q -x</automated>
  </verify>
  <done>All three breakers evaluate correctly and incrementally; the no-lookahead regression test passes; the function is provably pure (no I/O imports).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persistence layer (append-only, parameterized) + the human-only manual-restart clear path</name>
  <files>trader/risk/breakers.py, trader/risk/clear_breaker.py, tests/test_breakers.py</files>
  <behavior>
    - append_breaker_event(conn, breaker_type, action, trigger_value, reason, actor) inserts exactly one row via a parameterized statement (ASVS V5) -- a SQL-injection-shaped reason string is stored literally, never executed.
    - read_breaker_state(conn) returns all three breaker_types every time, defaulting to {"current_action": "normal", "since": None, "reason": None} for any breaker_type with zero events.
    - After a 'trip' event for drawdown, read_breaker_state reports current_action="trip"; after clear_manual_restart(conn, reason=...), it reports current_action="manual_restart" with the appended row's actor exactly "human".
    - record_breaker_transitions(conn, previous_state, evaluation) appends 'trip' only on a normal->tripped transition (no duplicate trip rows while already tripped) and appends 'reset' automatically for daily_loss/consecutive_loss on a tripped->normal transition, but NEVER auto-appends 'reset' or 'manual_restart' for drawdown.
    - Grepping trader/risk/breakers.py's source for the literal string "manual_restart" finds it used in exactly one function (clear_manual_restart).
    - `python -m trader.risk.clear_breaker --db-path <tmp> --reason "..."` clears drawdown's manual_restart_required, and the resulting row's actor is "human".
  </behavior>
  <read_first>
    migrations/0004_risk_breakers.sql (Plan 04-01 -- exact column/CHECK shapes)
    trader/data/db.py's `upsert_instrument` / `write_bars_cache` (parameterized-query convention to mirror exactly, ASVS V5)
    CLAUDE.md's standing rule 4 ("if the system and the exchange disagree, halt")
  </read_first>
  <acceptance_criteria>
    - `python -m pytest tests/test_breakers.py -q -x` passes in full (Task 1 + Task 2 together)
    - Every SQL statement in `breakers.py` uses `?`/named parameterized placeholders -- zero f-string/`.format()`/`%`-interpolated SQL
    - The "manual_restart" literal-string grep test passes, proving no auto-clear path exists anywhere in the module
    - `trader/risk/clear_breaker.py` has no importer anywhere in the codebase except as `__main__` or directly in tests -- never called from `evaluate_breakers`, `record_breaker_transitions`, or any other automated path
  </acceptance_criteria>
  <action>
    Implement `append_breaker_event(conn, breaker_type: str, action: str, trigger_value: float | None = None, reason: str | None = None, actor: str = "system") -> None` using a single parameterized `INSERT INTO breaker_events (breaker_type, action, trigger_value, reason, actor) VALUES (?, ?, ?, ?, ?)` statement (mirroring `trader/data/db.py`'s exact style), followed by `conn.commit()`. Implement `read_breaker_state(conn) -> dict` keyed by each of `"daily_loss"`, `"drawdown"`, `"consecutive_loss"` -- query `breaker_state_current`, defaulting any missing `breaker_type` to `{"current_action": "normal", "since": None, "reason": None}`. Implement `record_breaker_transitions(conn, previous_state: dict, evaluation: dict) -> None`: for `daily_loss` and `consecutive_loss`, compare `previous_state[type]["current_action"]` against the new tripped boolean from `evaluation` -- append `action="trip"` on normal->tripped, append `action="reset"` on tripped->normal, append nothing if unchanged; for `drawdown`, append `action="trip"` on normal->tripped ONLY -- by construction, no code path in this function ever passes `action="reset"` for `breaker_type="drawdown"`. Implement `clear_manual_restart(conn, reason: str) -> None` as the sole function permitted to call `append_breaker_event(conn, "drawdown", "manual_restart", reason=reason, actor="human")` -- document in its docstring that this is the ONLY clear path for the drawdown breaker's halt (security requirement: no auto-clear path). Create `trader/risk/clear_breaker.py` with `main(argv=None)` using `argparse` (`--reason` required, `--db-path` default `"data/trader.db"`), opening a connection via `trader.data.db.get_connection(args.db_path)`, calling `breakers.clear_manual_restart(conn, reason=args.reason)`, and printing a confirmation line including the reason and timestamp; guard the module with `if __name__ == "__main__": main()`. Document the exact invocation command in this file's module docstring: `python -m trader.risk.clear_breaker --reason "<why>"`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_breakers.py -q -x</automated>
  </verify>
  <done>Persistence is thin, parameterized, and append-only; read_breaker_state always reports all three breaker types; the drawdown manual-restart flag has exactly one clear path in the entire codebase, invoked only as an explicit human-run command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| breakers.py <-> SQLite (breaker_events) | In-process, parameterized only, no network |
| clear_breaker.py | The phase's one deliberately human-gated control surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Tampering | breaker_events INSERT statements | mitigate | Parameterized placeholders only (ASVS V5), mirrors trader/data/db.py's convention exactly; test asserts a SQL-injection-shaped reason string is stored literally, never executed |
| T-04-10 | Elevation of Privilege | manual_restart_required auto-clear | mitigate | clear_manual_restart is the ONLY function that ever writes action="manual_restart"; a literal-string grep test fails the build if a second writer is introduced; the CLI script is the only invoker (security requirement) |
| T-04-11 | Repudiation | breaker state drift (system vs external) | mitigate | Standing rule 4 honored by re-deriving state from the append-only event log (breaker_state_current) rather than trusting a cached column -- record_breaker_transitions only writes a transition it can justify from a fresh evaluate_breakers() call |
| T-04-SC | Tampering | pip installs | accept | No new dependency in this plan |
</threat_model>

<verification>
`python -m pytest tests/test_breakers.py -q` passes (evaluate + persistence + CLI); `python -m pytest -q` (full suite) stays green.
</verification>

<success_criteria>
RISK-03 is fully implemented: all three breakers fire correctly against real Phase 2 harness equity curves; the no-lookahead HWM is proven by test; manual restart has exactly one, human-only clear path.
</success_criteria>

<output>
Create `.planning/phases/04-risk-gate-sizer/04-04-SUMMARY.md` when done.
</output>
