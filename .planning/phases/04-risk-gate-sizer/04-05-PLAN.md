---
phase: 04-risk-gate-sizer
plan: 05
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - tests/test_poisoned_list.py
autonomous: true
requirements: [RISK-04, RISK-01, RISK-02]

must_haves:
  truths:
    - "The committed 7-entry poisoned candidate list produces EXACTLY the expected accept/reject/clip outcomes with the exact reason codes from 04-RESEARCH.md Q6 -- D-07's literal exit-gate acceptance test"
    - "The gate and sizer are exercised together as a two-stage pipeline in this one test file, since the 'oversized memecoin allocation' scenario is a sizer behavior, not a gate rejection"
    - "This test, plus tests/test_risk_gate.py, tests/test_position_sizer.py, and tests/test_breakers.py, together are the phase's full RISK-04 coverage"
  artifacts:
    - path: "tests/test_poisoned_list.py"
      provides: "The committed poisoned fixture (7 entries) and the two-stage gate+sizer acceptance assertions"
      min_lines: 80
  key_links:
    - from: "tests/test_poisoned_list.py"
      to: "trader/risk/gate.py apply_risk_gate"
      via: "stage 1 of the pipeline"
      pattern: "apply_risk_gate\\("
    - from: "tests/test_poisoned_list.py"
      to: "trader/risk/sizer.py size_positions"
      via: "stage 2 of the pipeline, consuming stage 1's accepted candidates"
      pattern: "size_positions\\("
---

<objective>
Implement D-07/RISK-04's exit-gate acceptance test -- the phase's "soul" per CONTEXT.md -- a committed 7-entry poisoned candidate list (illiquid stock, brand-new token, wide-spread stock, a correlated pair, an oversized memecoin allocation, plus two clean survivors) that produces exactly the right accept/reject/clip outcomes with the exact reason codes, exercising the gate and sizer as a two-stage pipeline.

Purpose: this is Phase 4's literal exit criterion (ROADMAP.md success criterion 1) and the project's highest test bar so far.
Output: `tests/test_poisoned_list.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-risk-gate-sizer/04-CONTEXT.md
@.planning/phases/04-risk-gate-sizer/04-RESEARCH.md
@trader/risk/gate.py
@trader/risk/sizer.py
@trader/risk/config.py

<interfaces>
From trader/risk/gate.py (Plan 04-02, consume only):
  apply_risk_gate(candidates: list[dict], market_data: dict, config) -> tuple[list[dict], list[dict]]
  REJECT_LIQUIDITY, REJECT_LISTING_AGE, REJECT_SPREAD, REJECT_CORRELATION

From trader/risk/sizer.py (Plan 04-03, consume only):
  size_positions(scored_candidates: list[dict], equity: float, open_positions: list[dict], config) -> dict {"positions": [...], "cash_weight": float}

From trader/risk/config.py (Plan 04-01, consume only):
  MIN_DOLLAR_VOLUME_STOCK=1_000_000.0, MIN_LISTING_AGE_DAYS=30
  MAX_SPREAD_PCT={"stock": 0.05, "crypto_major": 0.10, "memecoin": 4.0}
  CORRELATION_THRESHOLD=0.8, SIZER_MEMECOIN_CAP=0.10
</interfaces>

<fixture>
## The committed 7-entry poisoned list (04-RESEARCH.md Q6)

| # | Candidate | Asset class | Construction | Gate outcome | Reason code |
|---|---|---|---|---|---|
| 1 | ILLQ | stock | 25 bars, close=10.0/volume=15,000 constant -> $150,000/day median dollar volume (below the $1,000,000 floor) | Gate-rejected | REJECT_LIQUIDITY |
| 2 | NEWTOK/USDT | memecoin | 5 bars only (ample volume, so liquidity alone would pass) | Gate-rejected | REJECT_LISTING_AGE |
| 3 | WIDESPRD | stock | 200 bars, ample volume ($5,000,000/day), spread_pct=0.5 (exceeds the 0.05 stock ceiling) | Gate-rejected | REJECT_SPREAD |
| 4a | CORRA | stock | 60 bars, fabricated correlated return series, score=0.6 | Gate-rejected (lower score) | REJECT_CORRELATION |
| 4b | CORRB | stock | 60 bars, fabricated correlated return series (correlation with 4a > 0.8), score=0.9 | Gate-accepted | -- |
| 5 | MEMER/USDT | memecoin | Ample volume/age/spread, score=0.95, sizer volatility=0.03 (raw pre-cap weight ~0.42) | Gate-accepted, sizer-clipped to 0.10 | -- (sizer clip, not a gate reason code) |
| 6 | CLEAN1 | stock | Ample everything, score=0.7, sizer volatility=0.05 | Gate-accepted, sized normally | -- |
| 7 | CLEAN2 | crypto_major | Ample everything, score=0.6, sizer volatility=0.06 | Gate-accepted, excluded from sizer's top-3 (4th by score) | -- |

Worked sizer arithmetic for the top-3 (MEMER/USDT, CORRB, CLEAN1), equity=100,000, open_positions=[]:
raw = score/volatility -> MEMER=0.95/0.03=31.67, CORRB=0.9/0.04=22.5, CLEAN1=0.7/0.05=14.0 (sum=68.17)
normalized to available_weight=0.90 -> w_MEMER=0.4181, w_CORRB=0.2971, w_CLEAN1=0.1848
single-position cap (0.50): none exceed
memecoin cap (0.10): w_MEMER clipped from 0.4181 to exactly 0.10, freed 0.3181 flows to cash
final: MEMER=0.10, CORRB~0.2971, CLEAN1~0.1848, cash=1-0.10-0.2971-0.1848~0.4181
</fixture>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Committed 7-entry poisoned fixture + gate-stage assertions</name>
  <files>tests/test_poisoned_list.py</files>
  <read_first>
    .planning/phases/04-risk-gate-sizer/04-RESEARCH.md Q6 (fixture table + rationale) and Pitfall 1 (a correlated pair test is required, not just same-class liquidity/age checks)
    trader/risk/gate.py's apply_risk_gate / reason-code constants (Plan 04-02)
    trader/risk/config.py's exact threshold values (Plan 04-01)
  </read_first>
  <acceptance_criteria>
    - `python -m pytest tests/test_poisoned_list.py -k "gate" -q -x` passes
    - All 4 gate-level rejections (ILLQ, NEWTOK/USDT, WIDESPRD, CORRA) carry exactly the reason codes REJECT_LIQUIDITY, REJECT_LISTING_AGE, REJECT_SPREAD, REJECT_CORRELATION respectively -- asserted individually, not just "gate rejected something"
    - CORRB, MEMER/USDT, CLEAN1, CLEAN2 are all present in the accepted set
    - A setup assertion confirms the fabricated CORRA/CORRB pair's actual computed correlation exceeds config.CORRELATION_THRESHOLD (0.8) BEFORE asserting gate behavior, so the fixture is self-verifying rather than dependent on hand-guessed numbers
  </acceptance_criteria>
  <action>
    In `tests/test_poisoned_list.py`, construct the 7-entry candidate list and `market_data` dict exactly per this plan's `<fixture>` table. For CORRA/CORRB, generate via `numpy.random.default_rng(42)`: build `returns_a` (length 60, small-magnitude gaussian), build `returns_b = 0.9 * returns_a + 0.1 * independent_noise` scaled so the pearson correlation exceeds 0.8, reconstruct price series from each via cumulative product from an arbitrary start price (e.g. 100.0) over 60 calendar-day timestamps, and assert the actual correlation (via `pandas.Series.corr` on the reconstructed daily returns) exceeds 0.8 in the test's own setup BEFORE invoking the gate. Build all other entries' bars/spread_pct/asset_class per the fixture table (ILLQ: 25 bars, close=10.0, volume=15,000 constant; NEWTOK/USDT: 5 bars, ample volume; WIDESPRD: 200 bars, ample volume, spread_pct=0.5; MEMER/USDT, CLEAN1, CLEAN2: ample volume/age/spread per the table). Call `gate.apply_risk_gate(candidates, market_data, risk_config)` once and assert the full partition: `rejected` contains ILLQ (REJECT_LIQUIDITY), NEWTOK/USDT (REJECT_LISTING_AGE), WIDESPRD (REJECT_SPREAD), CORRA (REJECT_CORRELATION); `accepted` contains CORRB, MEMER/USDT, CLEAN1, CLEAN2 and no others.
  </action>
  <verify>
    <automated>python -m pytest tests/test_poisoned_list.py -k "gate" -q -x</automated>
  </verify>
  <done>The committed fixture reproduces every gate-stage outcome from 04-RESEARCH.md Q6 exactly, self-verified for the fabricated correlation.</done>
</task>

<task type="auto">
  <name>Task 2: Two-stage pipeline -- sizer clips the memecoin allocation, full D-07 cross-check</name>
  <files>tests/test_poisoned_list.py</files>
  <read_first>
    trader/risk/sizer.py's size_positions contract (Plan 04-03)
    This plan's `<fixture>` block's worked sizer arithmetic (top-3 selection, raw/normalize/cap numbers)
  </read_first>
  <acceptance_criteria>
    - `python -m pytest tests/test_poisoned_list.py -q -x` passes in full (Task 1 + Task 2 together, the complete D-07 acceptance test)
    - The sizer step selects exactly MEMER/USDT, CORRB, CLEAN1 as the top-3 by score among the gate's 4 accepted candidates (CLEAN2 excluded, 4th by score)
    - MEMER/USDT's final weight is clipped to exactly `config.SIZER_MEMECOIN_CAP` (0.10), not its uncapped raw-normalized weight (~0.42)
    - CORRB and CLEAN1's weights match this plan's worked arithmetic within `pytest.approx(rel=1e-3)`
    - `cash_weight` absorbs the memecoin cap's freed weight (no redistribution to CORRB/CLEAN1)
  </acceptance_criteria>
  <action>
    Attach a `volatility` field to each of the gate's 4 accepted candidates for the sizer step (MEMER/USDT=0.03, CORRB=0.04, CLEAN1=0.05, CLEAN2=0.06 -- hardcoded here rather than derived from bars, since this test exercises the gate+sizer reason-code/clip contract, not `compute_volatility`'s own math, which Plan 04-03 already unit-tests separately). Call `sizer.size_positions(scored_candidates=<the 4 accepted, each with volatility attached>, equity=100_000.0, open_positions=[], config=risk_config)`. Assert the returned `positions` list contains exactly MEMER/USDT, CORRB, and CLEAN1 (not CLEAN2), with MEMER/USDT's weight exactly `0.10` and CORRB/CLEAN1's weights matching this plan's worked arithmetic (`raw = score/volatility`, normalized to 0.90, single-position-cap-checked, memecoin-cap-clipped) within `pytest.approx(rel=1e-3)`, and `cash_weight` equal to `1.0 - 0.10 - w_CORRB - w_CLEAN1` (absorbing the freed memecoin weight, never redistributed). This completes D-07's full two-stage acceptance test -- the phase's exit-gate criterion for RISK-04.
  </action>
  <verify>
    <automated>python -m pytest tests/test_poisoned_list.py -q -x</automated>
  </verify>
  <done>The complete 7-entry poisoned list produces exactly the right gate rejections (with exact reason codes) and exactly the right sizer clip -- D-07/RISK-04's literal exit criterion is met by a real, committed, passing test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests/test_poisoned_list.py | Fully offline test, exercises gate.py + sizer.py in-process; no network, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Repudiation | poisoned-list test coverage | mitigate | Every one of the four gate reason codes and the sizer's memecoin clip is asserted individually and by name -- a regression that silently changes a reason code or clip amount fails this test immediately |
| T-04-13 | Tampering | fabricated correlation fixture | mitigate | The test self-verifies the fabricated CORRA/CORRB pair's actual correlation exceeds the 0.8 threshold before asserting gate behavior, so the fixture cannot silently drift below the threshold and pass for the wrong reason |
| T-04-SC | Tampering | pip installs | accept | No new dependency in this plan |
</threat_model>

<verification>
`python -m pytest tests/test_poisoned_list.py -q` passes; `python -m pytest tests/test_risk_gate.py tests/test_position_sizer.py tests/test_breakers.py tests/test_poisoned_list.py -q` (full Phase 4 suite) passes; `python -m pytest -q` (full repo suite) stays green -- Phase 4 exit gate.
</verification>

<success_criteria>
All three Phase 4 success criteria are met: (1) the poisoned list produces exactly the right deletions/clip with correct reason codes; (2) breakers fire correctly in simulation (Plan 04-04); (3) unit + property tests pass on the gate, sizer, and breakers at the project's highest test bar.
</success_criteria>

<output>
Create `.planning/phases/04-risk-gate-sizer/04-05-SUMMARY.md` when done.
</output>
