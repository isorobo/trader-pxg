---
phase: 04-risk-gate-sizer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - trader/risk/__init__.py
  - trader/risk/config.py
  - migrations/0004_risk_breakers.sql
  - tests/test_risk_config.py
  - tests/test_risk_migration.py
autonomous: true
requirements: [RISK-01, RISK-02, RISK-03]

must_haves:
  truths:
    - "Every numeric threshold used anywhere in trader/risk traces back to a single named constant in trader/risk/config.py -- no inline magic numbers in downstream plans"
    - "migrations/0004_risk_breakers.sql is applied automatically by trader.data.db.get_connection and brings schema_version to 4"
    - "breaker_state_current always resolves to the latest event per breaker_type, so state can be re-derived instead of trusted from a cached column (standing rule 4)"
  artifacts:
    - path: "trader/risk/config.py"
      provides: "Frozen threshold constants for the gate, sizer, and breakers (RISK-01/02/03 pre-registered defaults)"
      exports: ["MIN_DOLLAR_VOLUME_STOCK", "MIN_QUOTE_VOLUME_CRYPTO_MAJOR", "MIN_QUOTE_VOLUME_MEMECOIN", "MIN_LISTING_AGE_DAYS", "MAX_SPREAD_PCT", "CORRELATION_WINDOW_DAYS", "CORRELATION_THRESHOLD", "CORRELATION_MIN_OVERLAP", "SIZER_TOP_N", "SIZER_VOLATILITY_WINDOW_DAYS", "SIZER_SINGLE_POSITION_CAP", "SIZER_MEMECOIN_CAP", "SIZER_CASH_RESERVE", "BREAKER_DAILY_LOSS_PCT", "BREAKER_DRAWDOWN_PCT", "BREAKER_CONSECUTIVE_LOSSES"]
      min_lines: 30
    - path: "migrations/0004_risk_breakers.sql"
      provides: "breaker_events append-only table + breaker_state_current view"
      contains: "breaker_events"
    - path: "tests/test_risk_config.py"
      provides: "Constant-value regression tests mirroring tests/test_backtest_config.py's pattern"
    - path: "tests/test_risk_migration.py"
      provides: "Schema/CHECK-constraint tests mirroring tests/test_backtest_migration.py's pattern"
  key_links:
    - from: "trader/risk/config.py"
      to: "trader/backtest/config.py"
      via: "MAX_SPREAD_PCT = dict(SLIPPAGE_PCT) -- reuses the existing slippage table as the spread-ceiling proxy, no new invented numbers (orchestrator decision)"
      pattern: "SLIPPAGE_PCT"
    - from: "migrations/0004_risk_breakers.sql"
      to: "trader/data/db.py apply_migrations"
      via: "4-digit filename version prefix, picked up automatically on the next get_connection() call"
      pattern: "0004_"
---

<objective>
Establish Phase 4's two foundational contracts before any gate/sizer/breaker logic is written: `trader/risk/config.py` (every pinned numeric default from 04-RESEARCH.md, named, frozen-style) and `migrations/0004_risk_breakers.sql` (the append-only breaker event log + current-state view that Plan 04-04 persists to). This is the phase's Wave 0/interface-first step -- Plans 04-02/03/04 import these contracts directly rather than re-deriving thresholds or DDL.

Purpose: Every downstream plan (gate, sizer, breakers) reads its thresholds from one place, and the breaker schema exists and is tested before any breaker logic is written against it.
Output: `trader/risk/config.py`, `migrations/0004_risk_breakers.sql`, `tests/test_risk_config.py`, `tests/test_risk_migration.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-risk-gate-sizer/04-CONTEXT.md
@.planning/phases/04-risk-gate-sizer/04-RESEARCH.md

<interfaces>
From trader/backtest/config.py (existing, consume only):
  SLIPPAGE_PCT: dict = {"stock": 0.05, "crypto_major": 0.10, "memecoin": 4.0}   # percentage units

From trader/data/db.py (existing, consume only):
  apply_migrations(conn, migrations_dir="migrations") -> None   # applies any *.sql file whose name starts with an unapplied 4-digit version; 0004 is the next version after 0003_backtest.sql
  get_connection(db_path) -> sqlite3.Connection   # calls apply_migrations automatically

From migrations/0002_instruments_bars.sql and migrations/0003_backtest.sql (existing, style reference):
  CHECK constraints inline on TEXT columns (e.g. asset_class TEXT NOT NULL CHECK (asset_class IN (...)))
  CREATE INDEX IF NOT EXISTS ... following table creation
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: trader/risk/config.py -- frozen threshold constants</name>
  <files>trader/risk/__init__.py, trader/risk/config.py, tests/test_risk_config.py</files>
  <read_first>
    trader/backtest/config.py (module docstring style, SLIPPAGE_PCT dict-of-constants convention to mirror)
    tests/test_backtest_config.py (existing constant-equality test pattern to mirror exactly)
    .planning/phases/04-risk-gate-sizer/04-RESEARCH.md Q1 (liquidity floors + median-not-mean rationale), Q2 (correlation window/threshold/overlap), Q3 (sizer caps + volatility window), Q4 (breaker defaults), Open Question 3 (spread proxy = SLIPPAGE_PCT reuse)
  </read_first>
  <acceptance_criteria>
    - `python -m pytest tests/test_risk_config.py -q -x` passes
    - `trader.risk.config.MAX_SPREAD_PCT` equals `{"stock": 0.05, "crypto_major": 0.10, "memecoin": 4.0}` exactly (reused from `trader.backtest.config.SLIPPAGE_PCT`, not re-typed as new literals)
    - Every constant listed in this plan's frontmatter `artifacts.exports` is importable from `trader.risk.config`
  </acceptance_criteria>
  <action>
    Create `trader/risk/__init__.py` as an empty package marker with a one-line docstring ("Phase 4's risk gate, sizer, and circuit-breaker package."). Create `trader/risk/config.py` with a module docstring stating: these are pre-registered defaults per 04-RESEARCH.md; Phases 5-6 may recalibrate them only via a pre-registered change, never mid-trade (per CONTEXT.md's standing rule); and the candidate-dict contract seam -- every candidate dict flowing into this phase's gate/sizer requires a `score: float` field, owned by Phase 4 (source-agnostic per D-06), with who computes that score in the live pipeline left as Phase 5's decision. Define exactly these named module-level constants with these pinned values: `LIQUIDITY_WINDOW_STOCK_DAYS = 20`, `LIQUIDITY_WINDOW_CRYPTO_DAYS = 7`, `MIN_DOLLAR_VOLUME_STOCK = 1_000_000.0`, `MIN_QUOTE_VOLUME_CRYPTO_MAJOR = 5_000_000.0`, `MIN_QUOTE_VOLUME_MEMECOIN = 250_000.0`, `MIN_LISTING_AGE_DAYS = 30`, `MAX_SPREAD_PCT = dict(SLIPPAGE_PCT)` (import `SLIPPAGE_PCT` from `trader.backtest.config` -- do not retype its values), `CORRELATION_WINDOW_DAYS = 60`, `CORRELATION_MIN_OVERLAP = 30`, `CORRELATION_THRESHOLD = 0.8`, `SIZER_TOP_N = 3`, `SIZER_VOLATILITY_WINDOW_DAYS = 20`, `SIZER_SINGLE_POSITION_CAP = 0.50`, `SIZER_MEMECOIN_CAP = 0.10`, `SIZER_CASH_RESERVE = 0.10`, `BREAKER_DAILY_LOSS_PCT = -0.03`, `BREAKER_DRAWDOWN_PCT = -0.10`, `BREAKER_CONSECUTIVE_LOSSES = 6`. Create `tests/test_risk_config.py` mirroring `tests/test_backtest_config.py`'s exact-equality style: one test per constant (or grouped logically), including an explicit test that `config.MAX_SPREAD_PCT == config.SLIPPAGE_PCT` is NOT required (no such name exists in trader.risk.config) but that `config.MAX_SPREAD_PCT` matches `trader.backtest.config.SLIPPAGE_PCT`'s three entries value-for-value.
  </action>
  <verify>
    <automated>python -m pytest tests/test_risk_config.py -q -x</automated>
  </verify>
  <done>trader/risk/config.py is importable with every pinned constant present at its exact researched value; tests/test_risk_config.py is green.</done>
</task>

<task type="auto">
  <name>Task 2: migrations/0004_risk_breakers.sql -- append-only event log + current-state view</name>
  <files>migrations/0004_risk_breakers.sql, tests/test_risk_migration.py</files>
  <read_first>
    migrations/0002_instruments_bars.sql and migrations/0003_backtest.sql (header-comment style, CHECK-constraint convention, index-naming convention to mirror exactly)
    trader/data/db.py apply_migrations (4-digit filename version parsing, schema_version bookkeeping)
    tests/test_backtest_migration.py (local data_conn fixture pattern -- defined per-file, tests/conftest.py is never modified)
    .planning/phases/04-risk-gate-sizer/04-RESEARCH.md Q4 (exact DDL recommendation: breaker_events columns/CHECKs, breaker_state_current view logic)
  </read_first>
  <acceptance_criteria>
    - `python -m pytest tests/test_risk_migration.py -q -x` passes
    - A fresh DB's `schema_version` table reaches exactly `MAX(version) == 4` after `db.get_connection()`
    - Inserting a `breaker_events` row with an out-of-enum `breaker_type`, `action`, or `actor` raises `sqlite3.IntegrityError` (three separate CHECK constraints)
    - `breaker_state_current` returns exactly one row per `breaker_type` that has at least one event, showing that type's most recent event (highest `event_id`)
  </acceptance_criteria>
  <action>
    Create `migrations/0004_risk_breakers.sql`, header-commented in 0002/0003's style ("-- Phase 4's new table: breaker_events (see 04-CONTEXT.md D-04/D-05, 04-RESEARCH.md Q4)..."). Define `breaker_events` with columns `event_id INTEGER PRIMARY KEY AUTOINCREMENT`, `ts TEXT NOT NULL DEFAULT (datetime('now'))`, `breaker_type TEXT NOT NULL CHECK (breaker_type IN ('daily_loss', 'drawdown', 'consecutive_loss'))`, `action TEXT NOT NULL CHECK (action IN ('trip', 'reset', 'manual_restart'))`, `trigger_value REAL`, `reason TEXT`, `actor TEXT NOT NULL DEFAULT 'system' CHECK (actor IN ('system', 'human'))`. Add `CREATE INDEX IF NOT EXISTS idx_breaker_events_type ON breaker_events(breaker_type)`. Add the view `breaker_state_current` selecting `breaker_type, action AS current_action, ts AS since, reason` from `breaker_events`, inner-joined against a per-`breaker_type` `MAX(event_id)` subquery, so the view always resolves to each type's single latest row -- exactly the DDL in 04-RESEARCH.md Q4. Create `tests/test_risk_migration.py` mirroring `tests/test_backtest_migration.py`'s locally-defined `data_conn` fixture (built on the root `tests/conftest.py`'s `tmp_db_path`, never modifying `conftest.py` itself). Assert schema_version reaches 4, both `breaker_events` and `breaker_state_current` exist in `sqlite_master`, all three CHECK constraints reject bad values via `pytest.raises(sqlite3.IntegrityError)`, and a hand-inserted trip-then-reset pair for one `breaker_type` surfaces only the latest row through `breaker_state_current`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_risk_migration.py -q -x</automated>
  </verify>
  <done>Migration applies cleanly on top of 0001-0003, schema_version reaches 4, all CHECK constraints and the current-state view behave exactly as asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.py / migration file | Fully offline; no user input, no network; DDL and constants only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | future `breaker_events` writers (Plan 04-04) | mitigate | This migration's schema is the only writable surface; Plan 04-04's persistence layer must use parameterized `?` placeholders exactly like `trader/data/db.py`'s existing writers -- never string-interpolated SQL (ASVS V5) |
| T-04-02 | Tampering | numeric thresholds in config.py | accept | Pre-registered defaults per 04-RESEARCH.md; CONTEXT.md restricts recalibration to a pre-registered Phase 6 change -- enforced by process/review at this stage, not runtime code |
| T-04-SC | Tampering | pip installs | accept | This plan introduces zero new dependencies; `hypothesis` is scoped to Plan 04-03 only, already audited (Approved) in 04-RESEARCH.md's Package Legitimacy Audit |
</threat_model>

<verification>
`python -m pytest tests/test_risk_config.py tests/test_risk_migration.py -q` passes.
</verification>

<success_criteria>
`trader/risk/config.py` and `migrations/0004_risk_breakers.sql` exist as committed, tested contracts. Plans 04-02, 04-03, and 04-04 import these directly, with zero re-derived thresholds or duplicated DDL.
</success_criteria>

<output>
Create `.planning/phases/04-risk-gate-sizer/04-01-SUMMARY.md` when done.
</output>
