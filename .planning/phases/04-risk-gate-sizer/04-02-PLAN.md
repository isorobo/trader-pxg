---
phase: 04-risk-gate-sizer
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - trader/risk/gate.py
  - tests/test_risk_gate.py
autonomous: true
requirements: [RISK-01]

must_haves:
  truths:
    - "apply_risk_gate never touches the network or a live DB connection -- it is a pure function over candidates plus a prebuilt market_data dict"
    - "Every rejected candidate carries exactly one of the four reason codes: REJECT_LIQUIDITY, REJECT_LISTING_AGE, REJECT_SPREAD, REJECT_CORRELATION"
    - "A fabricated 3-way fully-connected correlated cluster resolves down to exactly its single highest-scored member via the greedy sequential elimination in 04-RESEARCH.md Q2"
    - "Every accepted candidate is tagged with a resolved asset_class and exit_profile_tag -- never left unclassified"
  artifacts:
    - path: "trader/risk/gate.py"
      provides: "RISK-01's apply_risk_gate pure function, reason-code constants, correlation cluster resolution"
      exports: ["apply_risk_gate", "REJECT_LIQUIDITY", "REJECT_LISTING_AGE", "REJECT_SPREAD", "REJECT_CORRELATION"]
      min_lines: 60
    - path: "tests/test_risk_gate.py"
      provides: "Unit coverage of every RISK-01 check plus the N-way correlation cluster and date-alignment regression"
  key_links:
    - from: "trader/risk/gate.py"
      to: "trader/risk/config.py"
      via: "imports every threshold constant -- zero inline magic numbers"
      pattern: "from trader\\.risk import config|from trader\\.risk\\.config import"
    - from: "trader/risk/gate.py market_data contract"
      to: "trader/data/db.py read_bars_cache"
      via: "bars entries reuse the exact ts/open/high/low/close/volume dict shape read_bars_cache returns, so a caller can pass its output straight through"
      pattern: "\\[\"close\"\\]|\\['close'\\]"
---

<objective>
Implement RISK-01: the risk gate (D-01, D-02) -- a pure function that deletes unsafe candidates (illiquid, too new, too wide a spread, over-correlated with a higher-scored peer) and tags survivors with their asset class and EXIT_PROFILE tag.

Purpose: this is the first of the phase's three safety checks; Plan 04-05's D-07 acceptance test depends directly on this plan's exact reason codes.
Output: `trader/risk/gate.py`, `tests/test_risk_gate.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-risk-gate-sizer/04-CONTEXT.md
@.planning/phases/04-risk-gate-sizer/04-RESEARCH.md
@trader/risk/config.py

<interfaces>
From trader/risk/config.py (Plan 04-01, consume only):
  LIQUIDITY_WINDOW_STOCK_DAYS=20, LIQUIDITY_WINDOW_CRYPTO_DAYS=7
  MIN_DOLLAR_VOLUME_STOCK=1_000_000.0, MIN_QUOTE_VOLUME_CRYPTO_MAJOR=5_000_000.0, MIN_QUOTE_VOLUME_MEMECOIN=250_000.0
  MIN_LISTING_AGE_DAYS=30
  MAX_SPREAD_PCT: dict[str, float]  # {"stock": 0.05, "crypto_major": 0.10, "memecoin": 4.0}
  CORRELATION_WINDOW_DAYS=60, CORRELATION_MIN_OVERLAP=30, CORRELATION_THRESHOLD=0.8

From trader/data/db.py read_bars_cache (existing, shape reference only -- gate.py does NOT call this directly):
  returns list[dict] with keys ts (str, ISO date, ascending), open, high, low, close, volume (all float)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Candidate/market_data contract + liquidity, listing-age, spread checks</name>
  <files>trader/risk/gate.py, tests/test_risk_gate.py</files>
  <behavior>
    - Liquidity: a stock candidate whose trailing-20-day median dollar volume is below MIN_DOLLAR_VOLUME_STOCK is rejected with REJECT_LIQUIDITY; a memecoin below MIN_QUOTE_VOLUME_MEMECOIN (7-day window) and a crypto_major below MIN_QUOTE_VOLUME_CRYPTO_MAJOR (7-day window) are rejected the same way.
    - Listing age: a candidate with fewer than MIN_LISTING_AGE_DAYS bars is rejected with REJECT_LISTING_AGE.
    - Spread: a candidate whose market_data spread_pct exceeds config.MAX_SPREAD_PCT[asset_class] is rejected with REJECT_SPREAD.
    - A candidate passing all three individual checks is accepted and tagged with asset_class and exit_profile_tag (both equal to market_data's asset_class value).
    - Check order is liquidity -> listing_age -> spread: a candidate failing more than one reports only the FIRST failing reason code.
  </behavior>
  <read_first>
    trader/risk/config.py (this plan's dependency -- every threshold name and value)
    trader/data/db.py read_bars_cache docstring (bars dict shape: ts/open/high/low/close/volume)
    .planning/phases/04-risk-gate-sizer/04-RESEARCH.md Q1 (median-not-mean rationale, dollar_volume = close * volume per bar), Pattern 1 (reason-coded rejection shape), Pitfall 4 (score:float is Phase 4's own required field; no import from trader.backtest.strategies)
  </read_first>
  <acceptance_criteria>
    - `python -m pytest tests/test_risk_gate.py -k "not correlation" -q -x` passes
    - `trader/risk/gate.py` contains zero inline numeric thresholds -- every comparison references a `trader.risk.config` constant
    - `trader/risk/gate.py` has no import of anything under `trader.backtest.strategies` (D-06: no coupling to specific strategies)
  </acceptance_criteria>
  <action>
    Create `trader/risk/gate.py`'s module docstring documenting the candidate-dict contract (`list[dict]`, each requiring `symbol: str`, `venue: str`, `score: float` -- score is Phase 4's own required field, populated by whatever upstream caller invokes the gate, decoupled from Phase 3's `pick_entries` output per D-06/04-RESEARCH.md Pitfall 4/Assumption A9) and the `market_data` contract (`dict` keyed by `(symbol, venue)` tuple, each value `{"bars": list[dict]` with `ts/open/high/low/close/volume` ascending -- the exact shape `trader.data.db.read_bars_cache` returns, `"asset_class": str` already override-resolved by the caller, `"spread_pct": float`, a static per-candidate spread estimate in the same percentage units as `trader.backtest.config.SLIPPAGE_PCT}`). Define reason-code constants `REJECT_LIQUIDITY = "REJECT_LIQUIDITY"`, `REJECT_LISTING_AGE = "REJECT_LISTING_AGE"`, `REJECT_SPREAD = "REJECT_SPREAD"`, `REJECT_CORRELATION = "REJECT_CORRELATION"`. Implement `_trailing_median_volume(bars, asset_class)` using pandas: build a Series of `close * volume` per bar, take `.tail(window).median()` where window is `config.LIQUIDITY_WINDOW_STOCK_DAYS` for `asset_class == "stock"` else `config.LIQUIDITY_WINDOW_CRYPTO_DAYS`, compared against `config.MIN_DOLLAR_VOLUME_STOCK` / `MIN_QUOTE_VOLUME_CRYPTO_MAJOR` / `MIN_QUOTE_VOLUME_MEMECOIN` keyed by `asset_class`. Implement `_first_failing_check(entry, config)` checking, in this fixed order, liquidity (via `_trailing_median_volume`), listing age (`len(entry["bars"]) < config.MIN_LISTING_AGE_DAYS`), then spread (`entry["spread_pct"] > config.MAX_SPREAD_PCT[entry["asset_class"]]`) -- returning the first matching reason code or `None`. Implement `apply_risk_gate(candidates, market_data, config=risk_config)` that runs `_first_failing_check` per candidate (looked up in `market_data` by `(candidate["symbol"], candidate["venue"])`), splitting into an `accepted` list (each candidate dict spread with `{"asset_class": entry["asset_class"], "exit_profile_tag": entry["asset_class"]}`) and a `rejected` list (each candidate dict spread with `{"reason_code": reason}`). For this task, return `(accepted, rejected)` directly without running the correlation pass yet -- Task 2 wires that in.
  </action>
  <verify>
    <automated>python -m pytest tests/test_risk_gate.py -k "not correlation" -q -x</automated>
  </verify>
  <done>All individual-check tests pass; the contract is documented and importable; no inline magic numbers; no strategies-module import.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Correlation check (greedy elimination) + full apply_risk_gate wiring</name>
  <files>trader/risk/gate.py, tests/test_risk_gate.py</files>
  <behavior>
    - Two surviving candidates whose inner-joined-by-date daily-return correlation exceeds config.CORRELATION_THRESHOLD (0.8): the lower-scored one is rejected with REJECT_CORRELATION, the higher-scored one remains accepted.
    - A fabricated 3-way fully-connected correlated cluster resolves to exactly one survivor -- its single highest-scored member -- via the greedy sequential elimination (sort pairs above threshold descending, walk once, skip already-resolved pairs).
    - An overlap below config.CORRELATION_MIN_OVERLAP (30) between two candidates' date-aligned bars is treated as indeterminate -- neither is rejected on that pair.
    - Correlation is computed on inner-joined calendar dates, not positional index -- a stock/crypto pair whose calendars differ (crypto includes weekend dates the stock lacks) still aligns correctly (Pitfall 1 regression).
  </behavior>
  <read_first>
    .planning/phases/04-risk-gate-sizer/04-RESEARCH.md Q2 in full (algorithm, worked cluster example, pairwise_correlation code sample) and Pitfall 1 (positional-vs-date-aligned correlation)
    trader/risk/config.py's CORRELATION_* constants
  </read_first>
  <acceptance_criteria>
    - `python -m pytest tests/test_risk_gate.py -q -x` passes in full (liquidity/age/spread/correlation together)
    - `apply_risk_gate(candidates, market_data, config)` returns exactly `(accepted, rejected)` with correlation checked on every remaining survivor, not bypassed
    - The 3-way cluster test asserts exactly 1 survivor and 2 rejections, both carrying `REJECT_CORRELATION`
  </acceptance_criteria>
  <action>
    Implement `_pairwise_correlation(bars_a, bars_b, min_overlap=config.CORRELATION_MIN_OVERLAP)`: build two pandas Series indexed by each bar's `ts` (the calendar-date key, never a positional index), restricted to the trailing `config.CORRELATION_WINDOW_DAYS` calendar days of bars, compute simple returns via `.pct_change()`, inner-join the two return Series on their shared index (`pd.concat([returns_a, returns_b], axis=1, join="inner")`), return `None` if the joined length is below `min_overlap`, else the Pearson correlation via `.corr()`. Implement `_apply_correlation_check(accepted, market_data, config)`: compute all pairwise correlations among the still-accepted set, keep only pairs where the correlation is not `None` and exceeds `config.CORRELATION_THRESHOLD`, sort those pairs by correlation magnitude descending, walk the sorted list once -- for each pair, if both members are still in the surviving set, reject the lower-scored member (append `{"reason_code": REJECT_CORRELATION}`, remove from survivors); skip a pair if either member was already removed by an earlier pair in the same walk. This is exactly the algorithm in 04-RESEARCH.md Q2. Wire this into `apply_risk_gate`: after Task 1's individual per-candidate checks, run `_apply_correlation_check` over the survivors, merge its rejections into the final `rejected` list, and return the final `(accepted, rejected)`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_risk_gate.py -q -x</automated>
  </verify>
  <done>Full gate test file green; the 3-way cluster resolves to exactly one survivor; the date-alignment regression test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate.py inputs | Fully in-process; candidates/market_data are prebuilt by the caller (Phase 5, out of scope here); no network, no user input at this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | candidate/market_data inputs | accept | gate.py is a pure in-process function with no network/user input at this phase -- the caller owns input validation before invoking it |
| T-04-04 | Information Disclosure | candidate/market_data fields | accept | No secrets or PII flow through candidates or market_data |
| T-04-05 | Repudiation | rejected candidates | mitigate | Every rejection carries a machine-readable reason_code (D-01); tests/test_risk_gate.py asserts the exact code per rejection path |
| T-04-SC | Tampering | pip installs | accept | No new dependency introduced in this plan |
</threat_model>

<verification>
`python -m pytest tests/test_risk_gate.py -q` passes; `python -m pytest -q` (full suite) stays green.
</verification>

<success_criteria>
RISK-01 is fully implemented and tested: liquidity, listing-age, spread, and correlation checks all enforce 04-RESEARCH.md's pinned defaults; reason codes are exact; every accepted candidate is tagged with asset_class and exit_profile_tag.
</success_criteria>

<output>
Create `.planning/phases/04-risk-gate-sizer/04-02-SUMMARY.md` when done.
</output>
