---
phase: 00-ground-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - .env.example
  - .gitignore
  - pyproject.toml
  - trader/__init__.py
  - trader/ground_truth/__init__.py
autonomous: false
requirements:
  - DATA-01
user_setup:
  - service: coingecko
    why: "Demo API key needed to authenticate CoinGecko /coins/markets calls (D-03)"
    env_vars:
      - name: COINGECKO_API_KEY
        source: "CoinGecko Developer Dashboard -> https://www.coingecko.com/en/developers/dashboard -> Create Demo API key"
    dashboard_config: []

must_haves:
  truths:
    - "Project dependencies are installed in an isolated virtual environment, not the system Python"
    - "A CoinGecko demo API key is available in .env for the poller and report scripts to use"
    - "Secrets never reach git; the repo layout matches the Phase 1 src-layout convention"
  artifacts:
    - path: "requirements.txt"
      provides: "pinned dependency list matching 00-RESEARCH.md Standard Stack"
      contains: "yfinance==1.5.2"
    - path: ".env.example"
      provides: "documents the COINGECKO_API_KEY variable name without a real value"
      contains: "COINGECKO_API_KEY"
    - path: "pyproject.toml"
      provides: "pytest test discovery + ruff config"
      contains: "testpaths"
    - path: "trader/ground_truth/__init__.py"
      provides: "package marker for the Phase 0 module, src layout per Phase 1 D-04"
  key_links: []
---

<objective>
Stand up the repo skeleton, pinned dependencies, and secrets scaffolding that every later Phase 0 plan depends on.

Purpose: Phase 0 is greenfield — no code, no venv, no `.env` exists yet. Every downstream plan (source adapters, poller, report) needs a working Python environment and a place to put the CoinGecko demo key. This plan makes that foundation exist before any test or feature code is written.
Output: `trader/` package skeleton, pinned `requirements.txt`, `pyproject.toml` (pytest + ruff config), `.gitignore`, `.env.example`, a working `.venv/` with all six dependencies installed, and a real `.env` with a working `COINGECKO_API_KEY`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\PROJECT.md
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\ROADMAP.md
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\STATE.md
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\phases\00-ground-truth\00-CONTEXT.md
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\phases\00-ground-truth\00-RESEARCH.md
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\phases\01-accounts-data-plumbing\01-CONTEXT.md
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package skeleton, pinned dependency list, and config files</name>
  <files>requirements.txt, .env.example, .gitignore, pyproject.toml, trader/__init__.py, trader/ground_truth/__init__.py</files>
  <read_first>
    - C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\phases\00-ground-truth\00-RESEARCH.md (Standard Stack table for exact pinned versions; Recommended Project Structure diagram; Validation Architecture Test Framework table)
    - C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\phases\01-accounts-data-plumbing\01-CONTEXT.md (D-04 `trader/` src layout, D-05 venv+pip+requirements.txt, D-06 pytest/ruff/python-dotenv, D-07 `data/trader.db` gitignored)
  </read_first>
  <action>
    Create `trader/__init__.py` and `trader/ground_truth/__init__.py` as empty package markers (src layout per Phase 1 D-04). Create `requirements.txt` pinned to exactly these six lines: `yfinance==1.5.2`, `requests==2.34.2`, `python-dotenv==1.2.2`, `finviz==2.0.0`, `pytest==9.1.1`, `ruff==0.16.0` (versions verified against PyPI in 00-RESEARCH.md Standard Stack, 26 July 2026). Create `pyproject.toml` with a `[tool.pytest.ini_options]` section setting `testpaths = ["tests"]`, and a `[tool.ruff]` section setting `line-length = 100`. Create `.gitignore` with the literal entries `.venv/`, `__pycache__/`, `*.pyc`, `.env`, `data/` — do NOT add a `reports/` entry (D-12 discretion: dated markdown reports stay committed as durable evidence, matching the project's "never lose collected history" principle). Create `.env.example` containing exactly one line: `COINGECKO_API_KEY=`. Create the directories `data/`, `reports/`, `tests/`, `scripts/` (empty placeholders acceptable; `data/` will hold the gitignored `trader.db` created by a later plan).
  </action>
  <acceptance_criteria>
    - `requirements.txt` contains exactly six lines, one version-pinned entry each for yfinance, requests, python-dotenv, finviz, pytest, ruff
    - `.gitignore` contains the literal lines `.env` and `data/`, and does NOT contain a line `reports/`
    - `pyproject.toml` contains `testpaths = ["tests"]` under `[tool.pytest.ini_options]`
    - `.env.example` contains the literal line `COINGECKO_API_KEY=`
    - `trader/__init__.py` and `trader/ground_truth/__init__.py` both exist
  </acceptance_criteria>
  <verify>
    <automated>python -c "import pathlib; assert pathlib.Path('trader/ground_truth/__init__.py').exists(); assert 'yfinance==1.5.2' in pathlib.Path('requirements.txt').read_text()"</automated>
  </verify>
  <done>Package skeleton, pinned requirements.txt, pyproject.toml, .env.example, and .gitignore exist and match the acceptance criteria above.</done>
</task>

<task type="auto">
  <name>Task 2: Create the virtual environment and install pinned dependencies</name>
  <files>.venv/ (created locally, excluded from git via .gitignore)</files>
  <read_first>
    - requirements.txt (created in Task 1)
    - C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\phases\00-ground-truth\00-RESEARCH.md (Package Legitimacy Audit table — all six packages disposition "Approved")
  </read_first>
  <action>
    Run `python -m venv .venv` at the project root. Install with `.venv\Scripts\python.exe -m pip install -r requirements.txt`. Do not install any package not listed in requirements.txt. No legitimacy checkpoint is required for this install: all six packages carry disposition "Approved" in 00-RESEARCH.md's Package Legitimacy Audit (slopcheck [OK] on every entry, none `[ASSUMED]`/`[SUS]`/`[SLOP]`).
  </action>
  <acceptance_criteria>
    - `.venv\Scripts\python.exe -c "import yfinance, requests, dotenv, finviz, pytest"` exits 0 with no ImportError
    - `.venv\Scripts\python.exe -m pip show yfinance` reports `Version: 1.5.2`
  </acceptance_criteria>
  <verify>
    <automated>.venv\Scripts\python.exe -c "import yfinance, requests, dotenv, finviz, pytest; print('ok')"</automated>
  </verify>
  <done>Virtual environment exists at .venv/ with all six pinned packages importable.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 3: Obtain a CoinGecko Demo API key and store it in .env</name>
  <read_first>
    - .env.example (created in Task 1 — documents the expected variable name)
    - C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\phases\00-ground-truth\00-CONTEXT.md (D-03: CoinGecko demo API key requirement)
  </read_first>
  <what-built>.env.example documents COINGECKO_API_KEY as the one secret this phase needs. No code can obtain this key on your behalf — CoinGecko requires a human-created account.</what-built>
  <how-to-verify>
    1. Visit https://www.coingecko.com/en/developers/dashboard and create a free account if you do not already have one.
    2. Create a new Demo API key.
    3. Copy `.env.example` to `.env` at the project root (`.env` is already gitignored per Task 1).
    4. Add the line `COINGECKO_API_KEY=<your-demo-key>` to `.env`.
    5. (Optional, informational) While in the dashboard, note your plan's published rate limit (00-RESEARCH.md Open Question 2 cites 30-100 calls/min depending on source). Phase 0 makes at most 1 `/coins/markets` call per 15 minutes plus at most ~100 `/coins/{id}/history` calls/day from the report script — far below either figure — so no throttling logic is added; this step is confirmation only, not a blocker.
  </how-to-verify>
  <acceptance_criteria>
    - `.env` exists at the project root and is excluded from git (`git check-ignore .env` exits 0)
    - `.env` contains a line starting with `COINGECKO_API_KEY=` followed by a non-empty value
  </acceptance_criteria>
  <resume-signal>Type "key added" once .env contains a working COINGECKO_API_KEY value.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI registry -> local install | Third-party package code enters the environment via `pip install -r requirements.txt` |
| Human -> `.env` | Secret material entered by a human must never leave the machine via git |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | Tampering | `pip install -r requirements.txt` | mitigate | All six packages are disposition "Approved" in 00-RESEARCH.md's Package Legitimacy Audit (slopcheck [OK], multi-year PyPI history, high download counts); none are `[ASSUMED]`/`[SUS]`/`[SLOP]`, so no blocking human-verify install checkpoint is required |
| T-00-02 | Information Disclosure | `.env` / `COINGECKO_API_KEY` | mitigate | `.gitignore` excludes `.env`; `.env.example` documents only the variable name, never a real value |
</threat_model>

<verification>
Run `.venv\Scripts\python.exe -c "import yfinance, requests, dotenv, finviz, pytest"` — must exit 0.
Run `git check-ignore .env data/` — both must be reported as ignored.
Confirm `.env` contains a non-empty `COINGECKO_API_KEY` value (do not print the value to logs).
</verification>

<success_criteria>
- Package skeleton, pinned requirements.txt, pyproject.toml, .gitignore, and .env.example exist
- .venv/ exists with all six pinned dependencies installed and importable
- .env exists (gitignored) with a working COINGECKO_API_KEY
</success_criteria>

<output>
Create `.planning/phases/00-ground-truth/00-01-SUMMARY.md` when done
</output>
