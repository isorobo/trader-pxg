---
phase: 00-ground-truth
plan: 05
type: execute
wave: 4
depends_on:
  - "00-03"
  - "00-04"
files_modified: []
autonomous: false
requirements:
  - DATA-04

must_haves:
  truths:
    - "A live poll --once run inserts real rows into data/trader.db (both stock and crypto)"
    - "report.py runs against real collected data without crashing and produces a non-empty report"
    - "The Task Scheduler task is registered, enabled, and firing every 15 minutes"
  artifacts:
    - path: "data/trader.db"
      provides: "first real snapshots + poll_runs rows from a live poll"
    - path: "reports/{today}.md"
      provides: "first real daily report output, non-zero ticker count"
  key_links:
    - from: "schtasks /query"
      to: "TraderGroundTruthPoll task"
      via: "Task Scheduler registration confirmed enabled"
      pattern: "TraderGroundTruthPoll"
---

<objective>
Prove the whole pipeline works end-to-end against real data, then hand off to the two-week unattended clock that satisfies DATA-04.

Purpose: Every prior plan built and unit-tested individual pieces with mocks. This plan is the first live, real run of the full chain — poll writes real rows, report reads them back and produces a real (non-fixture) answer — plus a human checkpoint confirming the Task Scheduler task is live before walking away for two weeks. The two-week clock itself is runtime, not plan work; this plan's job is to prove the setup is correct and durable before that clock starts counting.
Output: Real rows in `data/trader.db`, a real dated report in `reports/`, and a confirmed-enabled Task Scheduler registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\PROJECT.md
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\ROADMAP.md
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\STATE.md
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\phases\00-ground-truth\00-CONTEXT.md
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\phases\00-ground-truth\00-03-SUMMARY.md
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\phases\00-ground-truth\00-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live end-to-end poll run and database verification</name>
  <files>data/trader.db (real rows inserted, not a source file change)</files>
  <read_first>
    - trader/ground_truth/poll.py
    - trader/ground_truth/db.py
  </read_first>
  <action>
    Run `.venv\Scripts\python.exe -m trader.ground_truth.poll --once`. Query `data/trader.db` (e.g. `.venv\Scripts\python.exe -c "import sqlite3; c=sqlite3.connect('data/trader.db'); print(c.execute(\"SELECT source, COUNT(*) FROM snapshots GROUP BY source\").fetchall()); print(c.execute(\"SELECT * FROM poll_runs ORDER BY id DESC LIMIT 1\").fetchone())"`). Confirm at least one row per source and that every crypto row has a non-null `coingecko_id`.
  </action>
  <acceptance_criteria>
    - `poll_runs` has exactly one new row; if either `stock_success` or `crypto_success` is 0, the SUMMARY.md for this plan documents which leg failed and why (D-06 tolerates this — it is not a blocking failure)
    - `snapshots` has at least 1 row where `source='stock'` and at least 1 row where `source='crypto'` with `coingecko_id IS NOT NULL`
  </acceptance_criteria>
  <verify>
    <automated>.venv\Scripts\python.exe -c "import sqlite3; c=sqlite3.connect('data/trader.db'); print(c.execute(\"SELECT source, COUNT(*) FROM snapshots GROUP BY source\").fetchall())"</automated>
  </verify>
  <done>A live poll has written real rows to both snapshots and poll_runs in data/trader.db.</done>
</task>

<task type="auto">
  <name>Task 2: Live report run against just-collected data</name>
  <files>reports/{today}.md</files>
  <read_first>
    - trader/ground_truth/report.py
  </read_first>
  <action>
    Run `.venv\Scripts\python.exe -m trader.ground_truth.report`. Confirm it reads the rows written in Task 1, computes a same-day close for at least the tickers just captured (next-day close correctly reports `None` since today has not ended), and writes `reports/{today}.md` with a non-zero ticker count and a coverage stat reflecting the single poll just run.
  </action>
  <acceptance_criteria>
    - `reports/{today}.md` exists and contains at least one ticker row (not the zero-state message from Plan 00-04 Task 3)
    - The stdout summary printed by the command includes the word "Coverage"
  </acceptance_criteria>
  <verify>
    <automated>.venv\Scripts\python.exe -m trader.ground_truth.report</automated>
  </verify>
  <done>report.py produces a real, non-empty report from the first live poll.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm Task Scheduler is live and acknowledge the two-week monitoring window</name>
  <read_first>
    - scripts/poll_task.xml (Plan 00-03 — the registered task definition)
  </read_first>
  <what-built>A registered Windows Task Scheduler task ("TraderGroundTruthPoll") firing scripts/poll.bat every 15 minutes with StartWhenAvailable enabled, a snapshots/poll_runs database seeded with the first real poll's data, and a report script that produces a dated markdown report with a coverage stat.</what-built>
  <how-to-verify>
    1. Run `schtasks /query /tn "TraderGroundTruthPoll" /v /fo list` and confirm `Scheduled Task State: Enabled` and a 15-minute repeat interval.
    2. Leave the laptop on, or at least awake periodically, over the next two weeks — missed polls are expected and acceptable (D-06); they show up as a lower coverage % in future reports, not as a failure.
    3. After a few days, run `.venv\Scripts\python.exe -m trader.ground_truth.report` again and confirm the coverage percentage is non-zero and the ticker list is growing.
    4. After two weeks (DATA-04's minimum), confirm the report's up-vs-dumped summary line answers the phase's exit-criterion question with real numbers, and that the logger is still running (Task Scheduler still Enabled, recent `poll_runs` rows exist).
  </how-to-verify>
  <acceptance_criteria>
    - Task Scheduler task confirmed `Enabled` with a 15-minute repeat interval
    - User has acknowledged the two-week monitoring expectation and the plan for checking back on coverage/report results
  </acceptance_criteria>
  <resume-signal>Type "approved" once the Task Scheduler status is confirmed and the two-week monitoring plan is acknowledged, or describe any errors seen instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Laptop sleep/wake state -> Task Scheduler reliability | Not a security boundary, but affects data completeness; noted for completeness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-10 | Denial of Service | Task Scheduler execution during laptop sleep | accept | Per D-06, missed polls are expected and tolerated; the daily report's coverage stat makes gaps visible rather than hiding them — no mitigation needed, this is intended behavior |
| T-00-11 | Information Disclosure | `schtasks /query` output shown during the checkpoint | accept | Output is local Task Scheduler metadata only (task name, trigger schedule) — contains no secrets |
</threat_model>

<verification>
Run `schtasks /query /tn "TraderGroundTruthPoll" /v /fo list` — must report Enabled with a 15-minute interval.
Confirm data/trader.db contains real rows from Task 1 and reports/{today}.md contains a non-zero report from Task 2.
</verification>

<success_criteria>
- Live poll and live report both ran successfully against real data
- Task Scheduler task confirmed enabled and firing every 15 minutes
- User has acknowledged the two-week monitoring window that satisfies DATA-04's runtime requirement
</success_criteria>

<output>
Create `.planning/phases/00-ground-truth/00-05-SUMMARY.md` when done
</output>
