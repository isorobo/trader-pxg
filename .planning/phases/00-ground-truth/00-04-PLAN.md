---
phase: 00-ground-truth
plan: 04
type: execute
wave: 3
depends_on:
  - "00-02"
files_modified:
  - trader/ground_truth/report.py
  - tests/test_report.py
autonomous: true
requirements:
  - DATA-03

must_haves:
  truths:
    - "The report shows same-day and next-day close for every ticker flagged that week (D-10)"
    - "The report states what % of flagged tickers ended the day up vs dumped from where the scanner saw them"
    - "The report includes a poll coverage statistic (polls completed vs expected, D-06)"
    - "The report does not crash when zero snapshots exist yet (pre-collection empty state)"
  artifacts:
    - path: "trader/ground_truth/report.py"
      provides: "daily report generator, dated markdown output plus stdout summary (D-12)"
      exports: ["compute_report_rows", "compute_coverage_stat", "write_report_markdown", "format_coingecko_date", "fetch_stock_close", "fetch_crypto_close", "main"]
  key_links:
    - from: "trader/ground_truth/report.py"
      to: "trader/ground_truth/db.py"
      via: "query_flagged_tickers_since + query_poll_run_coverage"
      pattern: "query_flagged_tickers_since"
    - from: "trader/ground_truth/report.py"
      to: "https://api.coingecko.com/api/v3/coins/{id}/history"
      via: "date param formatted DD-MM-YYYY"
      pattern: "format_coingecko_date"
---

<objective>
Build the daily report script that answers the phase's exit-criterion question directly: of everything flagged this week, what % ended the day up vs dumped from where the scanner first saw it?

Purpose: This is DATA-03. The scanner (Plans 00-02/00-03) only captures raw snapshots; this plan is the analysis layer that turns those snapshots into the real numbers the owner asked for, written as a dated markdown file plus a stdout summary.
Output: `trader/ground_truth/report.py` and a passing `tests/test_report.py`, verified against both a fixture dataset and the real (initially empty) database.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\PROJECT.md
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\ROADMAP.md
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\STATE.md
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\phases\00-ground-truth\00-CONTEXT.md
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\phases\00-ground-truth\00-RESEARCH.md
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\phases\00-ground-truth\00-02-SUMMARY.md
</context>

<interfaces>
<!-- Exports from Plan 00-02 that this plan consumes directly. No exploration needed. -->

From `trader/ground_truth/db.py`:
`get_connection(db_path="data/trader.db") -> sqlite3.Connection`; `query_flagged_tickers_since(conn, since_ts: str) -> list[dict]` (rows: source, ticker, coingecko_id, first_seen_ts, first_price, first_pct_gain); `query_poll_run_coverage(conn, since_ts: str) -> dict` (`{"completed": int, "expected": int, "pct": float}`).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for report computation (RED)</name>
  <files>tests/test_report.py</files>
  <read_first>
    - trader/ground_truth/db.py (Plan 00-02 — query_flagged_tickers_since, query_poll_run_coverage contracts)
    - C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\phases\00-ground-truth\00-RESEARCH.md (Common Pitfall #6: CoinGecko DD-MM-YYYY date format; Code Examples: yfinance same-day/next-day close)
    - C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\phases\00-ground-truth\00-CONTEXT.md (D-10: report core columns; D-12: dated markdown + stdout, no dashboards)
  </read_first>
  <behavior>
    - `test_format_coingecko_date_returns_dd_mm_yyyy` — asserts `format_coingecko_date(date(2026, 7, 26))` returns `"26-07-2026"`, not `"2026-07-26"`
    - `test_fetch_stock_close_uses_one_day_window` — mocks `yfinance.Ticker.history`, asserts `fetch_stock_close("AAA", date(2026, 7, 26))` calls `history(start="2026-07-26", end="2026-07-27")` and returns the mocked Close float
    - `test_fetch_crypto_close_uses_ddmmyyyy_param` — mocks `requests.get` for `/coins/{id}/history`, asserts the call's `date` query param equals `"26-07-2026"` and the function returns `market_data.current_price.usd`
    - `test_compute_report_rows_joins_snapshot_and_closes` — given 2 fixture snapshot rows (1 stock, 1 crypto) inserted via `db.insert_snapshot_rows` into a temp db, with `fetch_stock_close`/`fetch_crypto_close` mocked, asserts `compute_report_rows(conn, week_start)` returns 2 rows each with `first_seen_ts`, `first_price`, `first_pct_gain`, `same_day_close`, `next_day_close`, `pct_return_same_day`, `pct_return_next_day`
    - `test_compute_coverage_stat_matches_db_query` — asserts `compute_coverage_stat` delegates to `db.query_poll_run_coverage` and returns the identical dict shape
    - `test_write_report_markdown_creates_dated_file` — asserts `write_report_markdown(rows, coverage, out_path)` creates a markdown file containing a table header row and the coverage percentage as text
    - `test_compute_report_rows_handles_empty_snapshots` — asserts calling `compute_report_rows` against a db with zero snapshot rows returns an empty list without raising (guards the pre-collection empty state Plan 00-05 hits on day one)
  </behavior>
  <action>
    Write the seven test cases described in behavior against `trader.ground_truth.report`, which does not exist yet. Tests must fail with `ModuleNotFoundError`/`ImportError` on collection.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_report.py -x` fails with a collection error referencing `trader.ground_truth.report`
    - All 7 test cases listed in behavior exist as named test functions
  </acceptance_criteria>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_report.py -x</automated>
  </verify>
  <done>All 7 listed test cases exist and fail on collection/import, not on assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the daily report generator (GREEN)</name>
  <files>trader/ground_truth/report.py</files>
  <read_first>
    - tests/test_report.py (Task 1 — implement exactly against this contract)
    - trader/ground_truth/db.py
  </read_first>
  <action>
    Implement `format_coingecko_date(d: date) -> str` returning `d.strftime("%d-%m-%Y")` (Pitfall 6). Implement `fetch_stock_close(symbol: str, on_date: date) -> float | None` using `yfinance.Ticker(symbol).history(start=on_date.isoformat(), end=(on_date + timedelta(days=1)).isoformat())`, returning `None` if the result is empty. Implement `fetch_crypto_close(coingecko_id: str, on_date: date, api_key: str) -> float | None` calling `GET https://api.coingecko.com/api/v3/coins/{coingecko_id}/history` with `params={"date": format_coingecko_date(on_date), "localization": "false"}`, header `x-cg-demo-api-key`, `timeout=10`, returning `data["market_data"]["current_price"]["usd"]` or `None` on a missing/error response. Implement `compute_report_rows(conn, week_start: date) -> list[dict]`: call `db.query_flagged_tickers_since(conn, week_start.isoformat())`; for each distinct ticker, route to `fetch_crypto_close` (using `coingecko_id`) when `source == "crypto"`, else `fetch_stock_close`, for both the flag date and flag date + 1 day; compute `pct_return_same_day = (same_day_close - first_price) / first_price * 100` and the equivalent for next-day, leaving a field `None` when its close is unavailable (e.g. next-day has not happened yet); return an empty list when `query_flagged_tickers_since` returns no rows, never raising. Implement `compute_coverage_stat(conn, week_start: date) -> dict` delegating directly to `db.query_poll_run_coverage(conn, week_start.isoformat())`. Implement `write_report_markdown(rows, coverage, out_path: str) -> None` writing a markdown table with columns `Ticker | First Seen | First Price | First % Gain | Same-Day Close | Next-Day Close | Return (Same-Day) | Return (Next-Day)`, a line `Coverage: {completed}/{expected} polls ({pct}%)`, and a summary line counting rows where `pct_return_same_day > 0` vs `<= 0` — this line directly answers the phase's exit-criterion sentence: "of everything flagged this week, what % ended the day up vs dumped". (D-11: closes are fetched directly via yfinance/CoinGecko until Phase 1's `get_daily_bars` exists — migrating to that API is a noted follow-up, not a Phase 0 blocker). Implement CLI `main(argv=None)` with `argparse` `--date YYYY-MM-DD` (defaults to today), setting `week_start = parsed_date - timedelta(days=7)`, calling the functions above, writing to `reports/{parsed_date.isoformat()}.md`, and printing the same summary stats to stdout (D-12: markdown file plus stdout, no dashboards).
  </action>
  <acceptance_criteria>
    - `pytest tests/test_report.py -x` passes, all 7 tests green
    - `grep -n "%d-%m-%Y" trader/ground_truth/report.py` confirms the DD-MM-YYYY format is used for the CoinGecko history call, not ISO format
  </acceptance_criteria>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_report.py -x</automated>
  </verify>
  <done>report.py exists, implements the exact signatures tested in Task 1, and the full test_report.py suite passes.</done>
</task>

<task type="auto">
  <name>Task 3: Empty-state dry run against the real (pre-collection) database</name>
  <files>reports/{today}.md (generated by running report.py)</files>
  <read_first>
    - trader/ground_truth/report.py (Task 2 output)
  </read_first>
  <action>
    Run `.venv\Scripts\python.exe -m trader.ground_truth.report` against the real `data/trader.db` (created fresh by `db.ensure_schema` if it does not already exist — no poll has run against the real database yet at this point in the plan sequence, since Plan 00-05 runs after this one). Confirm it does not crash on zero rows and produces a report explicitly stating zero tickers flagged and 0/0 coverage, proving the empty-state guard from Task 1/2 holds against the real (not fixture) database path.
  </action>
  <acceptance_criteria>
    - The command exits 0
    - `reports/{today's date in YYYY-MM-DD}.md` exists and contains a literal zero-state phrase (e.g. "0 tickers")
  </acceptance_criteria>
  <verify>
    <automated>.venv\Scripts\python.exe -m trader.ground_truth.report</automated>
  </verify>
  <done>report.py runs cleanly against an empty real database and produces a valid zero-state markdown report.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External close-price APIs (yfinance, CoinGecko) -> report computation | Third-party response data used to compute percentage returns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-08 | Information Disclosure | `report.py` CoinGecko client | mitigate | Same convention as sources.py (T-00-05) — never log the `x-cg-demo-api-key` header value |
| T-00-09 | Tampering | `report.py` markdown writer | accept | Report content is derived entirely from internally-generated SQLite data (already validated at insert time, T-00-04) and API responses; no further untrusted input crosses this boundary — low risk |
</threat_model>

<verification>
Run `.venv\Scripts\python.exe -m pytest tests/test_report.py -x` — all 7 tests must pass.
Run `.venv\Scripts\python.exe -m trader.ground_truth.report` against the real database — must exit 0 and produce a valid zero-state report.
</verification>

<success_criteria>
- report.py exists with the exact exported functions listed in must_haves and the full test suite is green
- The report correctly formats CoinGecko dates as DD-MM-YYYY and yfinance dates as ISO
- The report handles zero collected snapshots without crashing
</success_criteria>

<output>
Create `.planning/phases/00-ground-truth/00-04-SUMMARY.md` when done
</output>
