---
phase: 00-ground-truth
plan: 03
type: execute
wave: 3
depends_on:
  - "00-02"
files_modified:
  - trader/ground_truth/poll.py
  - scripts/poll.bat
  - scripts/poll_task.xml
  - tests/test_poll.py
autonomous: false
requirements:
  - DATA-01
  - DATA-02
  - DATA-04

must_haves:
  truths:
    - "poll.py --once calls both source adapters and writes rows to snapshots exactly once per invocation"
    - "A poll_runs row is recorded on every invocation, even when one source fails, so gaps are visible (D-06)"
    - "Windows Task Scheduler fires poll.bat every 15 minutes without a long-running daemon (D-05)"
  artifacts:
    - path: "trader/ground_truth/poll.py"
      provides: "orchestration entrypoint wiring sources.py and db.py"
      exports: ["run_poll_once", "is_market_hours", "main"]
    - path: "scripts/poll.bat"
      provides: "Task Scheduler launcher that sets cwd and activates the venv (Pitfall 3)"
    - path: "scripts/poll_task.xml"
      provides: "Task Scheduler task definition with StartWhenAvailable enabled (Assumption A3)"
  key_links:
    - from: "trader/ground_truth/poll.py"
      to: "trader/ground_truth/db.py"
      via: "insert_snapshot_rows + record_poll_run calls"
      pattern: "insert_snapshot_rows"
    - from: "Windows Task Scheduler"
      to: "scripts/poll.bat"
      via: "schtasks /create /xml scripts/poll_task.xml, Repetition Interval PT15M"
      pattern: "PT15M"
---

<objective>
Wire the source adapters and database into a one-shot poll entrypoint, then register it with Windows Task Scheduler so it runs every 15 minutes forever without a daemon process.

Purpose: This is the piece that actually satisfies DATA-01 (polls both feeds every 15 minutes) and starts the DATA-04 clock (runs continuously, keeps running). Everything before this plan was foundation; this plan makes the scanner actually run on schedule.
Output: `trader/ground_truth/poll.py`, `scripts/poll.bat`, `scripts/poll_task.xml`, a passing `tests/test_poll.py`, and a live, registered, enabled Task Scheduler task named `TraderGroundTruthPoll`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\PROJECT.md
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\ROADMAP.md
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\STATE.md
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\phases\00-ground-truth\00-CONTEXT.md
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\phases\00-ground-truth\00-RESEARCH.md
@C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\phases\00-ground-truth\00-02-SUMMARY.md
</context>

<interfaces>
<!-- Exports from Plan 00-02 that this plan consumes directly. No exploration needed. -->

From `trader/ground_truth/sources.py`:
`StockGainersSource().fetch_top_movers(count=50) -> list[dict]` (each dict: ticker, price, pct_gain, rank, source="stock"); `CryptoMoversSource().fetch_top_movers(count=50) -> list[dict]` (each dict adds coingecko_id, source="crypto"); raises `SourceUnavailableError` if a source's primary and fallback both fail.

From `trader/ground_truth/db.py`:
`get_connection(db_path="data/trader.db") -> sqlite3.Connection`; `insert_snapshot_rows(conn, rows: list[dict]) -> int`; `record_poll_run(conn, poll_ts, stock_success, crypto_success, stock_row_count, crypto_row_count) -> None`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for the poll orchestration entrypoint (RED)</name>
  <files>tests/test_poll.py</files>
  <read_first>
    - trader/ground_truth/sources.py, trader/ground_truth/db.py (Plan 00-02 — the contracts this task's tests exercise)
    - C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\phases\00-ground-truth\00-RESEARCH.md (Architecture Patterns Pattern 3: Poll Unconditionally, Tag Market State; Open Question 3: crypto is always 24/7)
  </read_first>
  <behavior>
    - `test_run_poll_once_calls_both_sources_and_inserts_rows` — mocks `StockGainersSource.fetch_top_movers` and `CryptoMoversSource.fetch_top_movers`, each returning 3 fixture rows, against a temp db; asserts `run_poll_once(db_path)` returns a summary dict with `stock_rows == 3` and `crypto_rows == 3`, and each source's `fetch_top_movers` was called exactly once
    - `test_run_poll_once_continues_when_stock_source_raises` — mocks `StockGainersSource.fetch_top_movers` to raise `SourceUnavailableError`, mocks crypto to succeed; asserts `run_poll_once` still writes the crypto rows and records `stock_success=False, crypto_success=True` in `poll_runs` — one missed leg never blocks the other (D-06)
    - `test_is_market_hours_true_during_nyse_session` — asserts `is_market_hours(datetime(2026, 7, 27, 14, 30, tzinfo=timezone.utc))` (10:30am US/Eastern, a Monday) returns True
    - `test_is_market_hours_false_on_weekend` — asserts a Saturday UTC timestamp returns False
    - `test_crypto_rows_always_market_open_true` — asserts every crypto-sourced row gets `market_open=1` regardless of the stock-side market_open value (crypto never closes, Open Question 3)
    - `test_once_flag_runs_exactly_one_poll` — invokes `main(["--once"])` with sources/db mocked, asserts `run_poll_once` is called exactly once
    - `test_main_without_once_flag_exits_nonzero` — invokes `main([])` and asserts it exits with a non-zero code and prints a usage message referencing `--once` (Phase 0 has no daemon mode at all per D-05; omitting the flag is a usage error, not a silent no-op)
  </behavior>
  <action>
    Write the seven test cases described in behavior against `trader.ground_truth.poll`, which does not exist yet. Tests must fail with `ModuleNotFoundError`/`ImportError` on collection.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_poll.py -x` fails with a collection error referencing `trader.ground_truth.poll`
    - All 7 test cases listed in behavior exist as named test functions
  </acceptance_criteria>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_poll.py -x</automated>
  </verify>
  <done>All 7 listed test cases exist and fail on collection/import, not on assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the poll orchestration entrypoint (GREEN)</name>
  <files>trader/ground_truth/poll.py</files>
  <read_first>
    - tests/test_poll.py (Task 1 — implement exactly against this contract)
    - trader/ground_truth/sources.py, trader/ground_truth/db.py
  </read_first>
  <action>
    Implement `is_market_hours(now_utc: datetime) -> bool` per 00-RESEARCH.md Pattern 3: convert `now_utc` to `America/New_York` via stdlib `zoneinfo.ZoneInfo`, return `False` if `weekday() >= 5`, else return `True` iff `(9, 30) <= (hour, minute) < (16, 0)`. Implement `run_poll_once(db_path: str = "data/trader.db") -> dict`: open `db.get_connection(db_path)`; call `StockGainersSource().fetch_top_movers(count=50)` inside a try/except catching `SourceUnavailableError` — on success tag every row `market_open = is_market_hours(datetime.now(timezone.utc))`, on failure log the exception and set `stock_rows = 0, stock_success = False`; independently call `CryptoMoversSource().fetch_top_movers(count=50)` inside its own try/except, tagging every row `market_open = 1` unconditionally (crypto never closes) — never let a stock-side failure skip the crypto call or vice versa; call `db.insert_snapshot_rows` for whichever rows succeeded, then always call `db.record_poll_run` with the current UTC timestamp and both success flags/counts, then return the summary dict `{"stock_rows": n, "crypto_rows": n, "stock_success": bool, "crypto_success": bool}`. Implement `main(argv=None)` using `argparse` with a required `--once` flag (`required=True`, since Phase 0 has no other run mode per D-05 — omitting it is a usage error, exit code 2, printing a message that `--once` is required); on success call `run_poll_once()` and print the summary. Guard the CLI with `if __name__ == "__main__": main()`.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_poll.py -x` passes, all 7 tests green
    - `grep -n "market_open" trader/ground_truth/poll.py` shows crypto rows always set to `1`/`True` unconditionally, and stock rows call `is_market_hours`
  </acceptance_criteria>
  <verify>
    <automated>.venv\Scripts\python.exe -m pytest tests/test_poll.py -x</automated>
  </verify>
  <done>poll.py exists, implements the exact signatures tested in Task 1, and the full test_poll.py suite passes.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 3: Create the Task Scheduler launcher, then register it manually (D-07)</name>
  <files>scripts/poll.bat, scripts/poll_task.xml</files>
  <read_first>
    - trader/ground_truth/poll.py (Task 2 output — the CLI this .bat file invokes)
    - C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\phases\00-ground-truth\00-RESEARCH.md (Common Pitfall #3: wrong interpreter/cwd; Common Pitfall #4 + Assumption A3: sleep/wake and StartWhenAvailable)
    - C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.planning\phases\00-ground-truth\00-CONTEXT.md (D-07: Task Scheduler setup is a documented manual step with the exact schtasks command — locked decision, registration is never an automated Bash call)
  </read_first>
  <what-built>
    `scripts/poll.bat` — `@echo off`, `cd /d "C:\Users\Owner\Desktop\Claude Project\AI TRADRR"`, then `"C:\Users\Owner\Desktop\Claude Project\AI TRADRR\.venv\Scripts\python.exe" -m trader.ground_truth.poll --once`, then `exit /b %ERRORLEVEL%` (Pitfall 3: Task Scheduler's own "Start in" field is unreliable, so the `.bat` sets cwd and calls the venv interpreter explicitly).
    `scripts/poll_task.xml` — a Windows Task Scheduler XML task definition named `TraderGroundTruthPoll` with a `TimeTrigger` `StartBoundary` set to today's date at the next quarter-hour, a `Repetition` block with `Interval` `PT15M` and `Duration` `P9999D` (effectively forever), `Enabled` true; a `Settings` block with `StartWhenAvailable` set to `true` (directly addresses Assumption A3 — the task fires as soon as possible after a missed trigger, e.g. laptop was asleep) and `DisallowStartIfOnBatteries` set to `false`; an `Actions` block with `Exec/Command` pointing at `scripts\poll.bat`.
    Both files are created automatically as part of this task. Per D-07, the registration step itself is a documented manual action, not an automated Bash call.
  </what-built>
  <how-to-verify>
    1. Open a terminal (Command Prompt or PowerShell) at the project root: `C:\Users\Owner\Desktop\Claude Project\AI TRADRR`.
    2. Run exactly: `schtasks /create /tn "TraderGroundTruthPoll" /xml "C:\Users\Owner\Desktop\Claude Project\AI TRADRR\scripts\poll_task.xml" /f`
    3. Run: `schtasks /query /tn "TraderGroundTruthPoll" /v /fo list` and confirm the output shows `Scheduled Task State: Enabled` and a repeat interval of 15 minutes.
  </how-to-verify>
  <acceptance_criteria>
    - `scripts/poll_task.xml` contains the literal string `<StartWhenAvailable>true</StartWhenAvailable>`
    - `scripts/poll.bat` contains the literal path `.venv\Scripts\python.exe` and the literal argument `--once`
    - `schtasks /query /tn "TraderGroundTruthPoll" /v /fo list` reports `Scheduled Task State: Enabled` and a 15-minute repeat interval after the manual registration step above
  </acceptance_criteria>
  <resume-signal>Type "registered" once `schtasks /query` confirms the task is Enabled with a 15-minute repeat interval, or describe any error from the schtasks command instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Windows Task Scheduler -> poll.bat -> Python process | Local-machine trigger surface; no network-facing entry point |
| External HTTP calls inside run_poll_once | Time-bounded, third-party network calls on a fixed 15-minute cadence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-06 | Information Disclosure | `poll.py` logging on source failure | mitigate | Poll run logs never print the `COINGECKO_API_KEY` value even on a caught exception — log exception type/message only, matching sources.py's existing convention (T-00-05) |
| T-00-07 | Denial of Service | External HTTP calls inside `run_poll_once` | mitigate | Both source calls use `timeout=10` (set in sources.py) and are wrapped in independent try/except blocks so one hanging/failing call never blocks the 15-minute cadence or the other source (D-06, Pitfall 2) |
| T-00-SC | Tampering | Package installs | accept | This plan adds no new dependencies; see Plan 00-01's T-00-01 for the phase's package legitimacy disposition |
</threat_model>

<verification>
Run `.venv\Scripts\python.exe -m pytest tests/test_poll.py -x` — all 7 tests must pass.
Run `schtasks /query /tn "TraderGroundTruthPoll" /v /fo list` — must report Enabled with a 15-minute repeat interval.
</verification>

<success_criteria>
- poll.py exists with the exact exported functions listed in must_haves and the full test suite is green
- scripts/poll.bat and scripts/poll_task.xml exist and are registered as an enabled, 15-minute Task Scheduler task
- A single missed source (stock or crypto) never blocks the other from being logged
</success_criteria>

<output>
Create `.planning/phases/00-ground-truth/00-03-SUMMARY.md` when done
</output>
