---
phase: 03-strategy-lab
plan: 03
type: tdd
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - trader/backtest/sweep.py
  - tests/test_sweep_engine.py
autonomous: true
requirements: [STRAT-03]

must_haves:
  truths:
    - "A hash-tampered frozen config blocks every sweep cell from running, before any database write"
    - "Every tune-sweep cell's provenance (sweep_id, regime, split, asset_class_bucket, strategy) is recoverable from backtest_runs.params_json alone"
    - "A cell with fewer than 30 trades can never be selected as a top-5 candidate regardless of its profit factor"
  artifacts:
    - path: "trader/backtest/sweep.py"
      provides: "Tune-sweep grid orchestration (STRAT-03) + D-10 top-5 selection with min-trade floor"
      exports: ["run_tune_sweep", "select_top5", "DEFAULT_SWEEP_ID"]
    - path: "tests/test_sweep_engine.py"
      provides: "Fixture-grid smoke tests proving hash-gate, provenance, and selection-floor behavior"
  key_links:
    - from: "trader/backtest/sweep.py"
      to: "trader/backtest/frozen_config.py"
      via: "run_tune_sweep calls verify_frozen() before any grid iteration"
      pattern: "verify_frozen\\("
    - from: "trader/backtest/sweep.py"
      to: "trader/backtest/runner.py"
      via: "every cell calls run_backtest unmodified (D-07)"
      pattern: "run_backtest\\("
---

<objective>
Build the sweep orchestration engine: grid iteration over the frozen exit-profile grid, sweep provenance tagging, the frozen-config hash gate as a hard runtime check, and D-10's pre-registered top-5 selection rule with a minimum-trade-count floor.

Purpose: STRAT-03's exit-parameter sweep only counts if every cell goes through the unmodified engine (D-07) and every result is traceable and honestly selected (D-10, Overfitting Guards). This plan proves the engine correct against tiny fixture grids; Plan 03-04 runs it for real.
Output: `trader/backtest/sweep.py` with `run_tune_sweep` and `select_top5`, fully unit-tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-strategy-lab/03-CONTEXT.md
@.planning/phases/03-strategy-lab/03-RESEARCH.md

<interfaces>
From trader/backtest/runner.py (unchanged, consume only):
  run_backtest(strategy_fn, universe: list[str], profile: EXIT_PROFILE, bars_by_symbol: dict, seed: int, params: dict, strategy_id: str, conn) -> int  # returns run_id

From trader/backtest/ledger.py (unchanged, consume only):
  get_trades_for_run(conn, run_id) -> list[dict]

From trader/backtest/metrics.py (unchanged, consume only):
  compute_metrics(trades: list[dict]) -> dict  # keys: profit_factor, sharpe_ratio, max_drawdown, win_rate, avg_win, avg_loss, trade_count, total_fees_paid

From trader/backtest/frozen_config.py (Plan 03-02, consume only):
  verify_frozen(repo_root=None) -> None  # raises RuntimeError on hash mismatch

From trader/backtest/exit_grid.py (Plan 03-02, consume only):
  exit_profile_grid(bucket: str) -> Iterator[EXIT_PROFILE]

From trader/backtest/regimes.py (Plan 03-02, consume only):
  Regime(bucket, label, tune_start: str | None, tune_end, oos_start, oos_end)  # frozen dataclass
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: run_tune_sweep — grid iteration, hash gate, provenance</name>
  <files>trader/backtest/sweep.py, tests/test_sweep_engine.py</files>
  <read_first>
    trader/backtest/runner.py (run_backtest signature/contract, exact param names)
    trader/backtest/ledger.py (record_run/params_json storage, get_trades_for_run)
    trader/backtest/frozen_config.py (verify_frozen)
    trader/backtest/regimes.py (Regime fields)
    trader/backtest/exit_grid.py (exit_profile_grid)
    .planning/phases/03-strategy-lab/03-RESEARCH.md Pattern 2/3 and Sweep Engineering section
  </read_first>
  <behavior>
    - run_tune_sweep() calls frozen_config.verify_frozen() before iterating any cell; when FROZEN_HASH is monkeypatched to a wrong value, RuntimeError is raised and zero run_backtest calls occur (assert via a call-count spy on run_backtest).
    - With a tiny fixture universe (2-3 symbols, ~40 synthetic bars) and bucket="stock" (or an injected 2x2x1x1 grid), run_tune_sweep returns one result dict per grid cell, each with keys {run_id, params, metrics}.
    - Every returned result's persisted backtest_runs.params_json (read back from conn) contains sweep_id, regime, split=="tune", asset_class_bucket, strategy keys.
    - Bars passed to run_backtest are sliced to [regime.tune_start, regime.tune_end] only — a stub strategy_fn records the max date it was ever called with and it never exceeds tune_end.
  </behavior>
  <action>
    Implement `_slice_bars(bars_by_symbol: dict, start: str | None, end: str) -> dict`: per-symbol DataFrame filtered to the closed UTC date interval (`start=None` means "from the symbol's own first row", i.e. `df.index[0]`). Implement `run_tune_sweep(strategy_fn, strategy_id: str, bucket: str, regime, bars_by_symbol: dict, universe: list[str], conn, sweep_id: str, seed: int = 20260726) -> list[dict]`: call `frozen_config.verify_frozen()` first, propagating RuntimeError uncaught (no try/except swallowing it); slice bars once via `_slice_bars(bars_by_symbol, regime.tune_start, regime.tune_end)`; for each `profile` from `exit_grid.exit_profile_grid(bucket)`, build `params` with profile_name (composed from strategy_id/bucket/regime.label/"tune"/stop_pct/tp_pct/trailing_pct/max_hold_days), sweep_id, regime=regime.label, split="tune", asset_class_bucket=bucket, strategy=strategy_id, plus the four exit-profile fields; call `runner.run_backtest(strategy_fn, universe, profile, sliced_bars, seed, params, strategy_id, conn)`; read back trades via `ledger.get_trades_for_run(conn, run_id)`, compute `metrics.compute_metrics(trades)`; append `{"run_id": run_id, "params": params, "metrics": computed}`. Add module constant `DEFAULT_SWEEP_ID = "2026-07-26-strategy-lab-v1"` for scripts in later plans to reuse.
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_sweep_engine.py -k "tune or hash" -q -x` passes.
    - RuntimeError raised before any DB write when FROZEN_HASH is wrong; backtest_runs row count unchanged in that case.
    - Persisted params_json round-trips through json.loads with all 5 provenance keys present for every cell.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_sweep_engine.py -k "tune or hash" -q -x</automated>
  </verify>
  <done>run_tune_sweep implemented, hash-gated, provenance-tagged, calling run_backtest unmodified for every cell (D-07).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: select_top5 — D-10 rule with min-trade floor</name>
  <files>trader/backtest/sweep.py, tests/test_sweep_engine.py</files>
  <read_first>
    .planning/phases/03-strategy-lab/03-RESEARCH.md Overfitting Guards section (D-10, min-trade floor of 30, "rank by post-cost profit factor, not raw return")
    .planning/phases/03-strategy-lab/03-CONTEXT.md D-10
  </read_first>
  <behavior>
    - From a synthetic list of 8 cell-result dicts with varying trade_count and profit_factor, select_top5(results, min_trades=30) excludes any with trade_count < 30 even when profit_factor is high, and returns at most 5, ranked descending by profit_factor among eligible cells.
    - A cell with profit_factor == math.inf (zero losses) ranks above all finite values.
    - Fewer than 5 eligible cells returns all of them, no padding, no error.
    - Zero eligible cells (all below the trade floor) returns an empty list, not an error.
  </behavior>
  <action>
    Implement `select_top5(cell_results: list[dict], min_trades: int = 30) -> list[dict]`: filter to `r["metrics"]["trade_count"] >= min_trades`; sort descending by `r["metrics"]["profit_factor"] if r["metrics"]["profit_factor"] is not None else 0.0` (None floors to worst-ranked, math.inf naturally sorts first); return the first 5 (or fewer). Document inline that ranking by post-cost profit_factor (never raw return) is deliberate per 03-RESEARCH.md's Overfitting Guards #2 — `compute_metrics` already reflects fees/slippage.
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_sweep_engine.py -k select_top5 -q -x` passes covering all 4 behaviors above.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_sweep_engine.py -k select_top5 -q -x</automated>
  </verify>
  <done>select_top5 enforces the min-trade floor before ranking and never returns more than 5 results; D-10's pre-registered rule is the only code path that selects OOS candidates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sweep.py -> frozen_config.py | In-process file-hash check, no external input |
| sweep.py -> runner.run_backtest | In-process call into the unmodified Phase 2 engine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-03-08 | Tampering | sweep.run_tune_sweep | mitigate | frozen_config.verify_frozen() called first, before any grid iteration or DB write; RuntimeError propagates uncaught |
| T-03-09 | Repudiation | sweep.run_tune_sweep | mitigate | Every cell's params_json carries sweep_id/regime/split/asset_class_bucket/strategy; test reads params_json back from the DB (not in-memory state) to prove it round-trips |
| T-03-10 | Tampering | sweep.run_tune_sweep | mitigate | Every cell calls runner.run_backtest unmodified (D-07) — no bypass path for fills/fees/slippage exists in this module |
| T-03-11 | Repudiation | sweep.select_top5 | mitigate | Min-trade floor (>=30) enforced before ranking; a cherry-picked lucky small-N cell cannot reach the top-5 list |
</threat_model>

<verification>
`python -m pytest tests/test_sweep_engine.py -q` passes. Full suite (`python -m pytest -q`) stays green.
</verification>

<success_criteria>
STRAT-03's sweep engine is proven correct against tiny fixture grids: hash-gated, provenance-tagged, min-trade-floored, and D-10-compliant. Ready for Plan 03-04 to drive it against the real universe.
</success_criteria>

<output>
Create `.planning/phases/03-strategy-lab/03-03-SUMMARY.md` when done.
</output>
