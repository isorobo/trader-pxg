---
phase: 03-strategy-lab
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - trader/backtest/universe.py
  - trader/backtest/regimes.py
  - trader/backtest/exit_grid.py
  - trader/backtest/frozen_config.py
  - trader/backtest/backfill_universe.py
  - tests/test_regime_config.py
  - tests/test_exit_grid.py
  - tests/test_frozen_config.py
autonomous: true
requirements: [STRAT-03, STRAT-04, STRAT-05]

must_haves:
  truths:
    - "The sweep grid and every regime/tune/OOS date are frozen in committed code before any sweep cell runs"
    - "Every universe symbol has cached daily bars spanning back to its regime's earliest tune_start"
    - "A byte-level change to the frozen universe/regime/grid files is detected and blocks any sweep from running"
  artifacts:
    - path: "trader/backtest/universe.py"
      provides: "Frozen D-04 universe lists (18 stock, 4 crypto-major/legacy-memecoin, 3 new-memecoin)"
      exports: ["STOCK_UNIVERSE", "CRYPTO_MAJOR_LEGACY_MEME_UNIVERSE", "NEW_MEMECOIN_UNIVERSE", "UNIVERSE_BY_BUCKET", "BUCKET_STOCK", "BUCKET_CRYPTO_MAJOR_LEGACY_MEME", "BUCKET_NEW_MEMECOIN"]
    - path: "trader/backtest/regimes.py"
      provides: "6 frozen regime windows with tune/OOS splits (D-08/D-09)"
      exports: ["Regime", "REGIMES"]
    - path: "trader/backtest/exit_grid.py"
      provides: "D-06 exit-profile grid (270 cells stock/crypto-major bucket, 360 additive for new-memecoin)"
      exports: ["exit_profile_grid", "STOPS", "TPS", "TRAILS", "TIME_STOPS", "MEMECOIN_SHORT_HOLD_DAYS"]
    - path: "trader/backtest/frozen_config.py"
      provides: "Hash-based freeze gate — the code-level enforcement of frozen-before-results"
      exports: ["compute_hash", "verify_frozen", "FROZEN_HASH", "FROZEN_FILES"]
    - path: "trader/backtest/backfill_universe.py"
      provides: "One-time live backfill of the 25-symbol universe"
  key_links:
    - from: "trader/backtest/frozen_config.py"
      to: "trader/backtest/exit_grid.py"
      via: "FROZEN_FILES lists exit_grid.py's path for hashing"
      pattern: "exit_grid\\.py"
    - from: "trader/backtest/backfill_universe.py"
      to: "trader/data/api.py"
      via: "get_daily_bars(symbol, asset_class=..., conn=None) one-time live fetch"
      pattern: "get_daily_bars\\("
---

<objective>
Freeze the D-04 universe, D-08/D-09 regime windows and tune/OOS splits, and the D-06 exit-parameter grid in committed code, gate all three behind a hash-verification mechanism (frozen-before-results as a code gate, not a convention), and run the one-time live backfill so every universe symbol's history is cached offline for the sweeps in later waves.

Purpose: STRAT-03/04/05 all depend on frozen, auditable config that cannot silently change after results are viewed. Everything in this plan is config + one live I/O step — no sweep logic lives here.
Output: `universe.py`, `regimes.py`, `exit_grid.py`, `frozen_config.py`, `backfill_universe.py`, all committed with FROZEN_HASH locked.

Scope note (config.py's SLIPPAGE_SMALL_CAP_RUNNER): this documented-but-unwired Phase 3 hook (config.py's module docstring, "Slippage rationale" section) is explicitly OUT OF SCOPE this phase — D-04's fixed universe (18 large/mid-cap stocks + 7 named crypto pairs) contains no scanner-flagged small-cap-runner category at all, so there is nothing in this phase's universe to apply it to. It stays unwired, deferred alongside scanner-replay backtesting (D-05) until Phase 0's scanner data matures enough to backtest the live scanner's actual small-cap runner picks — not silently dropped, explicitly deferred.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-strategy-lab/03-CONTEXT.md
@.planning/phases/03-strategy-lab/03-RESEARCH.md

<interfaces>
From trader/backtest/config.py (unchanged, consume only):
  EXIT_PROFILE(stop_pct, tp_pct, scale_out: tuple, trailing_pct, max_hold_days, eod_flat)  # frozen dataclass; scale_out MUST be a tuple

From trader/data/api.py (unchanged, consume only):
  get_daily_bars(symbol, start=None, end=None, asset_class=None, conn=None) -> pd.DataFrame
  CRYPTO_COINGECKO_IDS: dict[str, str]  # base-asset -> CoinGecko id, covers BTC/ETH/DOGE/SHIB/PEPE/BONK/WIF

From trader/backtest/sanity_universe.py (pattern to mirror exactly for backfill_universe.py):
  main() iterates a fixed symbol list, calling get_daily_bars(symbol, asset_class=..., conn=None) with no start/end, printing a row-count/date-range report per symbol.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Frozen universe and regime windows</name>
  <files>trader/backtest/universe.py, trader/backtest/regimes.py, tests/test_regime_config.py</files>
  <read_first>
    .planning/phases/03-strategy-lab/03-RESEARCH.md Universe section and Regime Windows section (exact symbol lists and dates — copy verbatim, do not re-derive)
    .planning/phases/03-strategy-lab/03-CONTEXT.md D-04, D-08, D-09
    trader/data/api.py CRYPTO_COINGECKO_IDS (confirms BTC/ETH/DOGE/SHIB/PEPE/BONK/WIF are all classification-ready)
  </read_first>
  <behavior>
    - universe.STOCK_UNIVERSE has exactly 18 unique symbols including AAPL, MSFT, GOOGL, NVDA, AMD, TSLA, AMZN, META, NFLX, CRM, ADBE, COST, JPM, XOM, UNH, WMT, HD, DIS.
    - universe.CRYPTO_MAJOR_LEGACY_MEME_UNIVERSE == exactly ["BTC/USDT", "ETH/USDT", "DOGE/USDT", "SHIB/USDT"].
    - universe.NEW_MEMECOIN_UNIVERSE == exactly ["PEPE/USDT", "BONK/USDT", "WIF/USDT"].
    - regimes.REGIMES is a tuple of exactly 6 Regime instances, exactly 2 per bucket (stock, crypto_major_legacy_meme, new_memecoin).
    - Every regime's tune_end < oos_start (ISO string compare) and oos_start <= oos_end.
    - Only the new_memecoin "mania" regime has tune_start is None (symbol-relative first bar); every other regime has a fixed tune_start string.
  </behavior>
  <action>
    Create trader/backtest/universe.py with string constants BUCKET_STOCK="stock", BUCKET_CRYPTO_MAJOR_LEGACY_MEME="crypto_major_legacy_meme", BUCKET_NEW_MEMECOIN="new_memecoin" (distinct from config.py's/instruments-table's "stock"/"crypto_major"/"memecoin" per-symbol asset_class values — these are sweep/regime GROUPING buckets, never passed to fills.py or ledger's asset_class column). Define STOCK_UNIVERSE (18 symbols per 03-RESEARCH.md's Universe table), CRYPTO_MAJOR_LEGACY_MEME_UNIVERSE (4 symbols), NEW_MEMECOIN_UNIVERSE (3 symbols), and UNIVERSE_BY_BUCKET mapping each bucket constant to its list. Create trader/backtest/regimes.py importing the three bucket constants from universe.py, defining a frozen `@dataclass(frozen=True) class Regime` with fields bucket: str, label: str, tune_start: str | None, tune_end: str, oos_start: str, oos_end: str, and a `REGIMES` tuple of exactly 6 entries using 03-RESEARCH.md's Regime Windows dates verbatim: stock/trending (tune 2023-01-01..2024-06-30, oos 2024-07-01..2024-12-31), stock/choppy (tune 2015-01-01..2016-06-30, oos 2016-07-01..2016-12-31), crypto_major_legacy_meme/trending (tune 2023-01-01..2024-06-30, oos 2024-07-01..2024-12-31), crypto_major_legacy_meme/bear (tune 2022-01-01..2022-08-31, oos 2022-09-01..2022-12-31), new_memecoin/mania (tune_start=None, tune_end 2024-08-31, oos 2024-09-01..2024-12-31), new_memecoin/correction (tune 2025-01-01..2025-08-31, oos 2025-09-01..2025-12-31).
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_regime_config.py -q -x` passes.
    - `regimes.REGIMES` items are immutable (`dataclasses.FrozenInstanceError` on attribute reassignment).
    - Universe list lengths are exactly 18/4/3.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_regime_config.py -q -x</automated>
  </verify>
  <done>universe.py and regimes.py committed with exact frozen symbols/dates; tune/OOS separation proven for all 6 regimes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Frozen exit grid + hash-based freeze gate</name>
  <files>trader/backtest/exit_grid.py, trader/backtest/frozen_config.py, tests/test_exit_grid.py, tests/test_frozen_config.py</files>
  <read_first>
    trader/backtest/universe.py (Task 1's just-committed BUCKET_NEW_MEMECOIN constant)
    trader/backtest/config.py (EXIT_PROFILE contract — scale_out must be a tuple; also its module docstring's "Slippage rationale" note on SLIPPAGE_SMALL_CAP_RUNNER, which stays unwired and out of scope per this plan's objective note)
    .planning/phases/03-strategy-lab/03-RESEARCH.md Sweep Engineering section and Code Examples Pattern 2, and the orchestrator's resolution of Open Question 2 ("memecoin time-stop cells are ADDITIVE — grid grows to ~360 cells for that class")
  </read_first>
  <behavior>
    - len(list(exit_grid.exit_profile_grid(universe.BUCKET_STOCK))) == 270; same for BUCKET_CRYPTO_MAJOR_LEGACY_MEME.
    - len(list(exit_grid.exit_profile_grid(universe.BUCKET_NEW_MEMECOIN))) == 360.
    - Every yielded item is a config.EXIT_PROFILE with scale_out == () and eod_flat == False.
    - frozen_config.compute_hash() == frozen_config.FROZEN_HASH (self-consistency at commit time — this equality IS the freeze point).
    - frozen_config.verify_frozen() raises RuntimeError when any one of the three frozen files' bytes differ from what FROZEN_HASH was computed from (simulate via a tmp_path copy with FROZEN_FILES monkeypatched to point at a byte-mutated copy).
  </behavior>
  <action>
    Create trader/backtest/exit_grid.py with STOPS=(-0.05,-0.10,-0.15,-0.20,-0.25,-0.30), TPS=(0.20,0.40,0.60,0.80,1.00), TRAILS=(None,0.10,0.20), TIME_STOPS=(None,10,30), MEMECOIN_SHORT_HOLD_DAYS=3 (Claude's discretion per D-06's "memecoins additionally test eod_flat-style short holds" — a fourth, additive time-stop value used ONLY for the new_memecoin bucket, never replacing the base three; no other asset class gets a small-cap-runner-style variant this phase — see this plan's objective scope note on SLIPPAGE_SMALL_CAP_RUNNER). Implement `exit_profile_grid(bucket: str)`: time_stops = TIME_STOPS if bucket != universe.BUCKET_NEW_MEMECOIN else TIME_STOPS + (MEMECOIN_SHORT_HOLD_DAYS,); yield config.EXIT_PROFILE(stop_pct=s, tp_pct=t, scale_out=(), trailing_pct=tr, max_hold_days=m, eod_flat=False) via itertools.product(STOPS, TPS, TRAILS, time_stops) — 6x5x3x3=270 for stock/crypto_major_legacy_meme, 6x5x3x4=360 for new_memecoin, matching D-06 and the orchestrator's additive resolution exactly. Create trader/backtest/frozen_config.py with FROZEN_FILES = ("trader/backtest/universe.py", "trader/backtest/regimes.py", "trader/backtest/exit_grid.py"), `compute_hash(repo_root: Path | None = None) -> str` (sha256 over each FROZEN_FILES entry's raw bytes, in list order, relative to repo_root or `Path(__file__).resolve().parents[2]` by default), and `verify_frozen(repo_root: Path | None = None) -> None` raising RuntimeError (message citing standing rule 1: never edit graduation/kill criteria — or grid/universe/regime definitions — while looking at results) when `compute_hash(repo_root) != FROZEN_HASH`. After exit_grid.py is finalized, run `compute_hash()` once in a throwaway shell and hard-code its returned hex digest as the committed `FROZEN_HASH` constant in frozen_config.py — this hard-coding is the literal freeze point, not a comment or convention.
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_exit_grid.py tests/test_frozen_config.py -q -x` passes.
    - Grid cell counts are exactly 270/270/360 per bucket.
    - `verify_frozen()` raises RuntimeError on any byte-level change to any of the three frozen files; raises nothing when files are unchanged.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_exit_grid.py tests/test_frozen_config.py -q -x</automated>
  </verify>
  <done>exit_grid.py and frozen_config.py committed with FROZEN_HASH matching the actual current file contents; later waves' sweep code can call verify_frozen() as a hard, uncatchable gate.</done>
</task>

<task type="auto">
  <name>Task 3: One-time live universe backfill</name>
  <files>trader/backtest/backfill_universe.py</files>
  <read_first>
    trader/backtest/sanity_universe.py (existing one-time-backfill pattern — mirror its shape exactly, do not invent a new one)
    trader/data/api.py (get_daily_bars signature, resolve_instrument classification path)
    trader/backtest/universe.py (Task 1's UNIVERSE_BY_BUCKET, STOCK_UNIVERSE, CRYPTO_MAJOR_LEGACY_MEME_UNIVERSE, NEW_MEMECOIN_UNIVERSE)
  </read_first>
  <action>
    Create trader/backtest/backfill_universe.py mirroring sanity_universe.py's shape: iterate universe.STOCK_UNIVERSE with asset_class="stock" explicit (no classification call needed), then universe.CRYPTO_MAJOR_LEGACY_MEME_UNIVERSE + universe.NEW_MEMECOIN_UNIVERSE with asset_class=None (routes through resolve_instrument's live CoinGecko classification path for any symbol not yet registered — SHIB/PEPE/BONK/WIF are new registrations this phase; BTC/ETH/DOGE are already registered from Phase 2's sanity universe). Call get_daily_bars(symbol, asset_class=asset_class, conn=None) with no start/end (full available history, matching sanity_universe.py's own call shape) so cached history spans every regime window this phase needs — earliest requirement is the stock choppy regime's 2015-01-01 tune_start, well within the 10+ years verified in 03-RESEARCH.md's Universe table for every stock symbol. main() must raise RuntimeError (not silently continue) if any symbol backfills with zero rows, and print a row-count/date-range report per symbol via a `_report` helper matching sanity_universe.py's own style. Module docstring states this is Phase 3's one-time live network step, exempted from the fast pytest loop per 03-VALIDATION.md's "Live/manual command exemption."
  </action>
  <acceptance_criteria>
    - Running the script populates data/trader.db with all 25 universe symbols (18 stock + 4 crypto-major/legacy-meme + 3 new-memecoin), each returning nonzero rows.
    - Each symbol's cached range extends back at least to its regime bucket's earliest tune_start (2015-01-01 for stocks, 2022-01-01 for crypto-major/legacy-meme, each new memecoin's own first Binance candle for the mania regime).
    - Script exits 0 with no unhandled exception.
  </acceptance_criteria>
  <verify>
    <automated>python -m trader.backtest.backfill_universe</automated>
  </verify>
  <done>data/trader.db cache contains all 25 universe symbols with sufficient history depth for every frozen regime window; printed report confirms nonzero rows for every symbol.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backfill_universe.py -> yfinance/ccxt/CoinGecko | One-time live network call, reusing Phase 1's already-tested get_daily_bars cache-first path |
| exit_grid/universe/regimes -> frozen_config | Local file-content trust boundary — the hash gate exists to detect accidental or malicious post-hoc edits, not network attackers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-03-05 | Tampering | backfill_universe.py | accept | Reuses Phase 1's already-tested get_daily_bars cache-first fetch path verbatim; no new parsing/trust logic added in this plan |
| T-03-06 | Tampering | frozen_config.py | mitigate | FROZEN_HASH computed once and hard-coded at commit time; verify_frozen() recomputes and raises RuntimeError on any later edit to universe.py/regimes.py/exit_grid.py, enforced at every sweep AND validation entrypoint — Plan 03-03's run_tune_sweep (tune) and Plan 03-05's run_oos_validation (OOS) both call verify_frozen() first, before any grid iteration, regime lookup, or DB write |
| T-03-07 | Repudiation | regimes.py | mitigate | test_regime_config.py asserts tune_end < oos_start for all 6 regimes — an inverted or overlapping split would silently corrupt the OOS honesty guarantee |
| T-03-SC | Tampering | pip/npm/cargo installs | accept | No new package installs this plan (03-RESEARCH.md's Package Legitimacy Audit: not applicable, zero new dependencies this phase) |
</threat_model>

<verification>
`python -m pytest tests/test_regime_config.py tests/test_exit_grid.py tests/test_frozen_config.py -q` passes. `python -m trader.backtest.backfill_universe` completes with nonzero rows for all 25 symbols. Full suite (`python -m pytest -q`) stays green.
</verification>

<success_criteria>
STRAT-03's grid, STRAT-04/05's regime and tune/OOS splits are frozen in committed code, hash-gated against post-hoc edits at both the tune-sweep and OOS-validation entrypoints, and the full universe is cached offline — later waves' sweeps run against real data with zero further network calls.
</success_criteria>

<output>
Create `.planning/phases/03-strategy-lab/03-02-SUMMARY.md` when done.
</output>
