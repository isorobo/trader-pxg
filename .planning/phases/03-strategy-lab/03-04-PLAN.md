---
phase: 03-strategy-lab
plan: 04
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - trader/backtest/run_tune_sweep_all.py
  - tests/test_run_tune_sweep_all.py
autonomous: true
requirements: [STRAT-03, STRAT-04, STRAT-05]

must_haves:
  truths:
    - "The full D-06/D-10 tune sweep has actually executed against real cached history for every strategy/bucket/regime combination, not a subset"
    - "The persisted top-5 candidate list is reproducible from the database alone (run_id lookups), not just from the JSON artifact"
    - "Every persisted candidate's strategy_id is the exact composite f'{strategy_id}_{bucket}' string, so Plan 03-05's strip-suffix lookup can never be broken by an inconsistent tagging convention"
  artifacts:
    - path: "trader/backtest/run_tune_sweep_all.py"
      provides: "Real tune-sweep execution across both strategies, all 3 buckets, all 6 regimes"
      exports: ["main"]
    - path: "reports/backtests/tune_top5.json"
      provides: "Real (non-fixture) top-5 tune candidates per strategy/bucket/regime combo, feeding Plan 03-05"
  key_links:
    - from: "trader/backtest/run_tune_sweep_all.py"
      to: "trader/backtest/sweep.py"
      via: "sweep.run_tune_sweep + sweep.select_top5"
      pattern: "run_tune_sweep\\(|select_top5\\("
---

<objective>
Execute the real D-06/D-10 tune sweep against the full cached universe: both strategies, all 3 asset-class buckets, all 6 regimes, and persist the top-5 candidates per combination for Plan 03-05's out-of-sample validation.

Purpose: STRAT-03/04/05 require the sweep to actually run, not merely be engineered. This plan is the ~16-30 minute offline acceptance run 03-RESEARCH.md's Sweep Engineering section benchmarked.
Output: `trader/backtest/run_tune_sweep_all.py`, `reports/backtests/tune_top5.json` (real data).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-strategy-lab/03-RESEARCH.md

<interfaces>
From trader/backtest/sweep.py (Plan 03-03, consume only):
  run_tune_sweep(strategy_fn, strategy_id, bucket, regime, bars_by_symbol, universe, conn, sweep_id, seed=20260726) -> list[dict]
  select_top5(cell_results, min_trades=30) -> list[dict]
  DEFAULT_SWEEP_ID: str

From trader/backtest/universe.py (Plan 03-02, consume only):
  UNIVERSE_BY_BUCKET: dict[str, list[str]]

From trader/backtest/regimes.py (Plan 03-02, consume only):
  REGIMES: tuple[Regime, ...]  # filter by .bucket

From trader/backtest/strategies/momentum.py, trader/backtest/strategies/breakout.py (Plan 03-01, consume only):
  pick_entries(iterator, date, open_positions, rng) -> list[str]

From trader/data/api.py (unchanged, consume only):
  get_daily_bars(symbol, asset_class=None, conn=None) -> pd.DataFrame  # cache-hit only after Plan 03-02's backfill
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: run_tune_sweep_all.py + fast fixture test</name>
  <files>trader/backtest/run_tune_sweep_all.py, tests/test_run_tune_sweep_all.py</files>
  <read_first>
    trader/backtest/sweep.py (run_tune_sweep/select_top5 exact signatures from Plan 03-03)
    trader/backtest/universe.py (UNIVERSE_BY_BUCKET)
    trader/backtest/regimes.py (REGIMES)
    trader/backtest/strategies/momentum.py, trader/backtest/strategies/breakout.py
    .planning/phases/03-strategy-lab/03-RESEARCH.md Sweep Engineering runtime table
  </read_first>
  <acceptance_criteria>
    - `python -m pytest tests/test_run_tune_sweep_all.py -q -x` passes in under 10 seconds using an injected tiny grid.
    - The written JSON's total entry count never exceeds 60 (5 candidates x 2 strategies x 3 buckets x 2 regimes).
    - Every written candidate's `strategy_id` field equals exactly `f"{strategy_id}_{bucket}"` (e.g. `"momentum_stock"`, `"breakout_new_memecoin"`) — asserted directly in the fast fixture test, not just implied by the loop structure.
  </acceptance_criteria>
  <action>
    Create trader/backtest/run_tune_sweep_all.py with `main(conn=None, grid_fn=None) -> Path`. `grid_fn` is a test-only injection point (defaults to `exit_grid.exit_profile_grid` when None — production calls never override it; document this explicitly in the module docstring so the hook is never mistaken for a production knob). For (strategy_id, pick_entries) in [("momentum", momentum.pick_entries), ("breakout", breakout.pick_entries)]: for bucket in universe.UNIVERSE_BY_BUCKET: for regime in [r for r in regimes.REGIMES if r.bucket == bucket]: load each universe symbol's full cached bars via `get_daily_bars(symbol, asset_class=None if "/" in symbol else "stock", conn=conn)` (cache-hit only — no live calls, Plan 03-02 Task 3 already backfilled); build the composite tag `composite_strategy_id = f"{strategy_id}_{bucket}"` EXACTLY once per (strategy_id, bucket) pair and reuse that same local variable everywhere it is needed in this iteration — never re-derive it inline at a second call site, so there is exactly one place the tagging convention is spelled out; call `sweep.run_tune_sweep(pick_entries, composite_strategy_id, bucket, regime, bars_by_symbol, universe.UNIVERSE_BY_BUCKET[bucket], conn, sweep_id=sweep.DEFAULT_SWEEP_ID)`; call `sweep.select_top5(results)`; set `candidate["strategy_id"] = composite_strategy_id` on every selected candidate dict (this is the exact same composite string `run_tune_sweep` already persisted into `params_json["strategy"]` — Plan 03-05's `run_oos_validation_all.py` strips the trailing `_{bucket}` suffix from this exact field to recover the base strategy name, so it must never drift from this one convention) before accumulating. Write the combined candidate list (all 12 strategy x bucket x regime combos' top-5s, at most 60 entries) as JSON to `reports/backtests/tune_top5.json` (mkdir parents=True). Return the Path. Write a fast fixture test using a 2-symbol/~40-bar synthetic universe with an injected 2x2x1x1 `grid_fn`, asserting completion under 5s, a well-formed JSON schema (list of dicts with strategy_id/bucket/regime/params/run_id/metrics keys), and that every entry's `strategy_id` matches the `f"{strategy_id}_{bucket}"` composite exactly.
  </action>
  <verify>
    <automated>python -m pytest tests/test_run_tune_sweep_all.py -q -x</automated>
  </verify>
  <done>run_tune_sweep_all.py runs end-to-end against a fast fixture; output artifact schema is stable for Plan 03-05 to consume; the strategy_id tagging convention is spelled out in exactly one place and directly test-proven.</done>
</task>

<task type="auto">
  <name>Task 2: Real acceptance run — the full tune sweep</name>
  <files>trader/backtest/run_tune_sweep_all.py</files>
  <read_first>
    trader/backtest/run_tune_sweep_all.py (Task 1's just-committed script)
    .planning/phases/03-strategy-lab/03-VALIDATION.md sampling-rate section (per-plan acceptance run, not per-commit)
  </read_first>
  <action>
    Execute `python -m trader.backtest.run_tune_sweep_all` for real against the shared data/trader.db (conn=None, default grid_fn=exit_grid.exit_profile_grid) — the ~16-30 minute offline sweep 03-RESEARCH.md's Sweep Engineering section benchmarked (2 strategies x [270 stock + 270 crypto_major_legacy_meme + 360 new_memecoin cells] x 2 regimes per bucket, all against cached bars only, zero network calls). Before declaring success, compute the exact expected backtest_runs row-count delta (2 strategies x 2 regimes x (270+270+360) cells = 3600) and assert the real delta matches exactly — do not eyeball it.
  </action>
  <acceptance_criteria>
    - `reports/backtests/tune_top5.json` exists on disk with real run_ids and real metrics (not the Task 1 fixture's synthetic values), at most 60 entries.
    - Every entry's run_id resolves to a real backtest_runs row with `ledger.get_trades_for_run(conn, run_id)` returning >= 30 trades (matches the min-trade floor that made it eligible for top-5 selection).
    - backtest_runs row count delta equals the pre-computed expected total (3600) exactly.
  </acceptance_criteria>
  <verify>
    <automated>python -m trader.backtest.run_tune_sweep_all</automated>
  </verify>
  <done>The real tune sweep has executed end-to-end across every strategy/bucket/regime combination; reports/backtests/tune_top5.json holds the real top-5 (or fewer) candidates per combo, ready for Plan 03-05's OOS validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run_tune_sweep_all.py -> data/trader.db (cache-hit only) | No network; reads only what Plan 03-02's backfill already wrote |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-03-12 | Repudiation | run_tune_sweep_all.py | mitigate | Acceptance run asserts the exact expected backtest_runs row-count delta (3600) before declaring success — a silently-interrupted sweep is caught by count mismatch, not assumed complete |
| T-03-13 | Tampering | run_tune_sweep_all.py | accept | `grid_fn` injection point exists only for the Task 1 fast fixture test; production path always calls exit_grid.exit_profile_grid directly with no override — documented in the module docstring as a test-only hook |
</threat_model>

<verification>
`python -m pytest tests/test_run_tune_sweep_all.py -q` passes (fast fixture). `python -m trader.backtest.run_tune_sweep_all` completes for real with the exact expected row-count delta. Full suite (`python -m pytest -q`) stays green.
</verification>

<success_criteria>
The real tune sweep has run against every strategy/bucket/regime combination, honestly, with D-10's top-5 rule as the only selection path. `reports/backtests/tune_top5.json` is real and ready for OOS validation, with a single, test-proven strategy_id tagging convention.
</success_criteria>

<output>
Create `.planning/phases/03-strategy-lab/03-04-SUMMARY.md` when done.
</output>
