---
phase: 03-strategy-lab
plan: 08
type: execute
wave: 7
depends_on: ["03-07"]
files_modified:
  - trader/backtest/sweep_v2.py
  - trader/backtest/run_tune_sweep_all_v2.py
  - trader/backtest/run_oos_validation_all_v2.py
  - trader/backtest/sweep_report_v2.py
  - trader/backtest/write_kill_conditions_v2.py
  - tests/test_sweep_engine_v2.py
  - tests/test_run_tune_sweep_all_v2.py
  - tests/test_oos_validation_v2.py
  - tests/test_kill_conditions_v2.py
  - .planning/phases/03-strategy-lab/KILL-CONDITIONS.md
autonomous: true
requirements: [STRAT-03, STRAT-04, STRAT-05, STRAT-06]

must_haves:
  truths:
    - "The full v2 grid (exit grid x 3 entry variants x 2 regimes x 2 strategies, per bucket) has actually executed against real cached history — 10,800 tune-sweep runs total, matching the pre-registered D-14 estimate exactly"
    - "The v2 tune-sweep driver is checkpoint-resumable: an interrupted run, re-launched, skips every (strategy, bucket, regime, variant) unit already recorded, never re-running or double-counting it"
    - "v2's OOS validation runs behind the same verify_frozen_v2() hash gate v2's tune sweep already enforces"
    - "KILL-CONDITIONS.md is regenerated from v2's real oos_results only, with the same defence-in-depth frozen-config check v1's terminal gate already performs"
    - "v1's tune_top5.json, oos_results.json, and every v1-dated reports/backtests/*.md file remain on disk, byte-unmodified"
  artifacts:
    - path: "trader/backtest/sweep_v2.py"
      provides: "v2 tune-sweep + OOS-validation engine, entry-variant-aware, hash-gated by frozen_config_v2"
      exports: ["run_tune_sweep_v2", "run_oos_validation_v2"]
    - path: "trader/backtest/run_tune_sweep_all_v2.py"
      provides: "Real, checkpoint-resumable v2 tune-sweep driver (~10,800 runs, 4-5h, detached process)"
      exports: ["main"]
    - path: "trader/backtest/run_oos_validation_all_v2.py"
      provides: "Real v2 OOS validation driver over every v2 top-5 candidate"
      exports: ["main"]
    - path: "trader/backtest/write_kill_conditions_v2.py"
      provides: "Regenerates .planning/phases/03-strategy-lab/KILL-CONDITIONS.md from v2's real oos_results_v2.json (D-16)"
      exports: ["main"]
    - path: "reports/backtests/tune_top5_v2.json"
      provides: "Real v2 top-5 tune candidates, entry-variant tagged"
    - path: "reports/backtests/oos_results_v2.json"
      provides: "Every v2 candidate's OOS metrics + verdict"
  key_links:
    - from: "trader/backtest/sweep_v2.py"
      to: "trader/backtest/frozen_config_v2.py"
      via: "run_tune_sweep_v2 and run_oos_validation_v2 both call verify_frozen_v2() first"
      pattern: "verify_frozen_v2\\("
    - from: "trader/backtest/sweep_v2.py"
      to: "trader/backtest/sweep.py"
      via: "reuses select_top5 and determine_survivor UNCHANGED (D-15) — imported, never redefined"
      pattern: "from trader\\.backtest\\.sweep import|sweep\\.select_top5|sweep\\.determine_survivor"
    - from: "trader/backtest/write_kill_conditions_v2.py"
      to: ".planning/phases/03-strategy-lab/KILL-CONDITIONS.md"
      via: "regenerates the same file D-16 designates for v2 replacement"
      pattern: "KILL-CONDITIONS\\.md"
---

<objective>
Run the real v2 sweep — the frozen entry-variant x exit-grid grid across every bucket/regime, ~10,800 tune-sweep runs — validate every resulting top-5 candidate out-of-sample behind v2's hash gate, and regenerate `KILL-CONDITIONS.md` and the survivors report from v2's real results (D-16), while leaving every v1 artifact and data file untouched.

Purpose: Plan 03-07 froze v2's config; this plan proves it. D-14's estimate (~10,800 runs, 4-5h single-threaded) is pre-registered — this plan's Task 2 arithmetic must match it exactly before any run starts, and the driver must survive an interruption without losing completed work, since a multi-hour single-threaded run is a real operational risk.
Output: `sweep_v2.py`, `run_tune_sweep_all_v2.py`, `run_oos_validation_all_v2.py`, `sweep_report_v2.py`, `write_kill_conditions_v2.py`, real `reports/backtests/tune_top5_v2.json` / `oos_results_v2.json`, and a regenerated `.planning/phases/03-strategy-lab/KILL-CONDITIONS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-strategy-lab/03-CONTEXT.md
@.planning/phases/03-strategy-lab/03-RESEARCH.md

<interfaces>
From trader/backtest/sweep.py (v1, READ-ONLY — reuse these three, never redefine):
  _slice_bars(bars_by_symbol: dict, start: str | None, end: str) -> dict
  select_top5(cell_results: list[dict], min_trades: int = 30) -> list[dict]   # D-15: UNCHANGED floor and rule
  determine_survivor(oos_metrics: dict, min_trades: int = 15) -> str          # D-15: UNCHANGED floor and rule

From trader/backtest/frozen_config_v2.py (Plan 03-07, consume only):
  verify_frozen_v2(repo_root=None) -> None   # raises RuntimeError on hash mismatch

From trader/backtest/regimes_v2.py (Plan 03-07, consume only):
  REGIMES_V2: tuple[Regime, ...]   # 6 regimes, 2 per bucket, OOS >= 12mo everywhere

From trader/backtest/strategies/momentum_v2.py, trader/backtest/strategies/breakout_v2.py (Plan 03-07, consume only):
  MOMENTUM_VARIANTS / BREAKOUT_VARIANTS: dict[str, Variant]   # keys "strict"/"base"/"loose"
  make_pick_entries(variant) -> Callable[[iterator, date, open_positions, rng], list[str]]

From trader/backtest/run_tune_sweep_all.py / run_oos_validation_all.py (v1, READ-ONLY pattern reference — the exact composite strategy_id tagging convention `f"{strategy_id}_{bucket}"` and the exit_grid module-attribute monkeypatch trick for test-only tiny-grid injection):
  main(conn=None, grid_fn=None) -> Path   # v1's shape to mirror for v2's own drivers

From trader/backtest/sweep_report.py (v1, READ-ONLY — reuse write_sweep_summary UNCHANGED; run_id-disambiguated filenames make it collision-safe for v2 reuse):
  write_sweep_summary(oos_result: dict, tune_candidate: dict, conn, base_dir="reports/backtests") -> Path

From trader/backtest/write_kill_conditions.py (v1, READ-ONLY — reuse these constants/helpers, never redefine):
  PF_FLOOR = 0.9; MAX_DD_FLOOR = -0.15; MAX_DD_MULTIPLIER = 1.5; CONSECUTIVE_LOSS_KILL = 8
  _max_drawdown_trigger(observed_max_drawdown) -> float
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sweep_v2.py — variant-aware tune sweep + OOS validation engine</name>
  <files>trader/backtest/sweep_v2.py, tests/test_sweep_engine_v2.py</files>
  <read_first>
    trader/backtest/sweep.py (run_tune_sweep/run_oos_validation/_slice_bars/select_top5/determine_survivor — the exact pattern to mirror for tune/OOS, and the exact functions to import and reuse unchanged, per D-15)
    trader/backtest/frozen_config_v2.py, trader/backtest/regimes_v2.py (Plan 03-07)
    trader/backtest/strategies/momentum_v2.py, trader/backtest/strategies/breakout_v2.py (Plan 03-07's make_pick_entries factories)
    .planning/phases/03-strategy-lab/03-CONTEXT.md D-14 (entry-gate strictness as a swept dimension) and D-15 (floors/fees/exit grid UNCHANGED)
  </read_first>
  <behavior>
    - run_tune_sweep_v2() calls frozen_config_v2.verify_frozen_v2() before any grid iteration or DB write; a tampered FROZEN_HASH_V2 raises RuntimeError with a call-count spy on runner.run_backtest proving ZERO calls occurred.
    - Every cell's persisted params_json (read back from conn) contains sweep_id=="v2", regime, split=="tune", asset_class_bucket, strategy, AND entry_variant (the new v2-only provenance key) — five original keys plus one new one.
    - With a tiny fixture universe and an injected small grid, run_tune_sweep_v2 for ONE variant returns one result dict per grid cell (same {run_id, params, metrics} shape as v1's run_tune_sweep) — this function itself does not loop over variants; the caller (Task 2's driver) does, calling it once per variant and concatenating.
    - Bars passed to run_backtest are sliced to the regime's [tune_start, tune_end] via the reused sweep._slice_bars — never a new slicing implementation.
    - run_oos_validation_v2() calls frozen_config_v2.verify_frozen_v2() before any regime lookup or run_backtest call; tampered hash raises RuntimeError with zero run_backtest calls (identical pattern to v1's own OOS hash-gate test, T-03-14).
    - run_oos_validation_v2() looks up each candidate's regime from regimes_v2.REGIMES_V2 by (bucket, label) — never v1's regimes.REGIMES.
    - sweep.select_top5 and sweep.determine_survivor, called directly from this module (imported, not reimplemented), behave identically on v2 data as they do on v1 data — proven by feeding both v1-shaped and v2-shaped (with an extra entry_variant key) cell-result dicts through the same imported functions and observing identical selection/verdict behavior.
  </behavior>
  <action>
    Create trader/backtest/sweep_v2.py importing `_slice_bars`, `select_top5`, `determine_survivor` from `trader.backtest.sweep` (reuse verbatim, D-15) and `verify_frozen_v2` from `trader.backtest.frozen_config_v2`. Define `DEFAULT_SWEEP_ID_V2 = "v2"`. Implement `run_tune_sweep_v2(strategy_fn, strategy_id: str, bucket: str, regime, entry_variant_name: str, bars_by_symbol: dict, universe: list[str], conn, sweep_id: str = DEFAULT_SWEEP_ID_V2, seed: int = 20260726) -> list[dict]`: call `verify_frozen_v2()` first, uncaught; slice bars via `_slice_bars(bars_by_symbol, regime.tune_start, regime.tune_end)`; for each `profile` in `exit_grid.exit_profile_grid(bucket)` (imported from `trader.backtest.exit_grid`, UNCHANGED grid, D-15), build `params` with the same 5 keys v1's `run_tune_sweep` builds (`profile_name`, `sweep_id`, `regime=regime.label`, `split="tune"`, `asset_class_bucket=bucket`, `strategy=strategy_id`, plus the 4 exit-profile fields) PLUS `entry_variant=entry_variant_name`; call `runner.run_backtest(strategy_fn, universe, profile, sliced_bars, seed, params, strategy_id, conn)`; compute metrics via `ledger.get_trades_for_run` + `metrics.compute_metrics`; append `{"run_id": run_id, "params": params, "metrics": computed}`. Implement `run_oos_validation_v2(candidates: list[dict], bars_by_symbol_by_bucket: dict, conn, sweep_id: str = DEFAULT_SWEEP_ID_V2, seed: int = 20260726) -> list[dict]` mirroring v1's `run_oos_validation` exactly, except: call `verify_frozen_v2()` (not v1's `verify_frozen`), and look each candidate's regime up from `regimes_v2.REGIMES_V2` (imported from `trader.backtest.regimes_v2`) by `(candidate["bucket"], candidate["regime"])` instead of v1's `regimes.REGIMES`. Every other detail (EXIT_PROFILE reconstruction from `candidate["params"]`, `_slice_bars` to `[regime.oos_start, regime.oos_end]`, `run_backtest` call with `split="oos"`, metrics computation) is identical to v1's implementation.
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_sweep_engine_v2.py -q -x` passes.
    - The hash-gate tests prove RuntimeError + zero run_backtest calls for both run_tune_sweep_v2 and run_oos_validation_v2 on a tampered FROZEN_HASH_V2.
    - Every persisted v2 cell's params_json round-trips through json.loads with all 6 provenance keys (5 shared + entry_variant) present.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_sweep_engine_v2.py -q -x</automated>
  </verify>
  <done>sweep_v2.py implements the variant-aware tune-sweep and OOS-validation engine, hash-gated by v2's own frozen_config_v2, reusing select_top5/determine_survivor unchanged from v1 per D-15.</done>
</task>

<task type="auto">
  <name>Task 2: Real v2 drivers — checkpoint-resumable tune sweep + OOS validation</name>
  <files>trader/backtest/run_tune_sweep_all_v2.py, trader/backtest/run_oos_validation_all_v2.py, tests/test_run_tune_sweep_all_v2.py, tests/test_oos_validation_v2.py</files>
  <read_first>
    trader/backtest/run_tune_sweep_all.py (the exact composite strategy_id tagging convention and exit_grid monkeypatch-injection pattern to mirror)
    trader/backtest/run_oos_validation_all.py (the exact strip-suffix strategy_fn resolution pattern to mirror, extended here to also resolve entry_variant)
    trader/backtest/sweep_v2.py (Task 1)
    trader/backtest/regimes_v2.py, trader/backtest/strategies/momentum_v2.py, trader/backtest/strategies/breakout_v2.py (Plan 03-07)
  </read_first>
  <acceptance_criteria>
    - `python -m pytest tests/test_run_tune_sweep_all_v2.py tests/test_oos_validation_v2.py -q -x` passes in under 15 seconds using injected tiny grids/fixtures.
    - The checkpoint-resume fixture test pre-populates 1 of a small synthetic combo set in the checkpoint file, runs `main()`, and asserts (via a call-count spy on `sweep_v2.run_tune_sweep_v2`) that ONLY the missing combo(s) were executed — the pre-populated combo's results are reused from the checkpoint file, not re-computed.
    - Every written v2 candidate's `strategy_id` field equals the exact `f"{strategy_id}_{bucket}"` composite (same convention as v1, reused verbatim) AND its `params["entry_variant"]` is one of "strict"/"base"/"loose".
    - The real run-count arithmetic is stated and self-checked: 2 strategies x 3 entry variants x 2 regimes x 270 cells = 3,240 (stock) + 3,240 (crypto_major_legacy_meme) + 2 x 3 x 2 x 360 = 4,320 (new_memecoin) = 10,800 total tune-sweep runs — matching D-14's pre-registered estimate exactly.
  </acceptance_criteria>
  <action>
    Create trader/backtest/run_tune_sweep_all_v2.py with `main(conn=None, grid_fn=None) -> Path`. Define the 12 (strategy_name, bucket, regime) combos by iterating `("momentum", momentum_v2.MOMENTUM_VARIANTS, momentum_v2.make_pick_entries)` and `("breakout", breakout_v2.BREAKOUT_VARIANTS, breakout_v2.make_pick_entries)` against `universe.UNIVERSE_BY_BUCKET` and `[r for r in regimes_v2.REGIMES_V2 if r.bucket == bucket]` (mirrors v1's `run_tune_sweep_all.py` loop shape exactly, extended with a variants dimension) — giving 36 total (strategy, bucket, regime, variant) units of work. Maintain a JSON-lines checkpoint file at `reports/backtests/tune_v2.checkpoint.jsonl`: on startup, read every existing line into a dict keyed by `(strategy_name, bucket, regime.label, variant_name)`; for each of the 36 units, if its key is already checkpointed, reuse its stored cell-results list and skip re-running it; otherwise build `composite_strategy_id = f"{strategy_name}_{bucket}"` (ONCE per (strategy_name, bucket) pair, same single-source-of-truth convention as v1), build the variant-bound `pick_entries_fn` via the appropriate `make_pick_entries(variant)`, call `sweep_v2.run_tune_sweep_v2(pick_entries_fn, composite_strategy_id, bucket, regime, variant_name, bars_by_symbol, symbols, conn, sweep_id="v2")`, and IMMEDIATELY append `{"strategy": strategy_name, "bucket": bucket, "regime": regime.label, "variant": variant_name, "results": results}` as one new line to the checkpoint file (flushed to disk before moving to the next unit — a crash after unit N preserves units 1..N). Use the same `exit_grid.exit_profile_grid` module-attribute swap trick as v1's driver for the `grid_fn` test-only injection point (never used in production). Once all 36 units are present in the checkpoint, group them by `(strategy_name, bucket, regime.label)`, concatenate each group's 3 variants' cell-results into one combined list, call `sweep.select_top5(combined)` once per of the 12 groups (D-10/D-15, unchanged), tag every selected candidate's `strategy_id`/`bucket`/`regime` exactly as v1's driver does, write the combined candidate list to `reports/backtests/tune_top5_v2.json`, and return that Path. Document in the module docstring: launch this as a detached/background process (e.g. redirecting stdout/stderr to `reports/backtests/tune_v2_run.log` and backgrounding it) since the real run takes an estimated 4-5 hours; if interrupted for any reason, simply re-invoking `python -m trader.backtest.run_tune_sweep_all_v2` resumes from the checkpoint rather than restarting from zero — no unit is ever run twice, and no completed unit's results are ever lost. Create trader/backtest/run_oos_validation_all_v2.py mirroring `run_oos_validation_all.py`'s shape: read `reports/backtests/tune_top5_v2.json`, group by bucket, load bars, resolve each candidate's base strategy_name by stripping the `_{bucket}` suffix from `strategy_id` (same convention, reused), resolve its `pick_entries_fn` from `MOMENTUM_VARIANTS`/`BREAKOUT_VARIANTS` using `candidate["params"]["entry_variant"]` bound through the matching `make_pick_entries`, call `sweep_v2.run_oos_validation_v2(...)`, apply `sweep.determine_survivor` (reused, D-15) per result, and write the FULL list (every verdict, not only survivors) to `reports/backtests/oos_results_v2.json`. Run for real: launch `run_tune_sweep_all_v2` detached/backgrounded and wait for the checkpoint to reach all 36 units (or re-launch on interruption) before running `python -m trader.backtest.run_oos_validation_all_v2` (well under a minute — at most 5 candidates x 12 combos = 60 OOS runs, same ceiling as v1).
  </action>
  <verify>
    <automated>python -m pytest tests/test_run_tune_sweep_all_v2.py tests/test_oos_validation_v2.py -q -x</automated>
  </verify>
  <done>run_tune_sweep_all_v2.py and run_oos_validation_all_v2.py exist, checkpoint-resume is fixture-proven, and both have been run for real: reports/backtests/tune_top5_v2.json holds the real 10,800-run sweep's top-5 candidates (entry-variant tagged) and reports/backtests/oos_results_v2.json holds every candidate's real OOS verdict. v1's tune_top5.json/oos_results.json are untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate KILL-CONDITIONS.md and v2 reports from real v2 results</name>
  <files>trader/backtest/sweep_report_v2.py, trader/backtest/write_kill_conditions_v2.py, tests/test_kill_conditions_v2.py, .planning/phases/03-strategy-lab/KILL-CONDITIONS.md</files>
  <read_first>
    trader/backtest/write_kill_conditions.py (the exact structure, constants, and defence-in-depth hash-gate pattern to mirror — PF_FLOOR/MAX_DD_FLOOR/MAX_DD_MULTIPLIER/CONSECUTIVE_LOSS_KILL/_max_drawdown_trigger are imported and reused, never redefined)
    trader/backtest/sweep_report.py (write_sweep_summary is reused UNCHANGED — its filename includes the tune run_id, so v2's distinct run_ids make it collision-safe against v1's already-written reports; write_survivors_index is NOT reused as-is because its filename has no run_id/version disambiguator and would silently overwrite v1's dated survivors index)
    reports/backtests/oos_results_v2.json, reports/backtests/tune_top5_v2.json (real, from Task 2)
    .planning/phases/03-strategy-lab/03-CONTEXT.md D-16 (v1 artifacts remain untouched; KILL-CONDITIONS.md is regenerated from v2's oos_results only, after the v2 cycle completes)
  </read_first>
  <acceptance_criteria>
    - `.planning/phases/03-strategy-lab/KILL-CONDITIONS.md` is regenerated (overwritten) after the real run, sourced ONLY from `oos_results_v2.json`.
    - `python -m pytest tests/test_kill_conditions_v2.py -q -x` parses the regenerated file and asserts either (a) one entry per `verdict == "survivor"` in the real `oos_results_v2.json`, each with exactly the same 3 numeric-trigger fields v1's format uses, or (b) the exact "nothing survived" sentence when there are zero v2 survivors.
    - `write_kill_conditions_v2.main()` calls `frozen_config_v2.verify_frozen_v2()` as its first statement, before reading any v2 result file (defence in depth, mirroring v1's own T-03-20 pattern).
    - `reports/backtests/{today}-survivors-v2.md` exists (v2's own, distinctly-named survivors index) and every `reports/backtests/*-run{run_id}-sweep.md` file for a v2 run_id exists (one per `oos_results_v2.json` entry) — and every v1-dated report file present before this task started still exists, byte-unmodified.
  </acceptance_criteria>
  <action>
    Create trader/backtest/sweep_report_v2.py with a single new function `write_survivors_index_v2(oos_results: list[dict], base_dir: str = "reports/backtests") -> Path` mirroring `sweep_report.write_survivors_index`'s exact logic (survivor table or the "nothing survived — N candidates tested across M combinations" sentence, plus D-05's caveat appended verbatim — reuse `sweep_report.D05_CAVEAT`, import it, do not restate it) but writing to `{base_dir}/{today_str}-survivors-v2.md` instead of `{today_str}-survivors.md`, so it can never collide with or overwrite v1's already-committed survivors index. Do not touch `sweep_report.py`; `write_sweep_summary` is called directly from Task 3's driver, reused as-is (its filename already includes the tune `run_id`, making it collision-safe for v2 reuse without modification). Create trader/backtest/write_kill_conditions_v2.py mirroring `write_kill_conditions.py`'s structure: import `PF_FLOOR`, `MAX_DD_FLOOR`, `MAX_DD_MULTIPLIER`, `CONSECUTIVE_LOSS_KILL`, `_max_drawdown_trigger`, `NOTHING_SURVIVED_SENTENCE` from `trader.backtest.write_kill_conditions` (reuse verbatim — D-15's "floors unchanged" spirit extends to these kill-condition constants too, and importing avoids ever letting the two definitions drift); `main(conn=None) -> Path` that calls `frozen_config_v2.verify_frozen_v2()` FIRST (defence in depth, before reading anything); reads `reports/backtests/oos_results_v2.json` and `reports/backtests/tune_top5_v2.json`; for every `oos_results_v2.json` entry calls `sweep_report.write_sweep_summary(result, matching_tune_candidate, conn)` (looked up by `run_id`, same as v1); calls `sweep_report_v2.write_survivors_index_v2(oos_results_v2)`; builds the same kill-conditions markdown structure v1's `build_kill_conditions_text` produces (one entry per v2 survivor with the 3 numeric triggers, or the shared `NOTHING_SURVIVED_SENTENCE`) and OVERWRITES `.planning/phases/03-strategy-lab/KILL-CONDITIONS.md` with it — this is the one file D-16 explicitly designates for v2 regeneration, not an exception to the "leave v1 artifacts untouched" rule but its intended fulfillment. Run for real: `python -m trader.backtest.write_kill_conditions_v2`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_kill_conditions_v2.py -q -x</automated>
  </verify>
  <done>KILL-CONDITIONS.md is regenerated from v2's real results (2-3 concrete survivors with numeric triggers, or the honest nothing-survived statement) per D-16; v2's own survivors-v2.md and per-config sweep reports exist; every v1 report file and v1 data JSON remains byte-unmodified on disk.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run_tune_sweep_all_v2.py -> data/trader.db (cache-hit only) | No network; all v2 regime windows fall within v1's already-completed full-history backfill |
| run_tune_sweep_all_v2.py -> tune_v2.checkpoint.jsonl | Local file-based resumability state; read on startup, appended incrementally |
| write_kill_conditions_v2.py -> .planning/phases/03-strategy-lab/KILL-CONDITIONS.md | The one file D-16 designates for intentional v2 overwrite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-03-25 | Tampering | sweep_v2.run_tune_sweep_v2 / run_oos_validation_v2 | mitigate | `frozen_config_v2.verify_frozen_v2()` called first in both, mirroring v1's T-03-06/T-03-14 pattern exactly but scoped to v2's own 5-file frozen surface |
| T-03-26 | Repudiation | run_tune_sweep_all_v2.py checkpoint | mitigate | Checkpoint file is appended after EVERY unit (not just at the end); a crash mid-run loses at most the currently-in-flight unit, never previously-completed ones; the fixture test proves re-running `main()` never re-executes an already-checkpointed unit |
| T-03-27 | Repudiation | run_tune_sweep_all_v2.py's 10,800-run arithmetic | mitigate | The exact expected total (3,240 + 3,240 + 4,320 = 10,800) is computed and asserted before the real run is trusted as complete — matches D-14's pre-registered estimate exactly, not eyeballed |
| T-03-28 | Tampering | this plan's write surface vs v1 data artifacts | mitigate | Acceptance criteria explicitly verify every pre-existing v1-dated `reports/backtests/*.md` file and `tune_top5.json`/`oos_results.json` remain byte-unmodified after this plan's tasks run — v2 writes only to `-v2`-suffixed or run_id-disambiguated files, except KILL-CONDITIONS.md's designated (D-16) overwrite |
| T-03-29 | Repudiation | write_kill_conditions_v2.main() | mitigate | `verify_frozen_v2()` called first, before oos_results_v2.json is even read — defence in depth, mirroring v1's T-03-20 exactly |
</threat_model>

<verification>
`python -m pytest tests/test_sweep_engine_v2.py tests/test_run_tune_sweep_all_v2.py tests/test_oos_validation_v2.py tests/test_kill_conditions_v2.py -q` passes (fast fixtures). The real detached tune sweep completes (~4-5h, checkpoint-resumable), `run_oos_validation_all_v2` completes (<1 min), and `write_kill_conditions_v2` completes, regenerating the real `KILL-CONDITIONS.md`. Full suite (`python -m pytest -q`) stays green — v1's tests are unaffected since no v1 file changed.
</verification>

<success_criteria>
The real v2 sweep has run to completion (10,800 tune-sweep runs, matching the pre-registered estimate), every top-5 candidate has a real OOS verdict, and `KILL-CONDITIONS.md` reflects v2's honest outcome — either 2-3 concrete survivors with pre-registered numeric kill triggers, or the equally valid, explicitly-reported "nothing survived" statement — with zero modification to any v1 artifact or data file.
</success_criteria>

<output>
Create `.planning/phases/03-strategy-lab/03-08-SUMMARY.md` when done.
</output>
