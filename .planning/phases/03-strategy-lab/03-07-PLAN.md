---
phase: 03-strategy-lab
plan: 07
type: execute
wave: 6
depends_on: ["03-01", "03-02"]
files_modified:
  - trader/backtest/regimes_v2.py
  - trader/backtest/strategies/momentum_v2.py
  - trader/backtest/strategies/breakout_v2.py
  - trader/backtest/frozen_config_v2.py
  - tests/test_regime_config_v2.py
  - tests/test_entry_variants_v2.py
  - tests/test_frozen_config_v2.py
autonomous: true
requirements: [STRAT-03, STRAT-04, STRAT-05]

must_haves:
  truths:
    - "v2's regime windows give every bucket an OOS window of at least 12 months, fixing the 4-6 month windows that starved v1's 15-trade floor"
    - "v2's entry-gate strictness (3 momentum + 3 breakout variants) is frozen in code before any v2 result exists, exactly like v1's exit grid"
    - "The v2 'base' variant reproduces v1's momentum/breakout defaults byte-for-byte — v2 adds a new swept dimension, it does not silently change what v1 already proved"
    - "A byte-level change to any v2-frozen file (regimes_v2.py, momentum_v2.py, breakout_v2.py, or the still-shared universe.py/exit_grid.py) is detected and blocks v2 sweeps from running"
    - "No v1 file (momentum.py, breakout.py, regimes.py, exit_grid.py, frozen_config.py, sweep.py, universe.py, run_tune_sweep_all.py, run_oos_validation_all.py, sweep_report.py, write_kill_conditions.py, or any 03-01..03-06 PLAN.md) is modified by this plan"
  artifacts:
    - path: "trader/backtest/regimes_v2.py"
      provides: "D-13's frozen v2 regime windows — 6 regimes (2 per bucket), OOS >= 12 months everywhere, tune_end < oos_start"
      exports: ["REGIMES_V2"]
    - path: "trader/backtest/strategies/momentum_v2.py"
      provides: "D-14's 3 momentum entry variants (strict/base/loose) as a parameterized pick_entries factory"
      exports: ["MomentumVariant", "MOMENTUM_VARIANTS", "make_pick_entries"]
    - path: "trader/backtest/strategies/breakout_v2.py"
      provides: "D-14's 3 breakout entry variants (strict/base/loose contraction-gate strictness) as a parameterized pick_entries factory"
      exports: ["BreakoutVariant", "BREAKOUT_VARIANTS", "make_pick_entries"]
    - path: "trader/backtest/frozen_config_v2.py"
      provides: "v2's hash-based freeze gate over regimes_v2 + entry variants + the still-shared universe/grid files"
      exports: ["compute_hash_v2", "verify_frozen_v2", "FROZEN_HASH_V2", "FROZEN_FILES_V2"]
  key_links:
    - from: "trader/backtest/frozen_config_v2.py"
      to: "trader/backtest/regimes_v2.py"
      via: "FROZEN_FILES_V2 lists regimes_v2.py's path for hashing"
      pattern: "regimes_v2\\.py"
    - from: "trader/backtest/strategies/momentum_v2.py"
      to: "trader/backtest/iterator.py"
      via: "the returned pick_entries closure reads iterator.history(symbol) point-in-time, matching v1's contract exactly"
      pattern: "iterator\\.history\\("
---

<objective>
Freeze v2's regime windows (D-13) and entry-variant registry (D-14) in new, standalone modules — none of them modifying any v1 file — and gate both behind a new v2 hash-verification mechanism, exactly mirroring v1's frozen-before-results discipline but scoped to v2's own frozen surface.

Purpose: v1 concluded honestly with 0 survivors / 15 insufficient_sample. The owner-approved diagnosis (03-CONTEXT.md D-13/D-14, pre-registered before any v2 result exists) is that v1's 4-6 month OOS windows were arithmetically incompatible with the 15-trade floor, and breakout's single fixed entry gate produced zero eligible 30-trade tune cells. v2 fixes both root causes — longer OOS windows, entry-gate strictness as a swept dimension — while leaving every floor, fee model, and the exit grid itself untouched (D-15). This plan is config only; Plan 03-08 runs it.
Output: `regimes_v2.py`, `strategies/momentum_v2.py`, `strategies/breakout_v2.py`, `frozen_config_v2.py`, all committed with FROZEN_HASH_V2 locked. v1's `regimes.py`, `strategies/momentum.py`, `strategies/breakout.py`, `frozen_config.py`, and every other v1 artifact remain byte-for-byte untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-strategy-lab/03-CONTEXT.md
@.planning/phases/03-strategy-lab/03-RESEARCH.md

<interfaces>
From trader/backtest/regimes.py (v1, READ-ONLY — reuse the Regime class, never redefine it):
  Regime(bucket: str, label: str, tune_start: str | None, tune_end: str, oos_start: str, oos_end: str)  # frozen dataclass; import and reuse for REGIMES_V2's entries

From trader/backtest/universe.py (v1, READ-ONLY, unchanged this phase per D-13 — v2 uses the SAME universe):
  BUCKET_STOCK = "stock"; BUCKET_CRYPTO_MAJOR_LEGACY_MEME = "crypto_major_legacy_meme"; BUCKET_NEW_MEMECOIN = "new_memecoin"
  UNIVERSE_BY_BUCKET: dict[str, list[str]]

From trader/backtest/exit_grid.py (v1, READ-ONLY, unchanged per D-15):
  exit_profile_grid(bucket: str) -> Iterator[EXIT_PROFILE]  # 270 cells stock/crypto_major_legacy_meme, 360 new_memecoin — v2 reuses this verbatim, no new grid

From trader/backtest/strategies/momentum.py, trader/backtest/strategies/breakout.py (v1, READ-ONLY — mirror their exact RSI-Wilder/NR7 logic, never import or modify them):
  pick_entries(iterator, date, open_positions, rng) -> list[str]
  momentum.py's RSI_MOMENTUM_FLOOR=60, VOLUME_SURGE_MULT=2.0, RSI_PERIOD=14, BREAK_LOOKBACK=20
  breakout.py's NR_WINDOW=7, VOLUME_CONFIRM_MULT=1.5, BREAKOUT_LOOKBACK=20 (no-retest, unchanged scope this phase)

From trader/backtest/frozen_config.py (v1, READ-ONLY — same hashing pattern, new file for v2, not an extension of this one):
  compute_hash(repo_root=None) -> str; verify_frozen(repo_root=None) -> None; FROZEN_FILES; FROZEN_HASH
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Frozen v2 regime windows (D-13)</name>
  <files>trader/backtest/regimes_v2.py, tests/test_regime_config_v2.py</files>
  <read_first>
    trader/backtest/regimes.py (Regime dataclass — import and reuse, do not redefine or modify)
    trader/backtest/universe.py (BUCKET_STOCK/BUCKET_CRYPTO_MAJOR_LEGACY_MEME/BUCKET_NEW_MEMECOIN constants)
    .planning/phases/03-strategy-lab/03-CONTEXT.md D-13 (v2's regime-window requirements: OOS >= 12 months per bucket, or max available for short-history memecoins; tune fitted to available history; tune_end < oos_start preserved)
    .planning/phases/03-strategy-lab/03-RESEARCH.md Universe section (stock 10+yr history; BTC/ETH since 2017; DOGE since 2019; SHIB since 2021; PEPE since 2023-05-05; BONK since 2023-12-15; WIF since 2024-03-05) and Regime Windows section (v1's original dates, superseded here, never modified in place)
  </read_first>
  <behavior>
    - regimes_v2.REGIMES_V2 is a tuple of exactly 6 Regime instances, exactly 2 per bucket (stock, crypto_major_legacy_meme, new_memecoin) — same structure as v1, new dates.
    - Every regime's tune_end < oos_start (ISO string compare).
    - Every regime's OOS window (oos_end - oos_start) is >= 365 days (12 months), computed via date arithmetic, not eyeballed.
    - Only new_memecoin's "mania_recovery_v2" regime has tune_start is None (symbol-relative — PEPE/BONK/WIF each list on Binance at a different date, mirroring v1's own mania regime); every other regime has a fixed tune_start string.
    - regimes_v2.REGIMES_V2's entries are `trader.backtest.regimes.Regime` instances (isinstance check) — proving this module reuses v1's class rather than defining a parallel one.
  </behavior>
  <action>
    Create trader/backtest/regimes_v2.py importing `Regime` from `trader.backtest.regimes` (reuse, never redefine) and the three bucket constants from `trader.backtest.universe`. Define `REGIMES_V2: tuple[Regime, ...]` with exactly these 6 frozen entries, derived from 03-RESEARCH.md's verified data facts (current project date 2026-07-26 — stocks have 10+ years cached, so extending tune backward and OOS forward is safe; crypto/memecoin dates are bounded by each symbol's real first-Binance-candle date already verified in research): stock/"trending_v2" (tune_start="2019-01-01", tune_end="2024-06-30", oos_start="2024-07-01", oos_end="2026-06-30" — 24mo OOS, extends v1's trending tune backward for more tune data and its OOS forward using real post-2024 history that has since accumulated); stock/"choppy_v2" (tune_start="2015-01-01", tune_end="2016-06-30" — identical to v1's choppy tune — oos_start="2016-07-01", oos_end="2017-12-31" — 18mo OOS, extending v1's original 6-month OOS tail using real SPY-era history); crypto_major_legacy_meme/"trending_v2" (tune_start="2023-01-01", tune_end="2024-06-30" — identical to v1's crypto trending tune — oos_start="2024-07-01", oos_end="2026-06-30" — 24mo OOS); crypto_major_legacy_meme/"bear_recovery_v2" (tune_start="2022-01-01", tune_end="2022-12-31" — the full 2022 bear year as tune, wider than v1's 8-month tune — oos_start="2023-01-01", oos_end="2024-12-31" — 24mo OOS, covering the real 2023-2024 recovery/bull period, giving far denser trade counts than v1's thin 4-month 2022 OOS tail); new_memecoin/"mania_recovery_v2" (tune_start=None — symbol-relative, PEPE/BONK/WIF's own first candle — tune_end="2024-12-31", oos_start="2025-01-01", oos_end="2025-12-31" — exactly 12mo OOS); new_memecoin/"current_v2" (tune_start="2025-01-01", tune_end="2025-06-30", oos_start="2025-07-01", oos_end="2026-06-30" — exactly 12mo OOS, using WIF's full available history up to one month before the project's current date of 2026-07-26, safely within what backfill_universe.py's existing (v1) full-history cache already covers — no new backfill task is needed this plan). Do not touch trader/backtest/regimes.py or its REGIMES tuple.
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_regime_config_v2.py -q -x` passes.
    - `regimes_v2.REGIMES_V2` has exactly 6 entries, 2 per bucket, every OOS window >= 365 days, every tune_end < oos_start.
    - `trader/backtest/regimes.py`'s `REGIMES` tuple is byte-identical to its state before this task (verified by re-reading the file and comparing against the version read in `<read_first>`).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_regime_config_v2.py -q -x</automated>
  </verify>
  <done>regimes_v2.py committed with 6 frozen v2 windows, every OOS window >= 12 months, tune_end < oos_start proven for all 6; v1's regimes.py is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Entry-variant registry (D-14) — momentum + breakout</name>
  <files>trader/backtest/strategies/momentum_v2.py, trader/backtest/strategies/breakout_v2.py, tests/test_entry_variants_v2.py</files>
  <read_first>
    trader/backtest/strategies/momentum.py (mirror this EXACT RSI-Wilder/volume-surge/break logic — read every line, do not import it, reimplement it parameterized)
    trader/backtest/strategies/breakout.py (mirror this EXACT NR7/volume-confirm/break logic — read every line, do not import it, reimplement it parameterized)
    tests/test_strategy_momentum.py, tests/test_strategy_breakout.py (v1's fixture-builder patterns — reuse the same fixture shapes to prove v2's "base" variant matches v1's real output on the same fixtures)
    .planning/phases/03-strategy-lab/03-CONTEXT.md D-14 (3 momentum variants on RSI threshold + volume multiplier; 3 breakout variants on contraction-gate strictness; defined in code before results)
  </read_first>
  <behavior>
    - MOMENTUM_VARIANTS has exactly 3 keys: "strict", "base", "loose". BREAKOUT_VARIANTS has exactly 3 keys: "strict", "base", "loose".
    - MOMENTUM_VARIANTS["base"].rsi_floor == 60.0 and .volume_surge_mult == 2.0 (v1's exact constants); MOMENTUM_VARIANTS["strict"].rsi_floor == 70.0 and .volume_surge_mult == 3.0 (tighter); MOMENTUM_VARIANTS["loose"].rsi_floor == 50.0 and .volume_surge_mult == 1.5 (looser).
    - BREAKOUT_VARIANTS["base"].nr_window == 7 and .volume_confirm_mult == 1.5 (v1's exact constants); BREAKOUT_VARIANTS["strict"].nr_window == 10 and .volume_confirm_mult == 2.0; BREAKOUT_VARIANTS["loose"].nr_window == 4 and .volume_confirm_mult == 1.2.
    - Behavioral parity proof: given the SAME rising-price/volume-surge fixture tests/test_strategy_momentum.py's `test_momentum_signals_on_rising_fixture` uses, `momentum_v2.make_pick_entries(MOMENTUM_VARIANTS["base"])(iterator, date, set(), rng)` returns the IDENTICAL picks list as the real `momentum.pick_entries(iterator, date, set(), rng)` on that same fixture. Same parity proof for breakout_v2's "base" variant against breakout.py's own fixture.
    - Monotonic strictness sanity check: on a fixture engineered to fire under "loose" thresholds but not under "strict" thresholds (e.g. RSI/volume/NR-window values between the strict and loose cutoffs), the "loose" variant signals and the "strict" variant does not, for both momentum and breakout.
    - Neither module imports `trader.backtest.strategies.momentum` or `trader.backtest.strategies.breakout` — each is fully self-contained (grep-verified).
  </behavior>
  <action>
    Create trader/backtest/strategies/momentum_v2.py: module constants RSI_PERIOD=14, BREAK_LOOKBACK=20 (fixed across all variants, per D-14 — only RSI threshold and volume multiplier vary); a frozen `@dataclass MomentumVariant(name: str, rsi_floor: float, volume_surge_mult: float)`; `MOMENTUM_VARIANTS: dict[str, MomentumVariant]` with "strict"=(70.0, 3.0), "base"=(60.0, 2.0 — reproduces v1 exactly), "loose"=(50.0, 1.5); a private `_rsi_wilder(closes, period=RSI_PERIOD)` helper duplicating momentum.py's own Wilder-smoothing implementation verbatim (this is intentional duplication, not modification — momentum.py is never imported or touched); `make_pick_entries(variant: MomentumVariant) -> Callable` returning a closure matching the exact `pick_entries(iterator, date, open_positions, rng) -> list[str]` 4-argument contract (the variant is bound via closure, not a 5th positional argument, because `runner.run_backtest` calls every `strategy_fn` uniformly with exactly 4 args and that engine contract is never modified) — the closure's body is byte-for-byte momentum.py's own signal logic, with `variant.rsi_floor`/`variant.volume_surge_mult` replacing the hardcoded `RSI_MOMENTUM_FLOOR`/`VOLUME_SURGE_MULT`, baseline/prior-high windows still excluding today's own bar (`history[-(N+1):-1]`). Create trader/backtest/strategies/breakout_v2.py identically: BREAKOUT_LOOKBACK=20 fixed; frozen `@dataclass BreakoutVariant(name, nr_window: int, volume_confirm_mult: float)`; `BREAKOUT_VARIANTS` with "strict"=(10, 2.0), "base"=(7, 1.5 — reproduces v1 exactly), "loose"=(4, 1.2); `make_pick_entries(variant) -> Callable` returning a closure duplicating breakout.py's NR7-contraction/volume-confirm/no-retest logic parameterized by `variant.nr_window`/`variant.volume_confirm_mult` (no-retest scope stays fixed across all 3 variants, unchanged from v1's orchestrator-resolved scope decision).
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_entry_variants_v2.py -q -x` passes.
    - `momentum_v2.MOMENTUM_VARIANTS["base"]` and `breakout_v2.BREAKOUT_VARIANTS["base"]`'s closures produce output identical to the real `momentum.pick_entries`/`breakout.pick_entries` on v1's own fixture-bar tests.
    - `grep -c "from trader.backtest.strategies import momentum\b\|from trader.backtest.strategies.momentum import\|import trader.backtest.strategies.momentum$" trader/backtest/strategies/momentum_v2.py` returns 0 (no v1 module import — full self-containment), and the equivalent check for breakout_v2.py/breakout.py returns 0.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_entry_variants_v2.py -q -x</automated>
  </verify>
  <done>momentum_v2.py and breakout_v2.py committed with 3 pinned variants each; "base" proven byte-for-byte identical to v1's real strategy output on v1's own fixtures; v1's momentum.py/breakout.py are untouched and never imported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: v2 hash-based freeze gate</name>
  <files>trader/backtest/frozen_config_v2.py, tests/test_frozen_config_v2.py</files>
  <read_first>
    trader/backtest/frozen_config.py (the exact hashing/verify pattern to mirror — a NEW file, not an extension of this one)
    trader/backtest/regimes_v2.py, trader/backtest/strategies/momentum_v2.py, trader/backtest/strategies/breakout_v2.py (Task 1/2's just-committed files)
    .planning/phases/03-strategy-lab/03-CONTEXT.md D-13/D-14/D-15 (v2's frozen surface: regimes_v2 + entry variants + the still-shared, unchanged universe/exit_grid)
  </read_first>
  <behavior>
    - frozen_config_v2.compute_hash_v2() == frozen_config_v2.FROZEN_HASH_V2 (self-consistency at commit time — this equality IS the v2 freeze point).
    - frozen_config_v2.verify_frozen_v2() raises RuntimeError when any one of FROZEN_FILES_V2's 5 entries' bytes differ from what FROZEN_HASH_V2 was computed from (same tmp_path-copy-with-monkeypatched-FROZEN_FILES_V2 tamper simulation as v1's test_frozen_config.py).
    - frozen_config_v2.FROZEN_FILES_V2 contains exactly these 5 paths, in this order: trader/backtest/universe.py, trader/backtest/regimes_v2.py, trader/backtest/exit_grid.py, trader/backtest/strategies/momentum_v2.py, trader/backtest/strategies/breakout_v2.py.
    - Tampering v1's frozen_config.FROZEN_HASH (unrelated to this module) does NOT trip verify_frozen_v2() — the two gates are fully independent (v1's sweep/OOS entrypoints stay hash-gated by v1's own frozen_config.py, untouched).
  </behavior>
  <action>
    Create trader/backtest/frozen_config_v2.py with `FROZEN_FILES_V2: tuple[str, ...] = ("trader/backtest/universe.py", "trader/backtest/regimes_v2.py", "trader/backtest/exit_grid.py", "trader/backtest/strategies/momentum_v2.py", "trader/backtest/strategies/breakout_v2.py")` — universe.py and exit_grid.py are included because D-13/D-15 keep them as part of v2's frozen input surface even though their content is unchanged from v1; `compute_hash_v2(repo_root: Path | None = None) -> str` (sha256 over each FROZEN_FILES_V2 entry's raw bytes, in list order — a fresh, small implementation, not a call into frozen_config.compute_hash, since that function's FROZEN_FILES is module-scoped to v1's own 3-file list with no override parameter); `FROZEN_HASH_V2` hard-coded from a one-time `compute_hash_v2()` run after Tasks 1-2 are finalized; `verify_frozen_v2(repo_root: Path | None = None) -> None` raising RuntimeError (message citing standing rule 1 and D-13's v2 pre-registration) when `compute_hash_v2(repo_root) != FROZEN_HASH_V2`. Do not modify trader/backtest/frozen_config.py, its FROZEN_FILES, or its FROZEN_HASH in any way.
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_frozen_config_v2.py -q -x` passes.
    - `verify_frozen_v2()` raises RuntimeError on any byte-level change to any of the 5 v2 frozen files; raises nothing when unchanged.
    - `trader/backtest/frozen_config.py`'s `FROZEN_HASH` constant is byte-identical to its value before this task.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_frozen_config_v2.py -q -x</automated>
  </verify>
  <done>frozen_config_v2.py committed with FROZEN_HASH_V2 matching the 5 v2-relevant files' actual current contents; Plan 03-08's sweep/OOS entrypoints can call verify_frozen_v2() as a hard, uncatchable gate, fully independent of v1's own frozen_config.verify_frozen().</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| regimes_v2/momentum_v2/breakout_v2 -> frozen_config_v2 | Local file-content trust boundary, independent of v1's own frozen_config.py |
| This plan -> v1 source files | Read-only consumption boundary — no write access exercised against any v1 artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-03-21 | Tampering | frozen_config_v2.py | mitigate | FROZEN_HASH_V2 computed once and hard-coded at commit time; verify_frozen_v2() recomputes and raises RuntimeError on any later edit to regimes_v2.py/momentum_v2.py/breakout_v2.py/universe.py/exit_grid.py, to be enforced at every v2 sweep AND validation entrypoint (Plan 03-08) |
| T-03-22 | Repudiation | regimes_v2.py | mitigate | test_regime_config_v2.py asserts tune_end < oos_start AND oos window >= 365 days for all 6 v2 regimes — a regime that silently reintroduces v1's thin-OOS failure mode would fail this gate before any v2 sweep could run |
| T-03-23 | Tampering | momentum_v2.py, breakout_v2.py | mitigate | Neither module imports v1's momentum.py/breakout.py (grep-verified); the "base" variant's behavioral-parity test proves v2 does not silently change what v1 already validated, only adds new swept variants alongside it |
| T-03-24 | Tampering | this plan's write surface | mitigate | Acceptance criteria explicitly re-read and byte-compare trader/backtest/regimes.py and trader/backtest/frozen_config.py's committed constants before/after this plan's tasks — any accidental v1 write is caught, not assumed absent |
</threat_model>

<verification>
`python -m pytest tests/test_regime_config_v2.py tests/test_entry_variants_v2.py tests/test_frozen_config_v2.py -q` passes. Full suite (`python -m pytest -q`) stays green — v1's 150+ tests (including all of Phase 3's v1 tests) are unaffected since no v1 file changed.
</verification>

<success_criteria>
D-13's v2 regime windows (OOS >= 12 months everywhere) and D-14's entry-variant registry (3 momentum + 3 breakout variants, "base" == v1 exactly) are frozen in committed code and hash-gated, with zero modification to any v1 artifact. Ready for Plan 03-08 to drive the real v2 sweep.
</success_criteria>

<output>
Create `.planning/phases/03-strategy-lab/03-07-SUMMARY.md` when done.
</output>
