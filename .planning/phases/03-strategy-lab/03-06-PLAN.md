---
phase: 03-strategy-lab
plan: 06
type: execute
wave: 5
depends_on: ["03-05"]
files_modified:
  - trader/backtest/sweep_report.py
  - trader/backtest/write_kill_conditions.py
  - tests/test_sweep_report.py
  - tests/test_kill_conditions.py
  - .planning/phases/03-strategy-lab/KILL-CONDITIONS.md
autonomous: true
requirements: [STRAT-06]

must_haves:
  truths:
    - "Every reported survivor has a written, numeric, pre-registered kill condition before Phase 4 starts"
    - "If nothing survived, that outcome is stated plainly in both KILL-CONDITIONS.md and the survivors report — never hidden or silently reinterpreted as success"
    - "The survivors report carries D-05's survivorship-bias caveat, so a reader never mistakes this fixed universe's results for a claim about the live scanner universe"
  artifacts:
    - path: ".planning/phases/03-strategy-lab/KILL-CONDITIONS.md"
      provides: "STRAT-06's pre-registered kill-condition gate, committed before Phase 4"
    - path: "trader/backtest/sweep_report.py"
      provides: "Per-config markdown reports (tune vs OOS side-by-side, per-symbol P&L) and the survivors index (D-12)"
      exports: ["write_sweep_summary", "write_survivors_index"]
    - path: "trader/backtest/write_kill_conditions.py"
      provides: "Real end-to-end run producing KILL-CONDITIONS.md and every reports/backtests/*.md artifact"
      exports: ["main"]
  key_links:
    - from: "trader/backtest/write_kill_conditions.py"
      to: "reports/backtests/oos_results.json"
      via: "reads the real OOS verdicts as the only input"
      pattern: "oos_results\\.json"
    - from: "trader/backtest/write_kill_conditions.py"
      to: ".planning/phases/03-strategy-lab/KILL-CONDITIONS.md"
      via: "writes one entry per survivor, or the explicit nothing-survived statement"
      pattern: "KILL-CONDITIONS\\.md"
---

<objective>
Produce the phase's final artifacts: per-config markdown sweep reports (tune vs OOS side by side, per-symbol P&L per the orchestrator's cross-asset correlation-visibility resolution), the survivors index, and `KILL-CONDITIONS.md` — a pre-registered, concrete numeric kill condition for every survivor, or the honest "nothing survived" statement (D-05/D-11/D-12).

Purpose: STRAT-06 is Phase 3's exit gate. This plan closes the phase either with 2-3 concrete, kill-condition-bearing survivors, or with an equally valid, explicitly-reported "nothing survived" outcome that sends work back to Phase 3, not forward (ROADMAP.md Phase 3 success criterion 3).
Output: `sweep_report.py`, `write_kill_conditions.py`, real `reports/backtests/*.md`, real `.planning/phases/03-strategy-lab/KILL-CONDITIONS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-strategy-lab/03-CONTEXT.md
@.planning/phases/03-strategy-lab/03-RESEARCH.md

<interfaces>
From trader/backtest/metrics.py (unchanged, consume only):
  write_report(run_id, metrics, strategy_id, base_dir="reports/backtests") -> Path   # existing single-run report pattern to extend, not duplicate
  METRIC_KEYS: tuple[str, ...]

From trader/backtest/ledger.py (unchanged, consume only):
  get_trades_for_run(conn, run_id) -> list[dict]   # each dict includes "symbol" and "pnl" — group by symbol for per-symbol P&L

From reports/backtests/oos_results.json (Plan 03-05, real data):
  list[dict] with keys candidate, oos_run_id, oos_metrics, verdict in {"survivor","insufficient_sample","killed"}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sweep_report.py — per-config summaries and survivors index</name>
  <files>trader/backtest/sweep_report.py, tests/test_sweep_report.py</files>
  <read_first>
    trader/backtest/metrics.py (write_report existing pattern, compute_metrics output keys)
    trader/backtest/ledger.py (get_trades_for_run for per-symbol breakdown)
    .planning/phases/03-strategy-lab/03-CONTEXT.md D-05 (survivorship-bias caveat: this fixed universe is NOT the live scanner universe — Phase 6 judges paper/scanner-fed results as the real test) and D-12
    .planning/phases/03-strategy-lab/03-RESEARCH.md Open Question 3 and the orchestrator's resolution ("Per-symbol P&L reported alongside aggregates in all sweep summaries") and Overfitting Guards #3 ("report the trial count")
  </read_first>
  <acceptance_criteria>
    - `python -m pytest tests/test_sweep_report.py -q -x` passes.
    - A zero-survivor fixture case produces the exact "Nothing survived" sentence quoting the real trial count, not an empty or missing file.
    - `write_survivors_index`'s output always includes D-05's survivorship-bias caveat line, in both the survivors and nothing-survived branches.
  </acceptance_criteria>
  <action>
    Create trader/backtest/sweep_report.py with `write_sweep_summary(oos_result: dict, tune_candidate: dict, conn, base_dir="reports/backtests") -> Path` producing `{base_dir}/{date}-{strategy}-{bucket}-{regime}-sweep.md` (per 03-RESEARCH.md's Recommended Project Structure): a tune-metrics table (from `tune_candidate["metrics"]`), an OOS-metrics table (from `oos_result["oos_metrics"]`), a per-symbol P&L table built by grouping `ledger.get_trades_for_run(conn, oos_result["oos_run_id"])` by `symbol` and summing `pnl` per symbol, and the verdict line. Create `write_survivors_index(oos_results: list[dict], base_dir="reports/backtests") -> Path` producing `{base_dir}/{date}-survivors.md`: a table of every `verdict == "survivor"` entry (strategy, bucket, regime, OOS profit_factor, OOS trade_count) if any exist; otherwise the literal line `"Nothing survived this sweep — {N} candidates tested across {M} strategy/bucket/regime combinations."` quoting the real `len(oos_results)` and combination count (03-RESEARCH.md Overfitting Guards #3). In both branches, append a fixed closing line stating D-05's caveat verbatim: "This fixed research universe is NOT the live scanner universe and carries survivorship bias — treat these results as cheap-kill signal, not a live-trading guarantee; Phase 6 judges scanner-fed paper results as the real test."
  </action>
  <verify>
    <automated>python -m pytest tests/test_sweep_report.py -q -x</automated>
  </verify>
  <done>sweep_report.py renders both the "some survivors" and "nothing survived" branches correctly from fixture data, matching D-12's reporting requirement and always carrying D-05's caveat.</done>
</task>

<task type="auto">
  <name>Task 2: write_kill_conditions.py — real run + phase-gate test</name>
  <files>trader/backtest/write_kill_conditions.py, tests/test_kill_conditions.py, .planning/phases/03-strategy-lab/KILL-CONDITIONS.md</files>
  <read_first>
    reports/backtests/oos_results.json (real, from Plan 03-05)
    .planning/phases/03-strategy-lab/03-CONTEXT.md D-11 (KILL-CONDITIONS.md path/content requirement, concrete numeric triggers, committed before Phase 4)
    .planning/ROADMAP.md Phase 3 success criteria and standing rule 1 (never edit graduation/kill criteria while looking at results)
  </read_first>
  <acceptance_criteria>
    - `.planning/phases/03-strategy-lab/KILL-CONDITIONS.md` exists after the real run.
    - `python -m pytest tests/test_kill_conditions.py -q -x` parses it and asserts either (a) one entry per survivor named in the real `reports/backtests/oos_results.json`, each with exactly 3 numeric triggers, or (b) the exact "Nothing survived" sentence when there are zero survivors.
    - `reports/backtests/*-sweep.md` files exist, one per oos_result entry, plus one `*-survivors.md` index file.
  </acceptance_criteria>
  <action>
    Create trader/backtest/write_kill_conditions.py with `main(conn=None) -> Path`: read `reports/backtests/oos_results.json`; filter to `verdict == "survivor"`; for each survivor compute three concrete, fixed numeric triggers from its own OOS metrics — (1) rolling-30-trade profit factor floor of 0.9 (fixed constant, matches ROADMAP.md's GRAD-03 vocabulary), (2) a max-drawdown kill level set to 1.5x the survivor's own observed OOS max_drawdown magnitude, floored at -15% (never a looser trigger than -15%), (3) a consecutive-loss kill count of 8 (fixed constant) — and append one markdown entry per survivor to `.planning/phases/03-strategy-lab/KILL-CONDITIONS.md` naming the strategy/bucket/regime/exit-profile and all three numeric triggers explicitly. If zero survivors, write the single required sentence: "Nothing survived this sweep — no kill conditions to register; work returns to Phase 3, not forward (ROADMAP.md Phase 3 success criterion 3)." Call `sweep_report.write_sweep_summary` for every oos_result entry (looking up its matching tune_top5.json candidate by run_id) and `sweep_report.write_survivors_index` for the full list, in this same script run, so reports and KILL-CONDITIONS.md are produced from one real execution over real data. Run it for real: `python -m trader.backtest.write_kill_conditions`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_kill_conditions.py -q -x</automated>
  </verify>
  <done>KILL-CONDITIONS.md is committed with a pre-registered, concrete, numeric kill trigger for every survivor (or the honest nothing-survived statement) before Phase 4 begins — phase exit criterion 2 is met either way.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| write_kill_conditions.py -> oos_results.json | In-process file read; sole source of truth for what gets a kill condition |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-03-17 | Repudiation | write_kill_conditions.py | mitigate | test_kill_conditions.py cross-checks the committed file 1:1 against the real oos_results.json survivor list — a missing or extra entry fails the gate |
| T-03-18 | Tampering | KILL-CONDITIONS.md | accept | Standing rule 1 is procedural from this point forward — Phase 3 enforces the FIRST write being correct; later phases' own gates (Phase 4/6) are responsible for refusing edits to this file, out of this plan's code-level reach |
| T-03-19 | Repudiation | sweep_report.write_survivors_index | mitigate | The exact trial count (N cells tested, M combinations) is quoted in the zero-survivor branch — "nothing survived" is never mistaken for "nothing was tried" |
</threat_model>

<verification>
`python -m pytest tests/test_sweep_report.py tests/test_kill_conditions.py -q` passes. `python -m trader.backtest.write_kill_conditions` completes for real, producing the committed KILL-CONDITIONS.md and every reports/backtests/*.md artifact. Full suite (`python -m pytest -q`) stays green — Phase 3 exit gate.
</verification>

<success_criteria>
Phase 3's three success criteria are all met: (1) either 2-3 profitable-OOS survivors are reported with real per-symbol P&L, or the honest "nothing survived" outcome is reported plainly; (2) every survivor has a pre-registered, concrete numeric kill condition in KILL-CONDITIONS.md before Phase 4; (3) the frozen-before-results discipline (grid, regimes, splits, top-5 rule, min-trade floors) has been enforced in code throughout, not merely by convention.
</success_criteria>

<output>
Create `.planning/phases/03-strategy-lab/03-06-SUMMARY.md` when done.
</output>
