---
phase: 03-strategy-lab
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - trader/backtest/strategies/__init__.py
  - trader/backtest/strategies/momentum.py
  - trader/backtest/strategies/breakout.py
  - tests/test_strategy_momentum.py
  - tests/test_strategy_breakout.py
autonomous: true
requirements: [STRAT-01, STRAT-02]

must_haves:
  truths:
    - "Momentum agent signals only when RSI(14) >= 60 AND today's volume > 2x its trailing 20-day average AND today's close breaks the prior 20-day high"
    - "Breakout agent signals only when today's high-low range is the narrowest of the last 7 bars (NR7) AND today's close breaks the prior 20-day high AND today's volume > 1.5x its trailing 20-day average"
    - "Neither agent ever re-signals a symbol already present in open_positions"
    - "Neither agent ever reads a bar dated after the current iterator pointer, and neither ever includes today's own bar in its rolling baseline"
  artifacts:
    - path: "trader/backtest/strategies/momentum.py"
      provides: "Momentum entry-signal pure function (STRAT-01)"
      exports: ["pick_entries", "RSI_PERIOD", "RSI_MOMENTUM_FLOOR", "VOLUME_SURGE_MULT", "BREAK_LOOKBACK"]
    - path: "trader/backtest/strategies/breakout.py"
      provides: "Breakout entry-signal pure function (STRAT-02)"
      exports: ["pick_entries", "NR_WINDOW", "BREAKOUT_LOOKBACK", "VOLUME_CONFIRM_MULT"]
    - path: "tests/test_strategy_momentum.py"
      provides: "Fixture-bar proof of every momentum signal rule"
    - path: "tests/test_strategy_breakout.py"
      provides: "Fixture-bar proof of every breakout signal rule"
  key_links:
    - from: "trader/backtest/strategies/momentum.py"
      to: "trader/backtest/iterator.py"
      via: "iterator.history(symbol) point-in-time read, iterator.symbols universe discovery"
      pattern: "iterator\\.history\\("
    - from: "trader/backtest/strategies/breakout.py"
      to: "trader/backtest/iterator.py"
      via: "iterator.history(symbol) point-in-time read"
      pattern: "iterator\\.history\\("
---

<objective>
Implement the two phase-document strategy agents (D-01) as pure functions matching Phase 2's shared `pick_entries(iterator, date, open_positions, rng)` contract: Momentum (RSI + volume surge) and Breakout (NR7 volatility contraction + 20-day-high, no-retest per the orchestrator's resolution of 03-RESEARCH.md Open Question 1).

Purpose: STRAT-01 and STRAT-02 are the entry-signal half of Phase 3's "kill cheaply" pipeline — the exit-parameter sweep (later waves) has nothing to sweep against until these exist. Both agents are long-only (Phase 2's `fills.worse_of_fill` raises `NotImplementedError` for any short-side exit — D-15 inherited constraint, not a Phase 3 decision).
Output: `trader/backtest/strategies/momentum.py`, `trader/backtest/strategies/breakout.py`, both fixture-proven against RED-then-GREEN tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-strategy-lab/03-CONTEXT.md
@.planning/phases/03-strategy-lab/03-RESEARCH.md

<interfaces>
From trader/backtest/iterator.py (unchanged, consume only):
  PointInTimeIterator.history(symbol) -> np.ndarray   # columns: open, high, low, close, volume; bounded at current pointer
  PointInTimeIterator.symbols -> list[str]             # universe discovery, construction order
  PointInTimeIterator.bar_on(symbol, date) -> dict | None  # NOT point-in-time bounded — do not use for signal generation

Shared strategy contract (trader/backtest/momentum_placeholder.py, trader/backtest/random_strategy.py):
  pick_entries(iterator, date, open_positions: set[str], rng: random.Random) -> list[str]
  - `date` and `rng` accepted for contract uniformity even if unused by a deterministic agent.
  - Never return a symbol already in open_positions.
  - Never signal with fewer than the agent's own lookback+1 bars of history.

From trader/backtest/config.py: asset_class is resolved downstream by the runner, not by strategies — strategies never construct or reference asset_class.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Momentum agent — RSI(14) + volume surge + 20-day-high break</name>
  <files>trader/backtest/strategies/__init__.py, trader/backtest/strategies/momentum.py, tests/test_strategy_momentum.py</files>
  <read_first>
    trader/backtest/momentum_placeholder.py (existing point-in-time discipline + LOOKBACK_DAYS convention to mirror)
    trader/backtest/iterator.py (history()/symbols contract)
    tests/test_backtest_strategies.py (fixture-builder patterns: _bars_with_closes, _momentum_universe — reuse this shape, do not invent a new one)
    .planning/phases/03-strategy-lab/03-RESEARCH.md Code Examples section ("RSI(14), volume surge, N-day-high signal") and Pattern 1/Common Pitfalls (baseline-window off-by-one)
    Strategys/07_momentum_trading.md (owner reference spec: RVOL>2x is the key filter, RSI not for overbought fades — momentum lives above 60-70, break of recent highs)
  </read_first>
  <behavior>
    - Signal fires: RSI(14) >= 60 AND today_volume > 2.0x trailing-20-day mean volume AND today_close > prior 20-day high (computed over history EXCLUDING today).
    - No signal: same price/volume shape but RSI computed below 60 (choppy/falling close sequence).
    - No signal: RSI and close-break conditions met, but today's volume is only 1.5x the trailing baseline (below the 2.0x floor).
    - No signal: fewer than BREAK_LOOKBACK(20) + 1 bars of history, regardless of shape (mirrors momentum_placeholder.py's own short-history guard).
    - No signal: symbol already present in open_positions, even when the fixture would otherwise fire.
    - Off-by-one guard: a fixture where including today's own volume in the baseline would mask a true surge (today's volume inflates its own 20-day average) must still fire — baseline is always `volumes[-(BREAK_LOOKBACK+1):-1]`, never `volumes[-BREAK_LOOKBACK:]`.
  </behavior>
  <action>
    Create trader/backtest/strategies/__init__.py (empty, package marker). Create trader/backtest/strategies/momentum.py with module-level fixed constants (never swept — Common Pitfall 1): RSI_PERIOD=14, RSI_MOMENTUM_FLOOR=60, VOLUME_SURGE_MULT=2.0, BREAK_LOOKBACK=20. Implement a private `_rsi_wilder(closes, period)` helper local to this module (Wilder's smoothed RSI over the last `period+1` closes via np.diff/gains/losses — do not import a shared helper; each strategy module stays self-contained per D-03's "pure functions, no state outside the ledger"). Implement `pick_entries(iterator, date, open_positions, rng) -> list[str]`: for each symbol in iterator.symbols, skip if in open_positions or len(history) < BREAK_LOOKBACK+1; compute closes=history[:,3], volumes=history[:,4], highs=history[:,1]; rsi=_rsi_wilder(closes, RSI_PERIOD); today_volume=volumes[-1]; baseline_volume=volumes[-(BREAK_LOOKBACK+1):-1].mean(); today_close=closes[-1]; prior_high=highs[-(BREAK_LOOKBACK+1):-1].max(); append symbol if rsi>=RSI_MOMENTUM_FLOOR and today_volume>VOLUME_SURGE_MULT*baseline_volume and today_close>prior_high. Long-only only — never construct a short-side signal path (Pitfall 4, inherited D-15 constraint).
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_strategy_momentum.py -q -x` passes with at least 6 distinct test functions covering every behavior listed above.
    - `trader/backtest/strategies/momentum.py` exports `pick_entries` with the exact 4-positional-argument signature `(iterator, date, open_positions, rng)`.
    - RED commit (failing test against a stub/absent momentum.py) exists before the GREEN commit that makes it pass.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_strategy_momentum.py -q -x</automated>
  </verify>
  <done>momentum.pick_entries implemented and green against all fixture cases above; module constants are fixed literals, not sweep-grid inputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Breakout agent — NR7 contraction + 20-day-high break, no-retest</name>
  <files>trader/backtest/strategies/breakout.py, tests/test_strategy_breakout.py</files>
  <read_first>
    trader/backtest/strategies/momentum.py (Task 1's just-committed sibling module — mirror its self-contained-helper style, do not share a helper module between the two)
    .planning/phases/03-strategy-lab/03-RESEARCH.md Code Examples section ("NR7 volatility contraction + 20-day-high breakout") and Open Question 1 (breakout retest variant — orchestrator resolution: fixed to no-retest for v1, maximises OOS trade count in the thin memecoin/bear windows)
    Strategys/03_breakout_trading.md (owner reference spec: tight-range/declining-ATR squeeze, volume > 1.5x 20-bar average, "preferred: wait for a retest" — explicitly NOT taken this phase per the orchestrator's locked resolution)
  </read_first>
  <behavior>
    - Signal fires: today's high-low range is the narrowest of the trailing 7 bars (NR7, inclusive of today) AND today_close > prior 20-day high (excluding today) AND today_volume > 1.5x trailing-20-day mean volume.
    - No signal: NR7 condition true but today's close does not break the prior 20-day high.
    - No signal: close-break true but today's range is not the narrowest of the last 7 (no real contraction).
    - No signal: NR7 + close-break true but volume is only 1.2x the trailing baseline (below the 1.5x floor).
    - No signal: fewer than BREAKOUT_LOOKBACK(20) + 1 bars of history.
    - No signal: symbol already present in open_positions.
    - No retest gate exists anywhere in this function — a single qualifying bar fires immediately (documents the orchestrator's no-retest resolution in code, not just in a comment).
  </behavior>
  <action>
    Create trader/backtest/strategies/breakout.py with module-level fixed constants: NR_WINDOW=7, BREAKOUT_LOOKBACK=20, VOLUME_CONFIRM_MULT=1.5. Implement `pick_entries(iterator, date, open_positions, rng) -> list[str]`: for each symbol in iterator.symbols, skip if in open_positions or len(history) < BREAKOUT_LOOKBACK+1; compute highs, lows, closes, volumes from history columns 1-4; ranges = highs-lows; is_nr7 = ranges[-1] == ranges[-NR_WINDOW:].min(); today_close=closes[-1]; prior_high=highs[-(BREAKOUT_LOOKBACK+1):-1].max(); today_volume=volumes[-1]; baseline_volume=volumes[-(BREAKOUT_LOOKBACK+1):-1].mean(); append symbol if is_nr7 and today_close>prior_high and today_volume>VOLUME_CONFIRM_MULT*baseline_volume. Add a one-line module docstring note stating the no-retest scope decision explicitly (Open Question 1's orchestrator resolution) so a future reader does not "fix" this as a missing feature.
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_strategy_breakout.py -q -x` passes with at least 6 distinct test functions covering every behavior listed above.
    - `trader/backtest/strategies/breakout.py` exports `pick_entries` with the exact 4-positional-argument signature.
    - RED commit exists before the GREEN commit.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_strategy_breakout.py -q -x</automated>
  </verify>
  <done>breakout.pick_entries implemented and green against all fixture cases; no-retest scope documented in the module docstring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iterator -> strategy | In-process only; no external input, no network, no user input this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-03-01 | Tampering | strategies/momentum.py, strategies/breakout.py | mitigate | Unit test asserts a symbol with fewer than lookback+1 bars never signals regardless of price/volume shape — `iterator.history()` is the only read path used, never `iterator.bar_on()` for a future date |
| T-03-02 | Tampering | strategies/momentum.py, strategies/breakout.py | mitigate | Baseline volume/high/range windows computed over `history[-(N+1):-1]`, never `history[-N:]` (excludes today's own bar) — dedicated off-by-one fixture test per agent |
| T-03-03 | Repudiation | strategies/momentum.py, strategies/breakout.py | accept | No persistence occurs in this plan; sweep provenance is enforced downstream in Plan 03-03's sweep.py |
| T-03-04 | Tampering | strategies/momentum.py, strategies/breakout.py | mitigate | Long-only only, per D-01/D-03/inherited D-15 — no code path constructs a negative quantity or a "sell short"/"buy to cover" signal; both reference specs' "mirror for short" is explicitly out of scope (Pitfall 4) |
</threat_model>

<verification>
Both agents pass their dedicated fixture-bar test files under `python -m pytest tests/test_strategy_momentum.py tests/test_strategy_breakout.py -q`. Full suite (`python -m pytest -q`) stays green (150 existing + new tests, no regressions to Phase 2's harness).
</verification>

<success_criteria>
STRAT-01 and STRAT-02 are implemented as pure functions matching the shared `pick_entries` contract, proven against fixture bars covering every fire/no-fire/edge condition, ready for Plan 03-03's sweep engine to drive them through `run_backtest` unmodified.
</success_criteria>

<output>
Create `.planning/phases/03-strategy-lab/03-01-SUMMARY.md` when done.
</output>
