---
phase: 03-strategy-lab
plan: 05
type: tdd
wave: 4
depends_on: ["03-04"]
files_modified:
  - trader/backtest/sweep.py
  - trader/backtest/run_oos_validation_all.py
  - tests/test_oos_validation.py
autonomous: true
requirements: [STRAT-04, STRAT-05]

must_haves:
  truths:
    - "Every top-5 tune candidate has been re-run against data its tuning never saw"
    - "A verdict of survivor requires both profitability after costs and at least 15 OOS trades — never one without the other"
  artifacts:
    - path: "trader/backtest/sweep.py"
      provides: "run_oos_validation + determine_survivor additions (STRAT-05's OOS rule as executable code)"
      exports: ["run_oos_validation", "determine_survivor"]
    - path: "trader/backtest/run_oos_validation_all.py"
      provides: "Real OOS validation run over every tune-sweep candidate"
      exports: ["main"]
    - path: "reports/backtests/oos_results.json"
      provides: "Every candidate's OOS metrics + verdict — the single source of truth for reporting and kill conditions"
  key_links:
    - from: "trader/backtest/run_oos_validation_all.py"
      to: "trader/backtest/sweep.py"
      via: "run_oos_validation + determine_survivor"
      pattern: "run_oos_validation\\(|determine_survivor\\("
---

<objective>
Validate every tune-sweep top-5 candidate against its regime's held-out OOS window (D-09), and determine each candidate's verdict — survivor, insufficient_sample, or killed — using a minimum-trade floor that prevents a thin lucky window from being reported as a survivor (Common Pitfall 3).

Purpose: STRAT-05's out-of-sample rule is the phase's core honesty mechanism. This plan makes it executable and runs it for real against Plan 03-04's real tune candidates.
Output: `sweep.run_oos_validation`/`determine_survivor`, `run_oos_validation_all.py`, `reports/backtests/oos_results.json` (real).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-strategy-lab/03-RESEARCH.md

<interfaces>
From trader/backtest/sweep.py (Plan 03-03, extend — do not duplicate):
  _slice_bars(bars_by_symbol, start, end) -> dict   # reuse this exact helper for OOS window slicing
  DEFAULT_SWEEP_ID: str

From trader/backtest/regimes.py (Plan 03-02, consume only):
  REGIMES: tuple[Regime, ...]  # look up by (bucket, label) to get oos_start/oos_end

From trader/backtest/config.py (unchanged, consume only):
  EXIT_PROFILE(stop_pct, tp_pct, scale_out, trailing_pct, max_hold_days, eod_flat)

From reports/backtests/tune_top5.json (Plan 03-04, real data):
  list[dict] with keys strategy_id, bucket, regime (label), params (stop_pct/tp_pct/trailing_pct/max_hold_days + provenance), run_id, metrics
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: run_oos_validation + determine_survivor</name>
  <files>trader/backtest/sweep.py, tests/test_oos_validation.py</files>
  <read_first>
    trader/backtest/sweep.py (Plan 03-03's _slice_bars/run_tune_sweep — extend this file, reuse _slice_bars, do not duplicate slicing logic)
    trader/backtest/exits.py, trader/backtest/config.py (EXIT_PROFILE reconstruction from stored params)
    .planning/phases/03-strategy-lab/03-RESEARCH.md Overfitting Guards #1 (OOS min-trade floor of 15) and Common Pitfall 3
  </read_first>
  <behavior>
    - Given a candidate dict (strategy_id, bucket, regime label matching a real regimes.Regime, params with stop_pct/tp_pct/trailing_pct/max_hold_days), run_oos_validation reconstructs an EXIT_PROFILE from those exact params and calls run_backtest with bars sliced to [regime.oos_start, regime.oos_end] only — a spy strategy_fn proves no bar date outside that window is ever visible.
    - determine_survivor(metrics, min_trades=15) returns "survivor" when trade_count>=15 and (profit_factor is math.inf or profit_factor>1.0).
    - determine_survivor returns "insufficient_sample" when trade_count<15, regardless of profit_factor.
    - determine_survivor returns "killed" when trade_count>=15 and profit_factor<=1.0.
    - A synthetic 4-trade OOS window with a high profit_factor still returns "insufficient_sample", never "survivor" (Pitfall 3 guard).
  </behavior>
  <action>
    Add `run_oos_validation(candidates: list[dict], bars_by_symbol_by_bucket: dict[str, dict], conn, sweep_id: str) -> list[dict]` to sweep.py: for each candidate, look up its matching `regimes.Regime` by (bucket, regime label) from `regimes.REGIMES`; reconstruct `config.EXIT_PROFILE(stop_pct=candidate["params"]["stop_pct"], tp_pct=candidate["params"]["tp_pct"], scale_out=(), trailing_pct=candidate["params"]["trailing_pct"], max_hold_days=candidate["params"]["max_hold_days"], eod_flat=False)`; slice `bars_by_symbol_by_bucket[bucket]` via the existing `_slice_bars` helper to `[regime.oos_start, regime.oos_end]`; call `runner.run_backtest(candidate["strategy_fn"], universe.UNIVERSE_BY_BUCKET[bucket], profile, sliced_bars, seed, params={**candidate["params"], "split": "oos", "sweep_id": sweep_id}, strategy_id=candidate["strategy_id"], conn=conn)` (candidate["strategy_fn"] is injected by the caller script, sweep.py itself holds no strategy-name registry); compute metrics via `ledger.get_trades_for_run` + `metrics.compute_metrics`; append `{"candidate": candidate, "oos_run_id": run_id, "oos_metrics": computed}`. Add `determine_survivor(oos_metrics: dict, min_trades: int = 15) -> str`: return "insufficient_sample" if `oos_metrics["trade_count"] < min_trades`; else "survivor" if `oos_metrics["profit_factor"] is math.inf or (oos_metrics["profit_factor"] or 0) > 1.0`; else "killed".
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_oos_validation.py -q -x` passes.
    - determine_survivor's three-way branch covers all cases above, including the Pitfall-3 thin-sample guard.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_oos_validation.py -q -x</automated>
  </verify>
  <done>run_oos_validation and determine_survivor implemented and unit-proven against fixture data; OOS bars provably restricted to the regime's oos_start/oos_end window.</done>
</task>

<task type="auto">
  <name>Task 2: run_oos_validation_all.py + real acceptance run</name>
  <files>trader/backtest/run_oos_validation_all.py</files>
  <read_first>
    reports/backtests/tune_top5.json (real, from Plan 03-04)
    trader/backtest/sweep.py (run_oos_validation/determine_survivor, Task 1)
    trader/backtest/strategies/momentum.py, trader/backtest/strategies/breakout.py
  </read_first>
  <action>
    Create trader/backtest/run_oos_validation_all.py with `main(conn=None) -> Path`: read `reports/backtests/tune_top5.json`; group candidates by bucket and load each bucket's universe bars once via `get_daily_bars` (cache-hit only, same pattern as Plan 03-04); build a `{"momentum": momentum.pick_entries, "breakout": breakout.pick_entries}` lookup and resolve each candidate's `strategy_fn` by stripping the `_{bucket}` suffix from its `strategy_id`; call `sweep.run_oos_validation(candidates, bars_by_symbol_by_bucket, conn, sweep.DEFAULT_SWEEP_ID)`; for each result call `sweep.determine_survivor(result["oos_metrics"])` and attach the verdict; write the FULL list (every candidate's OOS metrics + verdict — never filter to only survivors, D-12 requires reporting every trial) to `reports/backtests/oos_results.json`. Run it for real: `python -m trader.backtest.run_oos_validation_all` (well under a minute per 03-RESEARCH.md's runtime estimate — at most 60 runs against 4-8 month windows).
  </action>
  <acceptance_criteria>
    - `reports/backtests/oos_results.json` exists with one entry per Plan 03-04 candidate, each carrying a verdict in {"survivor", "insufficient_sample", "killed"}.
    - Running the script twice against the same tune_top5.json and same cached data reproduces identical verdicts (reproducibility, D-07/D-12).
  </acceptance_criteria>
  <verify>
    <automated>python -m trader.backtest.run_oos_validation_all</automated>
  </verify>
  <done>Every tune-sweep candidate has been validated against its held-out OOS window; reports/backtests/oos_results.json is the single source of truth Plan 03-06 reads to write kill conditions and the sweep reports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run_oos_validation -> regimes.REGIMES | In-process lookup; OOS window boundaries come only from the frozen regime config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-03-14 | Tampering | sweep.run_oos_validation | mitigate | Bars sliced to regime.oos_start/oos_end only via the same _slice_bars helper Plan 03-03 already tested; a spy-strategy_fn test proves no tune-window date ever reaches the OOS strategy_fn call |
| T-03-15 | Repudiation | sweep.determine_survivor | mitigate | trade_count<15 always returns "insufficient_sample" regardless of profit_factor magnitude — a thin lucky window can never be reported as a survivor |
| T-03-16 | Repudiation | run_oos_validation_all.py | mitigate | oos_results.json records every candidate's verdict, not only survivors — a "nothing survived" or "mostly insufficient sample" outcome stays visible in the artifact itself |
</threat_model>

<verification>
`python -m pytest tests/test_oos_validation.py -q` passes. `python -m trader.backtest.run_oos_validation_all` completes for real, producing a stable `oos_results.json`. Full suite stays green.
</verification>

<success_criteria>
STRAT-05's out-of-sample rule has actually run: every top-5 tune candidate has an OOS verdict, honestly labeled, with the thin-sample guard preventing a false "survivor."
</success_criteria>

<output>
Create `.planning/phases/03-strategy-lab/03-05-SUMMARY.md` when done.
</output>
